# backend/.env.example

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/screenwriter_db

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# Anthropic
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-6
AI_PROVIDER=anthropic

# Security
SECRET_KEY=your-secret-key-here-change-in-production

# Library admin gate (Phase 1.5). The book/agent/snippet library is global-read;
# only these emails (comma-separated) can upload/edit/delete library content.
# Empty: writes stay open in development and are locked in production.
ADMIN_EMAILS=yvesfogel@gmail.com

# CORS (comma-separated list)
ALLOWED_ORIGINS=http://localhost:4321,http://localhost:3000

# Rate limiting
MAX_TOKENS=1500
MAX_SECTION_LENGTH=1500

# Caching
CACHE_TTL=900

# Environment
ENVIRONMENT=development

# Server
PORT=8000

# Media storage. In production (Railway) set MEDIA_DIR to the mounted persistent
# volume path so uploads survive redeploys, e.g. MEDIA_DIR=/media
# MEDIA_DIR=/media

# MCP server base URL (metadata only — advertised issuer/resource_server_url).
# Static-bearer auth is unaffected by this value. Set to the public host in prod.
MCP_BASE_URL=http://localhost:8001

# MCP DNS-rebinding protection (validates inbound Host header on /mcp).
# Off by default (API-key auth is the primary defense). On a public host you may
# set this to true to harden the now-public surface.
MCP_DNS_REBINDING_PROTECTION=false

# Google Cloud (Imagen AI image generation)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
IMAGEN_MODEL=imagen-3.0-generate-001
IMAGEN_REGION=us-central1