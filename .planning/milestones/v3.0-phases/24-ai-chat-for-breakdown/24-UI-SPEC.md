---
phase: 24
slug: ai-chat-for-breakdown
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-20
---

# Phase 24 — UI Design Contract

> Visual and interaction contract for the AI Chat for Breakdown phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | Radix UI (existing) |
| Icon library | lucide-react |
| Font | DM Sans (body), Playfair Display (display), Courier Prime (screenplay) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding within message bubbles |
| sm | 8px | Compact element spacing, gap between confirmation card fields, ShotProposalCard header icon-to-label gap |
| md | 16px | Default element spacing, message area padding (p-4) |
| lg | 24px | Section padding, empty state vertical gaps |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (not used in this phase) |

Exceptions: Touch target for send button is 40px (10 x 4) minimum hit area, consistent with existing SidebarChat send button sizing.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (normal) | 1.625 (leading-relaxed) |
| Label | 10px (text-[10px]) | 600 (font-semibold) | 1.4 (leading-snug) |
| Heading | 12px (text-xs) | 600 (font-semibold) | 1.5 |
| Display | 14px (text-sm) | 400 (normal) | 1.5 |

Source: These match the existing SidebarChat typography exactly. Body text at 14px with leading-relaxed is used for all message content. Labels at 10px semibold uppercase with tracking-wider are used for section headers ("PROPOSED CHANGES", status indicators). Headings at 12px semibold uppercase tracking-wider are used for panel titles ("AI CHAT"). Display at 14px normal weight is used for empty state primary text, differentiated from Body by context (centered, muted color) rather than weight.

---

## Color

All colors use HSL CSS variables defined in `.breakdown-mode` scope in `index.css`. BreakdownChat inherits these automatically because `BreakdownLayout` adds `breakdown-mode` class to `<html>`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(220 13% 5%)` / var(--background) | Chat panel background, message area |
| Secondary (30%) | `hsl(220 12% 9%)` / var(--card) | AI message bubbles (bg-muted/50), input field (bg-input), confirmation cards |
| Accent (10%) | `hsl(213 80% 52%)` / var(--accent) | Send button background, input focus ring, streaming spinner, "Create Shot" confirmation button, shot creation success indicator |
| Destructive | `hsl(0 72% 51%)` / var(--destructive) | Error message bubbles only |

Accent reserved for: send button, input focus ring (focus:ring-2 focus:ring-primary/20), "Create Shot" and "Apply Changes" confirmation buttons on ShotProposalCard, streaming loading spinner, post-confirmation success checkmark icon.

Focal point: The send button (bg-primary, bottom-right of the chat panel) is the single dominant accent element and serves as the visual focal point of the BreakdownChat component.

### Breakdown-specific color mapping (from CONTEXT.md)

| SidebarChat element | BreakdownChat equivalent |
|---------------------|--------------------------|
| amber-500/15 (user bubble bg) | primary/15 (steel blue tint) |
| amber-500/10 (user bubble border) | primary/10 |
| amber-100 (user bubble text) | primary-foreground lightened to 92% |
| violet-500/25 (ProposedChangesCard border) | primary/25 (steel blue border) |
| violet-500/5 (ProposedChangesCard bg) | primary/5 |
| violet-400 (ProposedChangesCard icon + label) | primary (steel blue) |
| amber-400 (streaming indicator) | primary (steel blue) |
| amber-500/20 (focus ring) | primary/20 |

---

## Component Inventory

### New Components

| Component | Location | Purpose |
|-----------|----------|---------|
| `BreakdownChat` | `frontend/src/components/Breakdown/BreakdownChat.tsx` | Main chat component for breakdown mode right panel |
| `ShotProposalCard` | `frontend/src/components/Breakdown/ShotProposalCard.tsx` | Confirmation card for AI-proposed shot creation or modification |

### Reused Components (no changes)

| Component | Import Path | Usage |
|-----------|-------------|-------|
| `MarkdownContent` | `../Shared/MarkdownContent` | Render AI message markdown content |
| `BreakdownPanel` | `./BreakdownPanel` | Already wraps the right panel in BreakdownLayout |

### Modified Components

| Component | Change |
|-----------|--------|
| `BreakdownLayout.tsx` | Replace placeholder `<div>` in right panel with `<BreakdownChat projectId={projectId} />` |

### Component Anatomy: BreakdownChat

```
BreakdownChat (flex flex-col h-full)
  +-- Header bar (px-3 py-2, border-b, flex-shrink-0)
  |     +-- Title: "Breakdown AI" (text-xs font-semibold uppercase tracking-wider)
  |     +-- Clear button (RotateCcw icon, conditional on messages.length > 0, no confirmation -- instant clear)
  +-- Message area (flex-1 overflow-y-auto p-4 space-y-3)
  |     +-- Empty state (centered, py-8)
  |     +-- User message bubbles (justify-end, max-w-[85%], rounded-xl)
  |     +-- AI message bubbles (justify-start, max-w-[85%], rounded-xl)
  |     +-- Streaming bubble (with MarkdownContent, live-updating)
  |     +-- Loading spinner (Loader2 animate-spin, during stream init)
  |     +-- Error bubble (bg-destructive/10, border-destructive/20)
  |     +-- messagesEndRef (scroll anchor)
  +-- ShotProposalCard (conditional, above input, mx-3 mb-3)
  +-- Input area (p-3 border-t border-border flex-shrink-0)
        +-- textarea (flex-1, bg-input, focus:ring-2 focus:ring-primary/20)
        +-- Send button (px-3 py-2, bg-primary text-primary-foreground, rounded-lg, aria-label="Send message")
```

### Component Anatomy: ShotProposalCard

```
ShotProposalCard (mx-3 mb-3 rounded-xl border border-primary/25 bg-primary/5 p-3)
  +-- Header row (flex items-center gap-2 mb-2)
  |     +-- Icon: Camera or Plus (h-3.5 w-3.5 text-primary)
  |     +-- Label: "NEW SHOT" or "SHOT CHANGES" (text-xs font-semibold text-primary uppercase tracking-wider)
  +-- Fields section (space-y-2 mb-3 max-h-48 overflow-y-auto)
  |     +-- For each proposed field:
  |           +-- Field label (text-[10px] font-semibold text-muted-foreground uppercase tracking-wider)
  |           +-- Field value (text-xs text-foreground/70 leading-snug line-clamp-3)
  |     +-- Scene association line (if scene_item_id provided):
  |           +-- "Scene:" label + scene name
  +-- Action buttons (flex gap-2)
        +-- Primary: "Create Shot" / "Apply Changes" (flex-1, bg-primary/20 text-primary border border-primary/20)
        +-- Secondary: "Dismiss" (px-3, text-muted-foreground hover:bg-muted/50)
```

---

## Interaction States

### Chat Message States

| State | Visual Treatment |
|-------|-----------------|
| User message | bg-primary/15, text with slight blue tint (hsl(210 20% 92%)), border border-primary/10, rounded-xl |
| AI message (normal) | bg-muted/50, text-foreground/80, border border-border, rounded-xl |
| AI message (streaming) | Same as normal, MarkdownContent updates live, no special border |
| AI message (with shot action) | bg-primary/5, border border-primary/15, rounded-xl, followed by ShotProposalCard |
| Error message | bg-destructive/10, text-destructive, border border-destructive/20, AlertCircle icon |
| Loading (pre-stream) | bg-muted/50, border border-border, contains Loader2 h-4 w-4 animate-spin text-muted-foreground |

### ShotProposalCard States

| State | Visual Treatment |
|-------|-----------------|
| Default (awaiting user) | border-primary/25, bg-primary/5, buttons enabled |
| Applying (after confirm) | Primary button shows Loader2 h-3 w-3 animate-spin, both buttons disabled:opacity-50 |
| Success (post-apply) | Card dismissed, AI posts confirmation message in chat: "Shot #N created" or "Shot #N updated" |
| Dismissed | Card removed, no side effects |

### Input States

| State | Visual Treatment |
|-------|-----------------|
| Empty + enabled | bg-input, border border-border, placeholder "Ask about your shotlist..." in text-muted-foreground/50 |
| Focused | ring-2 ring-primary/20 (steel blue glow) |
| Typing | text-foreground, send button opacity transitions from 30% to 100% |
| Disabled (streaming) | opacity-50, cursor-not-allowed on both textarea and send button |
| Disabled (no session) | Same as streaming disabled state |

### Send Button States

| State | Visual Treatment |
|-------|-----------------|
| Enabled | bg-primary text-primary-foreground (steel blue button, dark text) |
| Hover | bg-primary with slight brightness increase (hover:brightness-110 or manual HSL shift) |
| Disabled | opacity-30, cursor-not-allowed |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Send" (via Send icon, no text label -- matches SidebarChat pattern) |
| Panel title | "Breakdown AI" |
| Empty state heading | "Breakdown assistant" |
| Empty state body | "Ask about your shotlist, breakdown elements, or request new shots. The AI has context about your entire project." |
| Input placeholder | "Ask about your shotlist..." |
| Error state | "Something went wrong. Try sending your message again." |
| Shot creation proposal label | "NEW SHOT" |
| Shot creation confirm button | "Create Shot" |
| Shot creation success | "Shot #{N} created in {scene_name}" |
| Shot modification proposal label | "SHOT CHANGES" |
| Shot modification confirm button | "Apply Changes" |
| Shot modification success | "Shot #{N} updated" |
| Dismiss button | "Dismiss" |
| Clear chat tooltip | "Clear conversation" |
| Destructive confirmation | "Clear conversation": No confirmation dialog. Instant clear on click, matching existing SidebarChat behavior where conversation history is ephemeral and low-cost to regenerate. |
| No project state heading | "No project selected" |
| Streaming indicator | Loader2 spinner only, no text (matches SidebarChat) |

---

## Animation Contract

| Trigger | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| New message appears | fade-up (opacity 0 to 1, translateY 8px to 0) | 500ms | ease-out |
| ShotProposalCard appears | scale-in (opacity 0 to 1, scale 0.96 to 1) | 200ms | ease-out |
| ShotProposalCard dismissed | No exit animation (instant removal) | 0ms | -- |
| Streaming text | Appended character-by-character, smooth scroll | N/A | smooth scroll behavior |
| Panel mount | fade-in | 400ms | ease-out |

Source: All animations already defined in tailwind.config.js keyframes. Use existing `animate-fade-up`, `animate-scale-in`, `animate-fade-in` utilities.

---

## Layout Contract

BreakdownChat renders inside the existing right panel of `BreakdownLayout`:

| Property | Value |
|----------|-------|
| Container | `BreakdownPanel` wrapper with `side="right"` |
| Default width | 25% of viewport (from `readStoredWidth` with 0.25 fallback) |
| Min width | 200px (MIN_PANEL_WIDTH constant) |
| Max width | 45% of viewport |
| Resizable | Yes, via existing BreakdownLayout drag handle |
| Collapsible | Yes, via existing BreakdownPanel toggle |
| Height | `h-full` (fills remaining viewport after 3.5rem header) |
| Internal layout | flex flex-col: header (shrink-0) + messages (flex-1 overflow-y-auto) + proposal card (conditional) + input (shrink-0) |

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| Keyboard send | Enter key sends message (Shift+Enter for newline) -- matches SidebarChat |
| Focus management | Textarea auto-focuses on panel mount; after sending, focus returns to textarea |
| Screen reader | Message area uses role="log" aria-live="polite" for streaming updates |
| Send button | aria-label="Send message" on the icon-only send button |
| Confirmation card | Primary action button uses aria-label="Create shot" or "Apply changes to shot" |
| Color contrast | All text meets WCAG AA (4.5:1 minimum). Primary text on dark background: hsl(210 20% 95%) on hsl(220 13% 5%) = 14.8:1 |
| Reduced motion | Streaming scroll uses `prefers-reduced-motion: reduce` media query to disable smooth scrolling |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not applicable | not applicable |
| Third-party | none | not applicable |

No shadcn or third-party registries are in use. All components are hand-rolled within the project codebase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
