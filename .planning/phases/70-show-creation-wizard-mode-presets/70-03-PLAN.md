---
phase: 70-show-creation-wizard-mode-presets
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - frontend/src/components/Shows/ShowDetail.tsx
  - frontend/src/components/Shows/BibleEditor.tsx
autonomous: false

requirements: [SWZ-01, SWZ-02]

must_haves:
  truths:
    - "On the show edit surface, the user sees the same three preset cards, pre-selected to the show's current continuity mode"
    - "Changing the selected preset persists the new continuity_mode on the show via PUT /api/shows/{id}"
    - "Pre-selection disambiguates Microserie vs Serie conectada by the stored episode_duration_minutes (2 → Microserie, else connected → Serie conectada)"
    - "The Season Arc bible section remains visible/editable for connected modes (existing BibleEditor behavior preserved)"
  artifacts:
    - path: "frontend/src/components/Shows/BibleEditor.tsx"
      provides: "Edit-side mode-change control reusing the three preset cards, pre-selected to the show's mode, mutating continuity_mode on change"
      contains: "SHOW_PRESETS"
      min_lines: 130
    - path: "frontend/src/components/Shows/ShowDetail.tsx"
      provides: "Passes the show's continuity_mode (and current duration) down to BibleEditor for pre-selection"
      contains: "continuity_mode"
  key_links:
    - from: "ShowDetail.tsx"
      to: "BibleEditor"
      via: "passes show.continuity_mode (and bible.episode_duration_minutes) as props"
      pattern: "continuity_mode"
    - from: "BibleEditor.tsx mode-change mutation"
      to: "api.updateShow(showId, { continuity_mode })"
      via: "React Query mutation invalidating QUERY_KEYS.SHOW(showId)"
      pattern: "updateShow"
---

<objective>
Add the edit-side mode-change control: reuse the identical three preset cards from creation (driven by the shared `SHOW_PRESETS`) inside `BibleEditor`, pre-selected to the show's current `continuity_mode` (disambiguating Microserie vs Serie conectada by the stored duration). Changing the selection issues `PUT /api/shows/{id}` updating `continuity_mode`. `ShowDetail` passes the show's `continuity_mode` down so the control can pre-select.

Purpose: Delivers the edit half of SWZ-01/SWZ-02 (D-08 — a later edit can change the mode, reusing the Phase 67 `PUT /api/shows/{id}` contract). UI-SPEC pins the decision to reuse the identical preset cards for visual + interaction consistency with creation. Scope is the mode control + season-arc visibility only; the full `BibleEditor` section-visibility adaptation is explicitly DEFERRED (D-08).

Output: Updated `BibleEditor.tsx` (adds the mode-change card control + mutation) and `ShowDetail.tsx` (threads `continuity_mode` down). This plan has a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-show-creation-wizard-mode-presets/70-CONTEXT.md
@.planning/phases/70-show-creation-wizard-mode-presets/70-PATTERNS.md
@.planning/phases/70-show-creation-wizard-mode-presets/70-UI-SPEC.md

<interfaces>
<!-- From Plan 70-01 (must be complete first): -->
frontend/src/types/index.ts:
  export type ContinuityMode = 'connected' | 'anthology' | 'standalone';
  Show { ...; continuity_mode: ContinuityMode }
  ShowCreate { ...; continuity_mode?: ContinuityMode }  // Partial<ShowCreate> covers updateShow
frontend/src/lib/constants.ts:
  SHOW_PRESETS: { id; label; helper; icon; mode: ContinuityMode; duration: number | null }[]

<!-- Existing API (no change needed): -->
api.updateShow(id, data: Partial<ShowCreate>): Promise<Show>   // api.tsx:1299 — sends { continuity_mode }
api.getShow(id): Promise<Show>                                  // api.tsx:1283 — now returns continuity_mode (Plan 01 typed it)

<!-- Constants: -->
QUERY_KEYS.SHOW(id) = ['show', id]    // constants.ts:197 — invalidate after mode change
QUERY_KEYS.BIBLE(id) = ['bible', id]  // constants.ts:198

<!-- Existing BibleEditor (BibleEditor.tsx, full file): -->
  props: { showId: string; bible: BibleResponse }
  updateBibleMutation = useMutation({ mutationFn: (data) => api.updateBible(showId, data), onSuccess: saved-field flash })
  renders BIBLE_SECTIONS accordions + EpisodeDurationPicker
  loaded-ref pattern guards initial state against query refetch (lines 34-46)

<!-- ShowDetail (ShowDetail.tsx): -->
  fetches `show` via api.getShow under QUERY_KEYS.SHOW(showId) (lines 16-19)
  fetches `bible` via api.getBible under QUERY_KEYS.BIBLE(showId) (lines 21-24)
  renders <BibleEditor showId={showId} bible={bible} />  (line 80)

<!-- Card pattern: same verbatim CreateProjectModal.tsx:131-164 pattern as Plan 02 (selected border-amber-500/40 bg-amber-500/5 glow-amber; icon chip; amber dot; space-y-2.5). -->
<!-- Pre-selection rule (UI-SPEC :102): 2 → Microserie, else connected → Serie conectada, anthology → Antología. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pass continuity_mode from ShowDetail into BibleEditor</name>
  <read_first>
    - frontend/src/components/Shows/ShowDetail.tsx (full file — already fetches `show` and `bible`; renders BibleEditor at line 80)
    - frontend/src/components/Shows/BibleEditor.tsx (props interface lines 9-12)
    - frontend/src/types/index.ts (Show now carries continuity_mode — Plan 01)
  </read_first>
  <files>frontend/src/components/Shows/ShowDetail.tsx</files>
  <action>
    `ShowDetail` already fetches `show` via `api.getShow` under `QUERY_KEYS.SHOW(showId)` (lines 16-19) and renders `<BibleEditor showId={showId} bible={bible} />` (line 80). Extend the BibleEditor render to also pass the show's current mode: `<BibleEditor showId={showId} bible={bible} continuityMode={show.continuity_mode} />`. The `show` object is already in scope and guaranteed non-null at the render point (the early-return guard at lines 37-49 handles `!show`). No new query is needed — reuse the existing `show` from `useQuery`. Do not change query keys or fetch logic.
  </action>
  <verify>
    <automated>cd frontend && grep -q "continuityMode={show.continuity_mode}" src/components/Shows/ShowDetail.tsx && npx tsc --noEmit 2>&1 | grep -v "BibleEditor" | grep -c "error TS" | grep -q "^0$" && echo SHOWDETAIL_WIRED || (npx tsc --noEmit; echo "tsc errors expected until BibleEditor prop added in Task 2")</automated>
  </verify>
  <acceptance_criteria>
    - `ShowDetail.tsx` renders `<BibleEditor ... continuityMode={show.continuity_mode} />`
    - No new query or fetch is introduced; the existing `show` from `api.getShow` is reused
    - (tsc may report a missing-prop error on BibleEditor until Task 2 adds the prop — that is expected and resolved in Task 2)
  </acceptance_criteria>
  <done>ShowDetail threads the show's continuity_mode down to BibleEditor as a prop.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the mode-change preset-card control + updateShow mutation to BibleEditor</name>
  <read_first>
    - frontend/src/components/Shows/BibleEditor.tsx (full file 1-114 — props, loaded-ref pattern, updateBibleMutation, render structure)
    - frontend/src/components/Projects/CreateProjectModal.tsx (lines 131-164 — card pattern to reuse)
    - frontend/src/lib/constants.ts (SHOW_PRESETS from Plan 01; QUERY_KEYS.SHOW line 197, QUERY_KEYS.BIBLE line 198)
    - frontend/src/types/index.ts (ContinuityMode, Show.continuity_mode)
    - .planning/phases/70-show-creation-wizard-mode-presets/70-UI-SPEC.md (lines 121-123 edit control decision; 137 in-flight disable)
    - .planning/phases/70-show-creation-wizard-mode-presets/70-PATTERNS.md (edit-side section — pre-selection rule, mutation/invalidate pattern)
  </read_first>
  <files>frontend/src/components/Shows/BibleEditor.tsx</files>
  <action>
    Extend the `BibleEditorProps` interface to add `continuityMode: ContinuityMode` (import the type from `../../types`). Import `SHOW_PRESETS` and `QUERY_KEYS` from `../../lib/constants`, the lucide icons `Zap`, `Link`, `LayoutGrid` (extend the existing lucide import), `api`'s `updateShow` (already via `api`), and `useQueryClient` from `@tanstack/react-query`.
    Compute the initial selected preset id from the incoming `continuityMode` + `bible.episode_duration_minutes` using the pre-selection rule (UI-SPEC :102): if `continuityMode === 'connected'` and `bible.episode_duration_minutes === 2` → `microserie`; else if `continuityMode === 'connected'` → `serie-conectada`; else (`anthology`) → `antologia`. Seed this into `useState` (e.g. `selectedPreset`). Guard the initial value with the SAME loaded-ref pattern already in the file (lines 34-46) so a query refetch does not clobber a user's in-session change.
    Add a `useQueryClient()` and an `updateShowMutation = useMutation({ mutationFn: (mode: ContinuityMode) => api.updateShow(showId, { continuity_mode: mode }), onSuccess: () => queryClient.invalidateQueries({ queryKey: QUERY_KEYS.SHOW(showId) }) })`.
    Render a `Continuity` mode-change section at the TOP of the editor (above the `BIBLE_SECTIONS` accordions), with a `Continuity` section label (`text-sm font-medium text-foreground` to match the editor's existing card-header weight, or a section heading consistent with the surrounding `space-y-3` layout — keep it visually aligned with the existing Episode Duration card). Inside, render the three preset cards by mapping `SHOW_PRESETS`, using the SAME verbatim card markup as the create modal (selected `border-amber-500/40 bg-amber-500/5 glow-amber`, icon chip `bg-amber-500/15 text-amber-400`, amber dot; unselected `border-border hover:border-muted-foreground/20 hover:bg-muted/30`; title `text-sm font-semibold`, helper `text-xs text-muted-foreground mt-0.5`; wrapper `space-y-2.5`). Map each preset's `icon` string to its component via a local `{ Zap, Link, LayoutGrid }` lookup.
    On card click: set `selectedPreset` to the preset id and fire `updateShowMutation.mutate(preset.mode)`. Per UI-SPEC :137, disable all three cards (e.g. `disabled={updateShowMutation.isPending}` + reduced-opacity styling) while the PUT is in flight. Show a brief saved indicator consistent with the file's existing `savedField` flash pattern if convenient, but the binding requirement is only the mutation + card disable.
    Do NOT reseed duration on mode change in this plan (the simpler, safe choice — durations stay editable metadata; reseeding-on-mode-change is an optional UI-SPEC nicety, and skipping it avoids clobbering a user's custom duration). Do NOT touch the BIBLE_SECTIONS accordions or add section-visibility logic — the full section-visibility adaptation is DEFERRED (D-08). The Season Arc bible section stays rendered/editable exactly as today for all modes (its mode-conditional hiding is out of Phase 70 scope).
  </action>
  <verify>
    <automated>cd frontend && grep -q "SHOW_PRESETS" src/components/Shows/BibleEditor.tsx && grep -q "continuityMode" src/components/Shows/BibleEditor.tsx && grep -q "updateShow" src/components/Shows/BibleEditor.tsx && grep -q "QUERY_KEYS.SHOW" src/components/Shows/BibleEditor.tsx && grep -q "isPending" src/components/Shows/BibleEditor.tsx && npx tsc --noEmit && echo BIBLEEDITOR_OK</automated>
  </verify>
  <acceptance_criteria>
    - `BibleEditorProps` includes `continuityMode: ContinuityMode`
    - The editor renders the three preset cards (Microserie / Serie conectada / Antología) above the bible accordions
    - On mount the card matching the show's mode is pre-selected: duration 2 → Microserie, other connected → Serie conectada, anthology → Antología
    - Clicking a different card calls `api.updateShow(showId, { continuity_mode })` and invalidates `QUERY_KEYS.SHOW(showId)`
    - Cards are disabled while the mutation `isPending`
    - The BIBLE_SECTIONS accordions (incl. Season Arc) are unchanged — no section-visibility logic added
    - `npx tsc --noEmit` exits 0 (and resolves the BibleEditor prop error from Plan 03 Task 1)
  </acceptance_criteria>
  <done>BibleEditor shows the pre-selected mode-change cards; changing the selection persists continuity_mode via updateShow and invalidates the show query; bible sections untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The edit-side mode-change control: `BibleEditor` now renders the same three preset cards (Microserie / Serie conectada / Antología), pre-selected to the show's current continuity mode, and persists a mode change via `PUT /api/shows/{id}`.</what-built>
  <how-to-verify>
    1. With frontend (`npm run dev`) + backend running, open a show created in Plan 02 (e.g. the `Serie conectada` show with 22-min duration).
    2. In the Series Bible, confirm a `Continuity` section with the three preset cards appears, and the card matching the show's mode is pre-selected (Serie conectada highlighted amber).
    3. Open the Microserie show (2-min duration) and confirm `Microserie` is the pre-selected card (duration-2 disambiguation).
    4. Open the Antología show and confirm `Antología` is pre-selected.
    5. On any show, click a different preset card — confirm the new card highlights, the cards briefly disable during save, and the selection sticks.
    6. Reload the page and confirm the new mode is the pre-selected card (the change persisted).
    7. Confirm the existing bible accordions (Characters / World / Season Arc / Tone) and the Episode Duration picker still render and edit normally — nothing was hidden or broken.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→`PUT /api/shows/{id}` | The edit control sends a client-controlled `continuity_mode`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-05 | Tampering | client-sent `continuity_mode` on updateShow | accept | Backend app-validates the `ContinuityMode` enum (Phase 67 D-03), rejecting out-of-enum with 422; the PUT endpoint is owner-scoped (Phase 67). The UI only ever sends `connected`/`anthology`. No new backend surface. |
| T-70-06 | Elevation of Privilege | editing another user's show via the mode control | accept | `PUT /api/shows/{id}` is already owner-scoped server-side (str-coerced owner filter, cross-owner 404 — Phase 67 convention). The frontend cannot bypass this; the control only acts on shows the user can already open. |
| T-70-SC | Tampering | npm installs | accept | No package installs in this plan (`lucide-react` already a dependency); no legitimacy gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `grep` confirms `continuityMode` (ShowDetail + BibleEditor), `SHOW_PRESETS`, `updateShow`, `QUERY_KEYS.SHOW`, and `isPending` appear in `BibleEditor.tsx`.
- Human checkpoint confirms pre-selection by mode/duration, persistence on change, and that bible sections are untouched.
</verification>

<success_criteria>
- The edit surface offers the same three preset cards, pre-selected to the show's current mode (duration-2 disambiguation for Microserie vs Serie conectada).
- Changing the preset persists `continuity_mode` via `PUT /api/shows/{id}` and invalidates the show query.
- The existing bible sections (incl. Season Arc) and duration picker remain fully functional — no deferred section-visibility work leaked in.
</success_criteria>

<output>
Create `.planning/phases/70-show-creation-wizard-mode-presets/70-03-SUMMARY.md` when done
</output>
