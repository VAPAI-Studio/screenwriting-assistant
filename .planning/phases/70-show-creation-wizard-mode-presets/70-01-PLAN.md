---
phase: 70-show-creation-wizard-mode-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/lib/constants.ts
autonomous: true
requirements: [SWZ-01, SWZ-02]

must_haves:
  truths:
    - "ShowCreate and Show payloads can carry a typed continuity_mode value"
    - "A single shared preset definition exists that both the create modal and the edit control import (one source of truth for label/helper/icon/mode/duration)"
  artifacts:
    - path: "frontend/src/types/index.ts"
      provides: "ContinuityMode union + continuity_mode on Show and ShowCreate"
      contains: "continuity_mode"
    - path: "frontend/src/lib/constants.ts"
      provides: "SHOW_PRESETS array (3 presets: id, label, helper, icon, mode, duration)"
      contains: "SHOW_PRESETS"
  key_links:
    - from: "frontend/src/types/index.ts"
      to: "backend/app/models/schemas.py ContinuityMode enum"
      via: "string-literal union mirrors the backend enum values"
      pattern: "connected.*anthology.*standalone"
    - from: "frontend/src/lib/constants.ts SHOW_PRESETS"
      to: "ContinuityMode union"
      via: "each preset.mode is typed as ContinuityMode"
      pattern: "mode:.*ContinuityMode"
---

<objective>
Establish the typed foundation for Phase 70: add the `ContinuityMode` string-literal union to the frontend, thread `continuity_mode` through the `Show` and `ShowCreate` interfaces (covering both create and edit via `Partial<ShowCreate>`), and define a single shared `SHOW_PRESETS` config that the create modal (Plan 02) and the edit-side mode control (Plan 03) both consume.

Purpose: `continuity_mode` does not exist anywhere in `frontend/src` today (grep returns zero hits). Both downstream plans depend on this type and this preset config. Defining the contract first prevents each consumer from inventing its own preset list and keeps Microserie/Serie conectada/Antología consistent across create and edit.

Output: Extended `types/index.ts`, new `SHOW_PRESETS` const in `constants.ts`. No component changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-show-creation-wizard-mode-presets/70-CONTEXT.md
@.planning/phases/70-show-creation-wizard-mode-presets/70-PATTERNS.md
@.planning/phases/70-show-creation-wizard-mode-presets/70-UI-SPEC.md

<interfaces>
<!-- Authoritative source of truth for the enum: backend already validates these three values (Phase 67). -->

backend/app/models/schemas.py ContinuityMode (str-enum, ~lines 914-921):
  values: "connected" | "anthology" | "standalone"

frontend/src/types/index.ts (current, lines 453-465 — must extend):
  export interface Show { id; owner_id; title; description; created_at; updated_at }
  export interface ShowCreate { title; description? }
  (neither has continuity_mode yet)

frontend/src/lib/constants.ts (current):
  BIBLE_SECTIONS (line 357) — bible_season_arc label "Season Arc"
  DURATION_PRESETS (line 364) — values 10/22/44/60/-1; 2 is NOT here and MUST NOT be added (UI-SPEC :115-119)

UI-SPEC preset table (70-UI-SPEC.md:96-100) — verbatim source for label/helper/icon/mode/duration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ContinuityMode union and thread continuity_mode through Show and ShowCreate</name>
  <read_first>
    - frontend/src/types/index.ts (lines 449-482 — the Show / ShowCreate / Bible block)
    - backend/app/models/schemas.py (~lines 914-943 — ContinuityMode enum + ShowCreate to mirror values)
    - .planning/phases/70-show-creation-wizard-mode-presets/70-PATTERNS.md (Integration Findings #1 and #3)
  </read_first>
  <files>frontend/src/types/index.ts</files>
  <action>
    In the Show-types block (around line 451), add a new exported type union immediately above `export interface Show`:
    `export type ContinuityMode = 'connected' | 'anthology' | 'standalone';`
    Include all three values for type-completeness even though the wizard only offers two (D-02 — `standalone` is the `show_id IS NULL` feature-film path, not a wizard preset).
    Add a required field `continuity_mode: ContinuityMode;` to the `Show` interface (it is always returned by the backend `ShowResponse`, default `'anthology'`).
    Add an optional field `continuity_mode?: ContinuityMode;` to the `ShowCreate` interface (optional because the backend defaults to `'anthology'` when omitted — Phase 67 D-01). Do NOT create a separate `ShowUpdate` interface: `api.updateShow` is already typed `Partial<ShowCreate>` (api.tsx:1299), so adding the field to `ShowCreate` automatically covers the edit payload (Integration Finding #3). Do NOT add `episode_duration_minutes` to `ShowCreate` — it is a Bible field on `BibleUpdate`/`BibleResponse`, not a Show field (Integration Finding #2); leave the existing `BibleUpdate`/`BibleResponse` interfaces unchanged.
  </action>
  <verify>
    <automated>cd frontend && grep -q "export type ContinuityMode = 'connected' | 'anthology' | 'standalone'" src/types/index.ts && grep -q "continuity_mode: ContinuityMode" src/types/index.ts && grep -q "continuity_mode?: ContinuityMode" src/types/index.ts && npx tsc --noEmit && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/index.ts` contains the exact line `export type ContinuityMode = 'connected' | 'anthology' | 'standalone';`
    - `Show` interface contains `continuity_mode: ContinuityMode;` (required)
    - `ShowCreate` interface contains `continuity_mode?: ContinuityMode;` (optional)
    - No `ShowUpdate` interface is introduced; no `episode_duration_minutes` added to `ShowCreate`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ContinuityMode union exists; Show requires it; ShowCreate optionally accepts it; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the shared SHOW_PRESETS config to constants.ts</name>
  <read_first>
    - frontend/src/lib/constants.ts (lines 357-371 — BIBLE_SECTIONS + DURATION_PRESETS `as const` shape to mirror)
    - .planning/phases/70-show-creation-wizard-mode-presets/70-UI-SPEC.md (lines 96-100 preset table; 151-153 copywriting contract)
    - frontend/src/types/index.ts (the ContinuityMode union added in Task 1)
  </read_first>
  <files>frontend/src/lib/constants.ts</files>
  <action>
    Add an exported `SHOW_PRESETS` array near `DURATION_PRESETS` (after line ~371), following the same `as const` shape used by `DURATION_PRESETS`. Import the `ContinuityMode` type from `../types` at the top of the file if not already imported, and type the array so each `mode` is a `ContinuityMode`. Three entries in this exact order, with these exact values (Spanish labels verbatim per D-05, English helpers per UI-SPEC copywriting contract):
    1. `{ id: 'microserie', label: 'Microserie', helper: 'Short connected episodes that build on each other (~2 min each).', icon: 'Zap', mode: 'connected', duration: 2 }`
    2. `{ id: 'serie-conectada', label: 'Serie conectada', helper: 'A connected series — each episode carries the season arc and prior episodes forward (~22 min each).', icon: 'Link', mode: 'connected', duration: 22 }`
    3. `{ id: 'antologia', label: 'Antología', helper: 'Independent episodes — each stands alone, no cross-episode context.', icon: 'LayoutGrid', mode: 'anthology', duration: null }`
    The `icon` value is the lucide-react component name as a string; consumers map it to the imported component. `duration: 2` for Microserie is a CUSTOM seed value — do NOT add `2` to `DURATION_PRESETS` (UI-SPEC decision; adding 2 to shared presets is explicitly Deferred). Antología has `duration: null` (leaves duration unset/default per D-01). The em dash `—` in the Serie conectada helper must be the literal em-dash character, matching the copywriting contract verbatim.
  </action>
  <verify>
    <automated>cd frontend && grep -q "SHOW_PRESETS" src/lib/constants.ts && grep -q "Microserie" src/lib/constants.ts && grep -q "Serie conectada" src/lib/constants.ts && grep -q "Antología" src/lib/constants.ts && grep -q "mode: 'connected'" src/lib/constants.ts && grep -q "mode: 'anthology'" src/lib/constants.ts && grep -vq "value: 2," src/lib/constants.ts && npx tsc --noEmit && echo PRESETS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `SHOW_PRESETS` is exported from `constants.ts` with exactly 3 entries in the order Microserie, Serie conectada, Antología
    - Microserie has `mode: 'connected'`, `duration: 2`; Serie conectada has `mode: 'connected'`, `duration: 22`; Antología has `mode: 'anthology'`, `duration: null`
    - Spanish labels appear verbatim: `Microserie`, `Serie conectada`, `Antología`
    - `DURATION_PRESETS` is unchanged — no `{ value: 2, ... }` entry added
    - Each preset `mode` is type-checked as `ContinuityMode` (tsc exits 0)
  </acceptance_criteria>
  <done>SHOW_PRESETS exists as the single source of truth for the three presets; tsc clean; DURATION_PRESETS untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→`POST/PUT /api/shows` | The frontend now sends `continuity_mode`; the value is client-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | client-sent `continuity_mode` on create/edit payload | accept | Backend already app-validates the `ContinuityMode` enum and returns 422 on out-of-enum values (Phase 67 D-03, schemas.py). No new backend surface; the frontend union is a typing aid, not the security boundary. |
| T-70-SC | Tampering | npm installs | accept | No new packages are installed in this plan (only `lucide-react` icon names referenced as strings; the library is already a dependency). No package-manager install task exists, so no legitimacy gate is required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the whole frontend.
- `grep` confirms `ContinuityMode` union and `SHOW_PRESETS` exist with the exact preset values.
- `DURATION_PRESETS` remains 10/22/44/60/-1 (no `2` added).
</verification>

<success_criteria>
- `ContinuityMode` union exists and is mirrored from the backend enum (3 values).
- `Show.continuity_mode` is required; `ShowCreate.continuity_mode` is optional.
- `SHOW_PRESETS` is a single shared config with the three presets, ready for Plans 02 and 03 to import.
- Type-checking is clean.
</success_criteria>

<output>
Create `.planning/phases/70-show-creation-wizard-mode-presets/70-01-SUMMARY.md` when done
</output>
