---
phase: 55-mcp-foundation-mount-auth-lifespan-client-spike
plan: 03
type: execute
wave: 3
depends_on: ["55-02"]
files_modified:
  - backend/app/tests/test_mcp_client_spike.py
  - .planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/CLIENT-SETUP.md
autonomous: false
requirements: [MCPF-01, MCPF-02, MCPF-03, MCPF-05]
must_haves:
  truths:
    - "A programmatic MCP client (mcp SDK client or httpx) completes initialize + tools/list over Streamable HTTP against the running /mcp"
    - "A valid sa_<key> static bearer authenticates and whoami returns the correct user via the real network path"
    - "An invalid/missing key is rejected over the real network path"
    - "request_count increments on an authenticated MCP call over the real network path"
    - "GUI-client UAT (Claude Code / Desktop / Hermes) instructions are documented and queued as a deferred human checkpoint"
  artifacts:
    - path: "backend/app/tests/test_mcp_client_spike.py"
      provides: "Programmatic client-compat spike (the automatable GO/NO-GO portion)"
      contains: "initialize"
    - path: ".planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/CLIENT-SETUP.md"
      provides: "claude mcp add / Desktop / Hermes static-header connection instructions"
      contains: "claude mcp add"
  key_links:
    - from: "backend/app/tests/test_mcp_client_spike.py"
      to: "http://localhost:8001/mcp (running server)"
      via: "mcp streamable-http client / httpx"
      pattern: "/mcp"
---

<objective>
Run the client-compatibility spike — the GO/NO-GO gate for the v8.0 milestone (MCPF-05). The AUTOMATABLE portion is a real, programmatic verification using the `mcp` SDK's own client (or httpx) against the running server at `http://localhost:8001/mcp`, proving over the real network transport: (a) initialize + tools/list round-trip, (b) a valid `sa_<key>` static bearer authenticates and whoami returns the right user, (c) an invalid/missing key is rejected, (d) request_count increments on an authenticated MCP call. The GUI-client UAT (Claude Code / Desktop / Hermes static-header connection) is documented and explicitly DEFERRED to the user as a human-verify checkpoint — it does NOT block this plan.

Purpose: Closes MCPF-05 (client-compat spike) with an automated proof over the wire, and resolves Pitfall 1 (static-bearer vs OAuth) on the programmatic path. Confirms MCPF-01/02/03 end-to-end against a live uvicorn process (not just an in-process TestClient), which is the difference between "the handler works" and "a real client over HTTP works."

Output: a programmatic spike test, a client-setup doc, and a deferred GUI-UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/55-CONTEXT.md
@.planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/55-02-SUMMARY.md

<interfaces>
<!-- Local runtime (from CONTEXT.md): backend host :8001 -> container :8000 (docker-compose.override.yml). MCP at http://localhost:8001/mcp. -->
<!-- mcp SDK client (official, installed): -->
#   from mcp.client.streamable_http import streamablehttp_client
#   from mcp.client.session import ClientSession
#   async with streamablehttp_client("http://localhost:8001/mcp", headers={"Authorization": f"Bearer {key}"}) as (read, write, _):
#       async with ClientSession(read, write) as session:
#           await session.initialize()
#           tools = await session.list_tools()
#           result = await session.call_tool("whoami", {})
#   (Confirm exact import paths against the installed mcp version; fall back to httpx raw JSON-RPC POSTs if the client API differs — record which in SUMMARY.)
# A real sa_ key for the spike: create one via POST /api/auth/api-keys with Authorization: Bearer mock-token (development ENVIRONMENT), or seed directly. The create response returns the full `key` once (starts with sa_).
# whoami returns {"user_id","email","display_name"}. request_count lives on the api_keys row (query the DB or a future usage endpoint).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Programmatic client-compat spike over the live /mcp — per spike_note (automatable GO/NO-GO portion)</name>
  <files>backend/app/tests/test_mcp_client_spike.py</files>
  <action>
    Write `backend/app/tests/test_mcp_client_spike.py` — an integration spike that runs the server and drives it with a REAL MCP client over the network (Streamable HTTP), proving the four facts the milestone gate depends on. Prefer driving an in-process uvicorn instance (spin up `uvicorn.Server` on a free port in a thread/async task with the real `app` from `app.main`, against the test DB or a disposable sqlite/postgres) so the test is self-contained and CI-runnable; if standing up uvicorn in-test is impractical, mark the test to target an already-running `http://localhost:8001/mcp` and `pytest.skip` when it is unreachable (so the suite stays green in CI but the spike runs locally). Record the chosen approach in SUMMARY.

    Use the `mcp` SDK streamable-http client (`streamablehttp_client` + `ClientSession`, import paths per the installed version — see interfaces). Assertions:
    1. With a valid `sa_<key>` in the Authorization header: `session.initialize()` succeeds AND `session.list_tools()` includes `ping` and `whoami` (proves MCPF-01 over the wire + static-bearer accepted, Pitfall 1 resolved on the programmatic path).
    2. `session.call_tool("whoami", {})` returns the key owner's email/user_id (MCPF-02 auth + MCPF-04 owner identity).
    3. With NO Authorization header (or a garbage `sa_invalid` key): initialize/first call is rejected (401 / auth error) — assert the client surfaces the rejection (MCPF-02 reject path).
    4. After one authenticated whoami call, the seeded key's `request_count` increased (query the DB row directly) — MCPF-03 over the wire.

    Generate the sa_ key for the spike by creating one through the REST endpoint (`POST /api/auth/api-keys` with `Bearer mock-token`) or by seeding an ApiKey+User row with a known sa_ token (sha256 → key_hash), mirroring test_api_keys.py. Keep the test deterministic and fast (no live AI calls — whoami/ping touch no AI).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest app/tests/test_mcp_client_spike.py -x -q</automated>
  </verify>
  <done>The spike test passes (or skips cleanly when no server is reachable in CI): a real MCP client over Streamable HTTP completes initialize + tools/list, authenticates with a static sa_ bearer, whoami returns the right user, an invalid/missing key is rejected, and request_count increments — all over the network path.</done>
</task>

<task type="auto">
  <name>Task 2: Write client connection instructions (CLI / Desktop / Hermes) — per spike_note GUI UAT documentation</name>
  <files>.planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/CLIENT-SETUP.md</files>
  <action>
    Write `CLIENT-SETUP.md` documenting how a human connects each real client to the running server at `http://localhost:8001/mcp` with a static sa_ header (the GUI portion of the spike that requires a person). Include, verbatim and copy-pasteable:
    - Claude Code: `claude mcp add --transport http screenwriting http://localhost:8001/mcp --header "Authorization: Bearer sa_<key>"` plus the `.mcp.json` `{"type":"http","url":"...","headers":{"Authorization":"Bearer sa_<key>"}}` form (type "streamable-http" is an accepted alias).
    - Claude Desktop: the manual remote-server config with `headers.Authorization` (NOT the claude.ai web connector — that UI is OAuth-only and out of scope; document this caveat explicitly per CONTEXT/PITFALLS).
    - Hermes: set the `Authorization` header directly; note Hermes static-header support is UNVERIFIED and is the one open item — if Hermes cannot send a static header, v8.0 still ships for the Claude clients and Hermes is deferred to v8.1 (NOT a milestone blocker, per the locked decision).
    - How to mint a real sa_ key (the /settings/api-keys UI or `POST /api/auth/api-keys`), and the expected success signal (tool list shows ping + whoami; calling whoami returns your user).
  </action>
  <verify>
    <automated>test -f .planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/CLIENT-SETUP.md && grep -q "claude mcp add" .planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/CLIENT-SETUP.md && grep -qi "hermes" .planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/CLIENT-SETUP.md</automated>
  </verify>
  <done>CLIENT-SETUP.md exists with copy-pasteable connection instructions for Claude Code, Claude Desktop, and Hermes, the web-connector caveat, and the Hermes-deferral note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A mounted Streamable HTTP MCP server at http://localhost:8001/mcp with static sa_ bearer auth and a whoami/ping tool. The programmatic client spike (Task 1) already proved initialize + auth + whoami + request_count over the wire. This checkpoint is the GUI-client portion of MCPF-05, which requires a human and is EXPLICITLY DEFERRED — do not block the plan on it.</what-built>
  <how-to-verify>
    Follow `CLIENT-SETUP.md`. With the backend running (`docker compose up` → http://localhost:8001):
    1. Mint a real sa_ key at /settings/api-keys (or via POST /api/auth/api-keys).
    2. Claude Code: `claude mcp add --transport http screenwriting http://localhost:8001/mcp --header "Authorization: Bearer sa_<key>"`, then confirm the tool list shows `ping` + `whoami` and calling `whoami` returns your user.
    3. Claude Desktop: add the same as a manual remote server with the Authorization header; confirm the same round-trip.
    4. Hermes (if available): attempt the same static-header connection. If it cannot send a static header, note it — this is DEFERRED to v8.1 and does NOT block v8.0.
    Expected: Claude Code and Claude Desktop both round-trip tool-list + one whoami call. Hermes is best-effort.
  </how-to-verify>
  <resume-signal>Type "approved" once Claude Code and Claude Desktop round-trip (Hermes optional), or describe what failed. This checkpoint may be approved later — the rest of the phase is complete without it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external MCP client → live /mcp over HTTP | The real over-the-wire path the spike validates (vs in-process TestClient) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-09 | Spoofing | static sa_ bearer over the network | mitigate | Spike asserts invalid/missing key is rejected over the real transport; valid key required for whoami |
| T-55-10 | Repudiation / usage-blindness | request_count over the wire | mitigate | Spike asserts request_count increments after an authenticated network call (MCPF-03 end-to-end) |
| T-55-11 | Information Disclosure | sa_ key in client config / logs | accept | Internal tool; keys minted per-user, redaction of Authorization in MCP-path logging already ensured by /mcp middleware exemption (LoggingMiddleware does not run on /mcp). Documented in CLIENT-SETUP.md |
</threat_model>

<verification>
- `pytest app/tests/test_mcp_client_spike.py` passes (or skips when no server reachable).
- CLIENT-SETUP.md present with CLI/Desktop/Hermes instructions and the web-connector + Hermes-deferral caveats.
- Deferred GUI checkpoint queued for the user (does not block phase completion).
</verification>

<success_criteria>
- MCPF-05: client-compat spike done on the automatable path (initialize + tools/list + static-bearer whoami + reject + request_count, all over the wire); GUI UAT documented and deferred to the user.
- MCPF-01/02/03 confirmed end-to-end against a live process, not just in-process.
</success_criteria>

<output>
Create `.planning/phases/55-mcp-foundation-mount-auth-lifespan-client-spike/55-03-SUMMARY.md` when done. Record: the spike harness (in-test uvicorn vs running-server-with-skip), the mcp client import paths used, the four assertion outcomes, and the status of the deferred GUI checkpoint.
</output>
