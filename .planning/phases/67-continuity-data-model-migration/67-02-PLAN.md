---
phase: 67-continuity-data-model-migration
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - backend/app/models/schemas.py
  - backend/app/api/endpoints/shows.py
  - backend/app/tests/test_shows_api.py
autonomous: true
requirements: [SCONT-01]
must_haves:
  truths:
    - "A show can be created with a continuity_mode of connected, anthology, or standalone, and the value persists"
    - "A show can be edited to change its continuity_mode, and GET returns the new value"
    - "Omitting continuity_mode on create yields the default 'anthology'"
    - "An invalid continuity_mode value (e.g. 'bogus') is rejected with 422"
  artifacts:
    - path: "backend/app/models/schemas.py"
      provides: "ContinuityMode Enum + continuity_mode on ShowCreate/ShowUpdate/ShowResponse"
      contains: "continuity_mode"
    - path: "backend/app/api/endpoints/shows.py"
      provides: "create/update_show thread continuity_mode through"
      contains: "continuity_mode"
    - path: "backend/app/tests/test_shows_api.py"
      provides: "Tests for set/edit/default/invalid continuity_mode"
      contains: "continuity_mode"
  key_links:
    - from: "backend/app/api/endpoints/shows.py create_show"
      to: "database.Show.continuity_mode"
      via: "body.continuity_mode passed to Show()"
      pattern: "continuity_mode"
    - from: "backend/app/models/schemas.py ShowResponse"
      to: "database.Show.continuity_mode"
      via: "from_attributes serialization"
      pattern: "continuity_mode"
---

<objective>
Expose `continuity_mode` on the Show API: editable on create/update, returned on read, validated against a 3-value enum at the app layer (per D-03/D-04). Delivers SCONT-01.

Purpose: Phases 68-71 read `continuity_mode` to branch generation/review. This plan makes it settable and readable through the public Show API.
Output: Updated `schemas.py` (ContinuityMode enum + ShowCreate/Update/Response fields), `shows.py` (create/update threading), and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67-continuity-data-model-migration/67-CONTEXT.md
@.planning/phases/67-continuity-data-model-migration/67-01-SUMMARY.md

<interfaces>
<!-- Show schemas to extend (schemas.py:912-944) -->
```python
class ShowCreate(BaseModel):
    title: str = Field(..., min_length=2, max_length=255)
    description: str = Field(default="", max_length=5000)
    # add continuity_mode here

class ShowUpdate(BaseModel):
    title: Optional[str] = ...
    description: Optional[str] = ...
    # add Optional continuity_mode here

class ShowResponse(BaseModel):
    id: UUID; owner_id: UUID; title: str; description: str
    created_at: datetime; updated_at: Optional[datetime] = None
    model_config = ConfigDict(from_attributes=True)
    # add continuity_mode here
```

<!-- Existing string-enum convention in schemas (database.py) -->
```python
class Framework(str, enum.Enum):
    THREE_ACT = "three_act"
```

<!-- shows.py create_show builds Show() at lines 21-29; update_show uses model_dump(exclude_unset=True) loop at 78-80 -->
```python
db_show = database.Show(title=body.title, description=body.description, owner_id=str(current_user.id))
# update loop:
for field, value in body.model_dump(exclude_unset=True).items():
    setattr(show, field, value)
```

<!-- Test harness: client + mock_auth_headers fixtures; mock auth token is Bearer mock-token (CLAUDE.md) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ContinuityMode enum and continuity_mode to Show schemas</name>
  <files>backend/app/models/schemas.py</files>
  <read_first>
    - backend/app/models/schemas.py:912-944 (ShowCreate/ShowUpdate/ShowResponse)
    - backend/app/models/database.py:121-135 (Framework/TemplateType str-enum convention to follow for the new ContinuityMode enum)
  </read_first>
  <behavior>
    - ShowCreate accepts continuity_mode in {connected, anthology, standalone}; omitting it defaults to anthology
    - ShowCreate with continuity_mode='bogus' raises a validation error (422 at the API)
    - ShowUpdate.continuity_mode is Optional (None when unset, excluded from model_dump(exclude_unset=True))
    - ShowResponse serializes continuity_mode from the ORM Show object
  </behavior>
  <action>
    Define a `ContinuityMode(str, enum.Enum)` with members CONNECTED="connected", ANTHOLOGY="anthology", STANDALONE="standalone", following the existing Framework str-enum convention (per D-03 this lives at the app layer as a Python Enum; place it in schemas.py near the Show schemas, or import from database.py if you place it there — match where Framework-style enums are imported from).
    On ShowCreate add `continuity_mode: ContinuityMode = ContinuityMode.ANTHOLOGY` (default anthology per D-01, so create without the field is unchanged behavior).
    On ShowUpdate add `continuity_mode: Optional[ContinuityMode] = None` (partial-update friendly; unset values excluded by model_dump(exclude_unset=True)).
    On ShowResponse add `continuity_mode: ContinuityMode` (read surface per D-04).
    Do NOT add episode_summary or episode_summary_stale to any Show schema (those belong to the Project/episode surface, Plan 03; episode_summary text stays internal per D-04).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && python -c "from app.models.schemas import ShowCreate, ShowUpdate, ShowResponse; m=ShowCreate(title='Demo Show'); assert m.continuity_mode.value=='anthology'; assert ShowCreate(title='Demo Show', continuity_mode='connected').continuity_mode.value=='connected'; assert 'continuity_mode' in ShowUpdate.model_fields and 'continuity_mode' in ShowResponse.model_fields; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ShowCreate(title=...) without continuity_mode yields continuity_mode == 'anthology'
    - ShowCreate(..., continuity_mode='connected') succeeds with value 'connected'
    - continuity_mode present in ShowUpdate.model_fields and ShowResponse.model_fields
    - No episode_summary / episode_summary_stale fields added to Show schemas
    - The bogus-value (ValidationError/422) rejection is NOT asserted here — it is covered by Task 2's pytest (POST /api/shows with continuity_mode='bogus' returns 422)
  </acceptance_criteria>
  <done>The three Show schemas carry a validated continuity_mode field defaulting to anthology; the out-of-enum rejection is proven by Task 2's API test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread continuity_mode through shows.py create/update and add API tests</name>
  <files>backend/app/api/endpoints/shows.py, backend/app/tests/test_shows_api.py</files>
  <read_first>
    - backend/app/api/endpoints/shows.py:14-83 (create_show, update_show)
    - backend/app/tests/test_shows_api.py:40-187 (TestShowsAPI create/update/get tests + client/mock_auth_headers usage)
  </read_first>
  <behavior>
    - POST /api/shows with continuity_mode='connected' returns 201 and the body shows continuity_mode='connected'
    - POST /api/shows without continuity_mode returns 201 with continuity_mode='anthology'
    - PUT /api/shows/{id} with continuity_mode='standalone' returns 200; subsequent GET returns 'standalone'
    - POST /api/shows with continuity_mode='bogus' returns 422
  </behavior>
  <action>
    In shows.py create_show (lines 21-29) pass `continuity_mode=body.continuity_mode.value` (or `body.continuity_mode` — match how the column stores the string) into the `database.Show(...)` constructor. update_show already uses the `model_dump(exclude_unset=True)` setattr loop (lines 78-80), so continuity_mode flows automatically once it is on ShowUpdate — verify it does and add no special-casing. Confirm ShowResponse serializes continuity_mode (it inherits from_attributes; the ORM column from Plan 01 provides the value).
    In test_shows_api.py add tests to TestShowsAPI: (a) create with continuity_mode='connected' → 201 + body value 'connected'; (b) create without the field → 201 + body value 'anthology' (D-01 default); (c) update to 'standalone' then GET returns 'standalone' (round trip); (d) create with continuity_mode='bogus' → 422 (this is the definitive out-of-enum rejection proof for the whole plan — Task 1 deliberately does not assert it). Use the existing client + mock_auth_headers fixtures (Bearer mock-token).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && PYTHONPATH=. pytest app/tests/test_shows_api.py -k continuity -x -q</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/shows with continuity_mode='connected' returns 201 and JSON continuity_mode == 'connected'
    - POST /api/shows without continuity_mode returns 201 and JSON continuity_mode == 'anthology'
    - PUT to 'standalone' then GET returns 'standalone'
    - A test case asserts POST /api/shows with continuity_mode='bogus' returns 422 (the plan's authoritative invalid-value rejection)
    - `pytest app/tests/test_shows_api.py -k continuity` exits 0
  </acceptance_criteria>
  <done>Show create/update persist continuity_mode, reads return it, invalid values 422 (proven by the dedicated bogus test), and new tests prove all four behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Show API | continuity_mode arrives as untrusted JSON on create/update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | Tampering | continuity_mode input on ShowCreate/ShowUpdate | mitigate | Pydantic ContinuityMode enum rejects any value outside {connected, anthology, standalone} → 422 at route entry; proven by the invalid-value test in Task 2. |
| T-67-04 | Information Disclosure | ShowResponse fields | accept | Only continuity_mode added to the read surface; episode_summary text intentionally NOT exposed (D-04). Ownership filter on shows already enforced in shows.py. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
- continuity_mode is settable, persisted, and readable through the Show API.
- Out-of-enum values are rejected with 422.
- Default on create is anthology (D-01).
</verification>

<success_criteria>
- SCONT-01 satisfied: a show can declare/edit its continuity_mode via the API and the value round-trips.
- Invalid continuity_mode is rejected; default is anthology.
</success_criteria>

<output>
Create `.planning/phases/67-continuity-data-model-migration/67-02-SUMMARY.md` when done
</output>
</content>
</invoke>
