---
phase: 67-continuity-data-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/delta/011_continuity_columns.sql
  - backend/app/models/database.py
autonomous: true
requirements: [SCONT-01, ESUM-02]
must_haves:
  truths:
    - "shows table has a continuity_mode VARCHAR column defaulting to 'anthology'"
    - "projects table has episode_summary (TEXT, nullable) and episode_summary_stale (Boolean, default False) columns"
    - "Re-running the boot migration is a no-op (no duplicate-column error)"
    - "Existing shows/episodes remain valid; standalone projects (show_id NULL) are unaffected"
  artifacts:
    - path: "backend/migrations/delta/011_continuity_columns.sql"
      provides: "Idempotent ADD COLUMN for all three continuity columns"
      contains: "ADD COLUMN IF NOT EXISTS continuity_mode"
    - path: "backend/app/models/database.py"
      provides: "SQLAlchemy column definitions mirroring the migration"
      contains: "continuity_mode"
  key_links:
    - from: "backend/app/models/database.py Show.continuity_mode"
      to: "backend/migrations/delta/011_continuity_columns.sql shows.continuity_mode"
      via: "column name + default parity"
      pattern: "continuity_mode"
    - from: "backend/app/models/database.py Project.episode_summary_stale"
      to: "backend/migrations/delta/011_continuity_columns.sql projects.episode_summary_stale"
      via: "column name + default parity"
      pattern: "episode_summary_stale"
---

<objective>
Lay the schema foundation for v10.0 continuity: add three columns via one new idempotent boot-applied delta migration and mirror them on the SQLAlchemy models.

Purpose: Every downstream phase (68 generation, 69 summary, 70 wizard, 71 review) reads these columns. They must exist, default safely, and survive re-runs.
Output: `backend/migrations/delta/011_continuity_columns.sql` and updated `Show`/`Project` SQLAlchemy models in `database.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-continuity-data-model-migration/67-CONTEXT.md

<interfaces>
<!-- Existing migration analogs (copy the idempotent ADD COLUMN IF NOT EXISTS style verbatim) -->

backend/migrations/delta/007_bible_columns.sql (shows columns analog):
```sql
ALTER TABLE shows ADD COLUMN IF NOT EXISTS bible_characters TEXT DEFAULT '';
ALTER TABLE shows ADD COLUMN IF NOT EXISTS episode_duration_minutes INTEGER;
```

backend/migrations/delta/008_episode_columns.sql (projects columns analog):
```sql
ALTER TABLE projects ADD COLUMN IF NOT EXISTS show_id UUID REFERENCES shows(id) ON DELETE CASCADE;
ALTER TABLE projects ADD COLUMN IF NOT EXISTS episode_number INTEGER;
```

Existing SQLAlchemy column definitions to mirror (database.py:163-164):
```python
breakdown_stale = Column(Boolean, default=False)
shotlist_stale = Column(Boolean, default=False)
```

Show model lives at database.py:91-106 (last column episode_duration_minutes).
Project model lives at database.py:151-178 (stale flags at 163-164).
Framework/TemplateType/PhaseType PG-enum convention at database.py:121-135 / 157-159 — D-03 INTENTIONALLY does NOT follow this for continuity_mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent delta migration 011_continuity_columns.sql</name>
  <files>backend/migrations/delta/011_continuity_columns.sql</files>
  <read_first>
    - backend/migrations/delta/007_bible_columns.sql (shows ADD COLUMN analog)
    - backend/migrations/delta/008_episode_columns.sql (projects ADD COLUMN analog)
    - backend/migrations/delta/README.md (naming + idempotency rules)
  </read_first>
  <action>
    Create backend/migrations/delta/011_continuity_columns.sql with three idempotent statements:
    (1) `ALTER TABLE shows ADD COLUMN IF NOT EXISTS continuity_mode VARCHAR DEFAULT 'anthology';` — per D-01 (anthology = zero behavior change on upgrade) and D-03 (VARCHAR with string default, NOT a Postgres ENUM type — conscious deviation from the Framework/TemplateType PG-enum convention; documented in a leading SQL comment).
    (2) `ALTER TABLE projects ADD COLUMN IF NOT EXISTS episode_summary TEXT;` — nullable storage only (generation is Phase 69).
    (3) `ALTER TABLE projects ADD COLUMN IF NOT EXISTS episode_summary_stale BOOLEAN DEFAULT FALSE;` — mirrors breakdown_stale/shotlist_stale, per ESUM-02.
    Add a top-of-file comment noting the migration number, the v10.0 purpose, and the D-03 VARCHAR-not-PG-enum deviation. Use only IF NOT EXISTS forms — no destructive statements, no edits to prior deltas 001-010.
  </action>
  <verify>
    <automated>grep -c 'ADD COLUMN IF NOT EXISTS' backend/migrations/delta/011_continuity_columns.sql | grep -qx 3 && grep -q "continuity_mode VARCHAR DEFAULT 'anthology'" backend/migrations/delta/011_continuity_columns.sql && grep -q 'episode_summary TEXT' backend/migrations/delta/011_continuity_columns.sql && grep -q 'episode_summary_stale BOOLEAN DEFAULT FALSE' backend/migrations/delta/011_continuity_columns.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - backend/migrations/delta/011_continuity_columns.sql contains exactly 3 `ADD COLUMN IF NOT EXISTS` statements
    - continuity_mode default is the string `'anthology'` (NOT 'connected', NOT a PG ENUM type)
    - episode_summary is TEXT and nullable (no NOT NULL, no default)
    - episode_summary_stale is BOOLEAN DEFAULT FALSE
    - No prior delta file (001-010) is modified
    - A leading comment records the D-03 VARCHAR-not-PG-enum conscious deviation
  </acceptance_criteria>
  <done>The new delta file exists, is fully idempotent, and adds all three columns with the D-01/D-03 defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the three columns on the SQLAlchemy Show and Project models</name>
  <files>backend/app/models/database.py</files>
  <read_first>
    - backend/app/models/database.py:91-106 (Show model — append continuity_mode after episode_duration_minutes)
    - backend/app/models/database.py:151-178 (Project model — append episode_summary fields near breakdown_stale/shotlist_stale at 163-164)
  </read_first>
  <action>
    In the Show model (database.py, after episode_duration_minutes at line 106) add: `continuity_mode = Column(String(20), nullable=False, default="anthology", server_default="anthology")`. Use String (NOT a PG Enum), matching the migration's VARCHAR default per D-03; server_default keeps DB-level parity with the delta so rows inserted outside the ORM also default correctly.
    In the Project model (database.py, alongside breakdown_stale/shotlist_stale at lines 163-164) add: `episode_summary = Column(Text, nullable=True)` and `episode_summary_stale = Column(Boolean, default=False, server_default="false")`. Mirror the existing Boolean stale-flag column style exactly.
    Do not add relationships, indexes, or any PG Enum type. Do not touch the Framework/TemplateType/PhaseType enum definitions.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && python -c "from app.models.database import Show, Project; assert 'continuity_mode' in Show.__table__.columns, 'Show.continuity_mode missing'; assert 'episode_summary' in Project.__table__.columns; assert 'episode_summary_stale' in Project.__table__.columns; assert str(Show.__table__.columns['continuity_mode'].type).upper().startswith('VARCHAR'), 'continuity_mode must be VARCHAR/String not Enum'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.models.database import Show, Project` imports without error
    - Show.__table__.columns includes continuity_mode and its type is String/VARCHAR (NOT an Enum)
    - Project.__table__.columns includes episode_summary (Text, nullable) and episode_summary_stale (Boolean)
    - continuity_mode has default/server_default of "anthology"
    - episode_summary_stale defaults to False
    - No new PG Enum type introduced
  </acceptance_criteria>
  <done>Both models expose the three new columns with String/Text/Boolean types and the D-01 anthology / False defaults, parity-matched to the migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot process → database | Migration SQL executes against the live DB on startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Denial of Service | boot migration re-run | mitigate | All statements use `ADD COLUMN IF NOT EXISTS`; re-run is a no-op — verified by Plan 03's idempotency test and the grep gate above. Non-destructive: no DROP/ALTER TYPE. |
| T-67-02 | Tampering | existing rows on upgrade | accept | Default `'anthology'` matches today's bible-only behavior (D-01), so backfill changes no generation behavior; standalone (show_id NULL) rows untouched. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan (pure SQL + SQLAlchemy column additions). |
</threat_model>

<verification>
- New delta file is idempotent and additive only.
- SQLAlchemy models import cleanly and expose the three columns with correct types/defaults.
- No PG Enum type created for continuity_mode (D-03 deviation honored).
</verification>

<success_criteria>
- `backend/migrations/delta/011_continuity_columns.sql` adds all three columns idempotently with anthology/null/False defaults.
- `Show` and `Project` models mirror the columns as String/Text/Boolean (no Enum), parity-matched to the migration.
</success_criteria>

<output>
Create `.planning/phases/67-continuity-data-model-migration/67-01-SUMMARY.md` when done
</output>
