---
phase: 67-continuity-data-model-migration
plan: 03
type: execute
wave: 3
depends_on: ["67-01", "67-02"]
files_modified:
  - backend/app/api/endpoints/phase_data.py
  - backend/app/models/schemas.py
  - backend/app/tests/test_episode_summary_staleness.py
autonomous: true
requirements: [ESUM-02]
must_haves:
  truths:
    - "Editing an episode whose episode_summary is non-empty sets episode_summary_stale=True"
    - "Editing an episode whose episode_summary is empty/null leaves episode_summary_stale=False"
    - "Editing a story/idea phase (not write/scenes) never sets episode_summary_stale"
    - "Standalone projects (show_id NULL) are unaffected — the hook is existence-gated on episode_summary only, not show linkage"
    - "episode_summary_stale is surfaced (read-only) on the Project/episode read response; episode_summary text is NOT exposed"
    - "Re-running the boot migration is a no-op (idempotency proven)"
  artifacts:
    - path: "backend/app/api/endpoints/phase_data.py"
      provides: "_mark_episode_summary_stale helper + call at the existing stale-mark site"
      contains: "_mark_episode_summary_stale"
    - path: "backend/app/models/schemas.py"
      provides: "episode_summary_stale (read-only) on the Project schema"
      contains: "episode_summary_stale"
    - path: "backend/app/tests/test_episode_summary_staleness.py"
      provides: "Existence-gating, phase-gating, standalone, and idempotency tests"
      contains: "episode_summary_stale"
  key_links:
    - from: "backend/app/api/endpoints/phase_data.py update flow (line ~256-261)"
      to: "_mark_episode_summary_stale"
      via: "call gated on phase in BREAKDOWN_SENSITIVE_PHASES"
      pattern: "_mark_episode_summary_stale\\(db, project_id\\)"
    - from: "backend/app/models/schemas.py Project"
      to: "database.Project.episode_summary_stale"
      via: "from_attributes serialization"
      pattern: "episode_summary_stale"
---

<objective>
Wire the episode-summary staleness hook and surface the read-only flag. Add `_mark_episode_summary_stale` mirroring the existing breakdown/shotlist helpers (existence-gated), call it at the same site, and expose `episode_summary_stale` on the Project read schema. Delivers ESUM-02.

Purpose: When an episode's screenplay/content changes, any existing summary must be marked stale so Phase 69's lazy regen knows to refresh it — without firing when no summary exists.
Output: Updated `phase_data.py` (helper + call), `schemas.py` (Project.episode_summary_stale), and a dedicated staleness test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67-continuity-data-model-migration/67-CONTEXT.md
@.planning/phases/67-continuity-data-model-migration/67-01-SUMMARY.md
@.planning/phases/67-continuity-data-model-migration/67-02-SUMMARY.md

<interfaces>
<!-- Existing stale helpers to mirror EXACTLY (phase_data.py:21-53) -->
```python
def _mark_breakdown_stale(db: Session, project_id) -> None:
    has_breakdown = db.query(BreakdownElement).filter(
        BreakdownElement.project_id == str(project_id),
        BreakdownElement.is_deleted == False,
    ).first() is not None
    if has_breakdown:
        project = db.query(database.Project).filter(
            database.Project.id == str(project_id)).first()
        if project:
            project.breakdown_stale = True
```

<!-- Sensitive-phase gate + call site (phase_data.py:16-18, 256-261) -->
```python
BREAKDOWN_SENSITIVE_PHASES = {"write", "scenes"}
SHOTLIST_SENSITIVE_PHASES = {"write", "scenes"}
# ...
if phase in BREAKDOWN_SENSITIVE_PHASES:
    _mark_breakdown_stale(db, project_id)
if phase in SHOTLIST_SENSITIVE_PHASES:
    _mark_shotlist_stale(db, project_id)
db.commit()   # caller commits — helpers must NOT commit
```

<!-- NOTE: 67-02 also modifies schemas.py (adds ContinuityMode enum + Show fields).
     Read 67-02-SUMMARY.md (referenced above) for the post-02 state of schemas.py so this
     plan appends Project.episode_summary_stale WITHOUT clobbering the ContinuityMode enum
     or the Show* schema edits from Plan 02. -->

<!-- Project read schema to extend (schemas.py:94-108). Note: breakdown_stale/shotlist_stale are NOT currently on this schema; add ONLY episode_summary_stale per D-04. -->
```python
class Project(ProjectBase):
    id: UUID; owner_id: UUID
    show_id: Optional[UUID] = None
    episode_number: Optional[int] = None
    # add episode_summary_stale here (read-only)
    model_config = ConfigDict(from_attributes=True)
```

<!-- Test fixtures: client, db_session, mock_auth_headers; helpers _create_project_via_api / _make_phase_data / _get_project mirror test_staleness.py. The db_session fixture is SQLite-backed (conftest.py: SQLALCHEMY_TEST_DATABASE_URL = "sqlite://"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _mark_episode_summary_stale helper and call it at the existing stale-mark site</name>
  <files>backend/app/api/endpoints/phase_data.py</files>
  <read_first>
    - backend/app/api/endpoints/phase_data.py:21-53 (the two helpers to mirror verbatim)
    - backend/app/api/endpoints/phase_data.py:256-261 (the call site + caller's db.commit())
    - backend/app/models/database.py:163-178 (Project.episode_summary / episode_summary_stale columns from Plan 01)
  </read_first>
  <behavior>
    - Helper sets episode_summary_stale=True only when the Project's episode_summary is non-empty (per D-02 existence gating)
    - Helper leaves episode_summary_stale=False when episode_summary is None or empty string
    - Helper does NOT call db.commit() (caller's commit at line 261 covers it, per D-02a)
    - Helper is called inside the `phase in BREAKDOWN_SENSITIVE_PHASES` block alongside the existing two marks
    - _mark_episode_summary_stale is defined AFTER _mark_shotlist_stale in source order (mirror placement exactly)
  </behavior>
  <action>
    Add `_mark_episode_summary_stale(db: Session, project_id) -> None` immediately after `_mark_shotlist_stale` (after line 53) in phase_data.py. Copy the `_mark_shotlist_stale` structure: load the Project by str(project_id); the existence gate is the Project's OWN `episode_summary` being non-empty (truthy after strip) — i.e. fetch the project, and if `project and project.episode_summary and project.episode_summary.strip()`, set `project.episode_summary_stale = True`. This mirrors D-02 (flip True only when the dependent artifact — here the summary text on the same row — already exists). Do NOT commit inside the helper.
    Call it at the existing site (lines 256-261), inside the SAME `if phase in BREAKDOWN_SENSITIVE_PHASES:` block (write/scenes edits), right after the breakdown/shotlist marks and before `db.commit()`. Gate it on the same set per D-02a — add `_mark_episode_summary_stale(db, project_id)` under the BREAKDOWN_SENSITIVE_PHASES condition (no new sensitive-phase set needed; {"write","scenes"} is correct). Do not add a commit; the existing db.commit() at line 261 persists it.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && python -c "from app.api.endpoints import phase_data; import inspect; assert hasattr(phase_data, '_mark_episode_summary_stale'); src=inspect.getsource(phase_data); assert 'episode_summary_stale = True' in src; assert src.count('_mark_episode_summary_stale(db, project_id)') >= 1, 'helper not called at site'; assert src.index('def _mark_episode_summary_stale') > src.index('def _mark_shotlist_stale'), 'episode_summary helper must be defined AFTER _mark_shotlist_stale'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - phase_data.py defines `_mark_episode_summary_stale` immediately after `_mark_shotlist_stale`
    - The helper's `def` appears AFTER `def _mark_shotlist_stale` in source order (verified by the automated index check above)
    - The helper gates on a non-empty `project.episode_summary` (existence gating, D-02)
    - The helper contains no `db.commit()` call
    - `_mark_episode_summary_stale(db, project_id)` is invoked inside the `phase in BREAKDOWN_SENSITIVE_PHASES` block before the existing db.commit()
    - The {"write","scenes"} sensitive-phase set is reused (no new constant)
  </acceptance_criteria>
  <done>The helper exists, is placed after _mark_shotlist_stale, mirrors the existence-gated no-commit pattern, and fires at the same write/scenes edit site as breakdown/shotlist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface episode_summary_stale on the Project schema and add the staleness test module</name>
  <files>backend/app/models/schemas.py, backend/app/tests/test_episode_summary_staleness.py</files>
  <read_first>
    - backend/app/models/schemas.py:94-108 (Project read schema)
    - backend/app/tests/test_staleness.py:32-160 (existence-gating + phase-gating test patterns and fixtures to mirror)
    - backend/app/tests/conftest.py (confirm db_session engine: SQLite, SQLALCHEMY_TEST_DATABASE_URL = "sqlite://")
  </read_first>
  <behavior>
    - Project read response includes episode_summary_stale (read-only boolean); does NOT include episode_summary text
    - Editing an episode (PATCH write/scenes) with a non-empty episode_summary sets episode_summary_stale=True
    - Editing with empty/null episode_summary leaves it False
    - Editing a non-write/scenes phase (e.g. story) never sets it True
    - A standalone project (show_id NULL) with a non-empty summary still flips True on edit, and with no summary stays False — proving the hook keys on summary existence, not show linkage
    - Running the boot delta migration twice is a no-op (idempotency)
  </behavior>
  <action>
    In schemas.py Project (lines 94-108) add `episode_summary_stale: bool = False` (read-only flag, D-04). Do NOT add episode_summary text to any schema (stays internal per D-04). Since the field is read-only and from_attributes is set, the ORM column from Plan 01 supplies the value.
    Create backend/app/tests/test_episode_summary_staleness.py mirroring test_staleness.py's fixtures/helpers (client, db_session, mock_auth_headers, _create_project_via_api, _make_phase_data, _get_project). Tests:
    (1) edit (PATCH write phase) on a project with a non-empty episode_summary → episode_summary_stale True;
    (2) edit on a project with empty/null episode_summary → stays False (existence gating, D-02);
    (3) edit on a story phase (not write/scenes) with a non-empty summary → stays False (phase gating);
    (4) standalone project (show_id None) with a non-empty summary → flips True on edit, and a second standalone project with no summary → stays False (proves unaffected-by-show-linkage / existence-only gating, per phase specifics);
    (5) idempotency: the test db_session fixture is SQLite-backed (conftest.py SQLALCHEMY_TEST_DATABASE_URL = "sqlite://"), which cannot execute Postgres-style `ADD COLUMN IF NOT EXISTS`. Therefore assert idempotency by STATIC FILE INSPECTION: read backend/migrations/delta/011_continuity_columns.sql and confirm every ADD COLUMN statement uses `IF NOT EXISTS` (no bare ADD COLUMN). Document in a test docstring/comment that this is the structural check and that the DEFINITIVE runtime idempotency proof is 67-01/Task 1's grep gate (all column adds use `ADD COLUMN IF NOT EXISTS`), not this SQLite-bound test.
    (6) Project read schema: serialize a Project ORM object and assert episode_summary_stale is present and episode_summary text is absent from the dumped fields.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && PYTHONPATH=. pytest app/tests/test_episode_summary_staleness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `episode_summary_stale` present in `schemas.Project.model_fields`; `episode_summary` NOT present
    - Test (1): PATCH write phase with non-empty summary → episode_summary_stale True
    - Test (2): PATCH write phase with empty/null summary → episode_summary_stale False
    - Test (3): PATCH story phase → episode_summary_stale stays False
    - Test (4): standalone (show_id None) with summary flips True; standalone without summary stays False
    - Test (5): idempotency is asserted via static inspection of 011_continuity_columns.sql (all ADD COLUMN use IF NOT EXISTS), because the test fixture is SQLite — the test documents that 67-01/Task 1's grep gate is the definitive runtime idempotency proof
    - Test (6): serialized Project includes episode_summary_stale, excludes episode_summary text
    - `pytest app/tests/test_episode_summary_staleness.py` exits 0
  </acceptance_criteria>
  <done>The read-only flag is surfaced (text kept internal) and a dedicated test module proves existence-gating, phase-gating, standalone behavior, idempotency (static inspection, with the 67-01 grep gate as the runtime proof of record), and the read surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → phase-data PATCH | Episode content edits flow through the existing PATCH route (auth + ownership already enforced) |
| boot process → database | The Plan 01 delta runs on startup; idempotency proven here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05 | Tampering | episode_summary_stale flag flips | mitigate | Helper is existence-gated (only flips when a summary exists) and reuses the same auth-gated PATCH path; no new endpoint, no new write surface. |
| T-67-06 | Information Disclosure | Project read response | mitigate | Only the boolean episode_summary_stale is added to the schema; episode_summary text is explicitly NOT added (D-04), so the internal summary cannot leak via the API this phase — asserted by test (6). |
| T-67-01 | Denial of Service | boot migration re-run | mitigate | Idempotency proven by 67-01/Task 1's grep gate (definitive runtime proof) and corroborated by test (5)'s static IF NOT EXISTS inspection; closes the migration-safety threat opened in Plan 01. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
- Stale flag flips True only when a summary exists and only on write/scenes edits.
- Standalone projects behave by the same existence gate (unaffected by show linkage).
- Migration re-run is a no-op (grep gate in 67-01 is the runtime proof; test (5) corroborates structurally).
- episode_summary_stale exposed read-only; summary text stays internal.
</verification>

<success_criteria>
- ESUM-02 satisfied: editing an episode marks its summary stale, mirroring breakdown_stale/shotlist_stale, gated on summary existence.
- The read-only flag is on the Project schema; episode_summary text is not exposed.
- Idempotency and standalone-unaffected behaviors are proven by tests.
</success_criteria>

<output>
Create `.planning/phases/67-continuity-data-model-migration/67-03-SUMMARY.md` when done
</output>
</content>
</invoke>
