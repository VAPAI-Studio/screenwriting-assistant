---
phase: 09-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/009_breakdown_tables.sql
autonomous: true
requirements:
  - BKDN-01
  - BKDN-02
  - BKDN-03
  - BKDN-04

must_haves:
  truths:
    - "The SQL migration file creates breakdown_elements, element_scene_links, and breakdown_runs tables with all columns, indexes, and constraints"
    - "The breakdown_elements table enforces UNIQUE(project_id, category, name) and includes user_modified and is_deleted columns"
    - "The element_scene_links table cascades on both breakdown_elements and list_items deletion"
    - "The breakdown_runs table tracks extraction status, element counts, and timestamps"
    - "The projects table gains a breakdown_stale BOOLEAN column defaulting to FALSE"
  artifacts:
    - path: "backend/migrations/009_breakdown_tables.sql"
      provides: "DDL for 3 new tables + ALTER TABLE for breakdown_stale"
      contains: "CREATE TABLE IF NOT EXISTS breakdown_elements"
  key_links:
    - from: "breakdown_elements.project_id"
      to: "projects.id"
      via: "REFERENCES ON DELETE CASCADE"
      pattern: "REFERENCES projects\\(id\\) ON DELETE CASCADE"
    - from: "element_scene_links.element_id"
      to: "breakdown_elements.id"
      via: "REFERENCES ON DELETE CASCADE"
      pattern: "REFERENCES breakdown_elements\\(id\\) ON DELETE CASCADE"
    - from: "element_scene_links.scene_item_id"
      to: "list_items.id"
      via: "REFERENCES ON DELETE CASCADE"
      pattern: "REFERENCES list_items\\(id\\) ON DELETE CASCADE"
    - from: "breakdown_runs.project_id"
      to: "projects.id"
      via: "REFERENCES ON DELETE CASCADE"
      pattern: "REFERENCES projects\\(id\\) ON DELETE CASCADE"
---

<objective>
Create the SQL migration file that defines the database schema for the v2.0 Script Breakdown feature.

Purpose: Establish the PostgreSQL tables (breakdown_elements, element_scene_links, breakdown_runs) and the breakdown_stale column on projects that the entire breakdown feature depends on.
Output: `backend/migrations/009_breakdown_tables.sql` -- idempotent DDL ready to run against PostgreSQL.
</objective>

<execution_context>
@/Users/yvesfogel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yvesfogel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-foundation/09-RESEARCH.md

@backend/migrations/008_agent_pipeline_maps.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 009_breakdown_tables.sql migration</name>
  <files>backend/migrations/009_breakdown_tables.sql</files>
  <action>
Create the migration file following the exact pattern from `008_agent_pipeline_maps.sql`. The DDL is fully specified in the research doc's Code Examples section. Include:

1. **Header comment** explaining migration purpose (3 new tables + 1 ALTER TABLE).

2. **breakdown_elements table** (BKDN-01):
   - `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
   - `project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE`
   - `category VARCHAR(50) NOT NULL` (NOT a PG ENUM -- VARCHAR for extensibility)
   - `name VARCHAR(500) NOT NULL`
   - `description TEXT DEFAULT ''`
   - `metadata JSONB DEFAULT '{}'` (JSONB not JSON -- supports indexing)
   - `source VARCHAR(20) DEFAULT 'ai'`
   - `user_modified BOOLEAN DEFAULT FALSE`
   - `is_deleted BOOLEAN DEFAULT FALSE`
   - `sort_order INTEGER DEFAULT 0`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `updated_at TIMESTAMPTZ DEFAULT NOW()`
   - `CONSTRAINT uq_breakdown_element UNIQUE (project_id, category, name)`
   - Indexes: `idx_breakdown_elements_project` on `(project_id)`, `idx_breakdown_elements_category` on `(project_id, category)`, partial index `idx_breakdown_elements_active` on `(project_id, category) WHERE is_deleted = FALSE`

3. **element_scene_links table** (BKDN-02):
   - `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
   - `element_id UUID NOT NULL REFERENCES breakdown_elements(id) ON DELETE CASCADE`
   - `scene_item_id UUID NOT NULL REFERENCES list_items(id) ON DELETE CASCADE` -- CRITICAL: both FKs must CASCADE
   - `context TEXT DEFAULT ''`
   - `source VARCHAR(20) DEFAULT 'ai'`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `CONSTRAINT uq_element_scene UNIQUE (element_id, scene_item_id)`
   - Indexes: `idx_element_scene_element` on `(element_id)`, `idx_element_scene_scene` on `(scene_item_id)`

4. **breakdown_runs table** (BKDN-03):
   - `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
   - `project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE`
   - `status VARCHAR(20) DEFAULT 'pending'`
   - `config JSONB DEFAULT '{}'`
   - `result_summary JSONB DEFAULT '{}'`
   - `error_message TEXT`
   - `elements_created INTEGER DEFAULT 0`
   - `elements_updated INTEGER DEFAULT 0`
   - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - `completed_at TIMESTAMPTZ`
   - Index: `idx_breakdown_runs_project` on `(project_id)`

5. **ALTER TABLE projects** (BKDN-04):
   - `ADD COLUMN IF NOT EXISTS breakdown_stale BOOLEAN DEFAULT FALSE`
   - MUST use `IF NOT EXISTS` and `DEFAULT FALSE` to handle existing rows safely

All CREATE TABLE and CREATE INDEX statements MUST use `IF NOT EXISTS` for idempotency.
  </action>
  <verify>
    <automated>test -f /Users/yvesfogel/Desktop/screenwriting-assistant/backend/migrations/009_breakdown_tables.sql && grep -q "CREATE TABLE IF NOT EXISTS breakdown_elements" /Users/yvesfogel/Desktop/screenwriting-assistant/backend/migrations/009_breakdown_tables.sql && grep -q "CREATE TABLE IF NOT EXISTS element_scene_links" /Users/yvesfogel/Desktop/screenwriting-assistant/backend/migrations/009_breakdown_tables.sql && grep -q "CREATE TABLE IF NOT EXISTS breakdown_runs" /Users/yvesfogel/Desktop/screenwriting-assistant/backend/migrations/009_breakdown_tables.sql && grep -q "breakdown_stale" /Users/yvesfogel/Desktop/screenwriting-assistant/backend/migrations/009_breakdown_tables.sql && grep -q "REFERENCES list_items(id) ON DELETE CASCADE" /Users/yvesfogel/Desktop/screenwriting-assistant/backend/migrations/009_breakdown_tables.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Migration file exists with all 3 CREATE TABLE statements (using IF NOT EXISTS), all indexes, the unique constraints, both CASCADE foreign keys on element_scene_links, and the ALTER TABLE for breakdown_stale. File follows the 008 migration pattern exactly.</done>
</task>

</tasks>

<verification>
- File `backend/migrations/009_breakdown_tables.sql` exists
- Contains `CREATE TABLE IF NOT EXISTS` for breakdown_elements, element_scene_links, breakdown_runs
- Contains `ALTER TABLE projects ADD COLUMN IF NOT EXISTS breakdown_stale`
- breakdown_elements has UNIQUE constraint on (project_id, category, name)
- element_scene_links has ON DELETE CASCADE on BOTH element_id and scene_item_id FKs
- All indexes use IF NOT EXISTS
- No PG ENUM types (uses VARCHAR for category)
</verification>

<success_criteria>
The migration file is complete, idempotent, and matches the schema specified in the ARCHITECTURE.md research. It can be applied to PostgreSQL via `psql -f` without errors on both fresh and existing databases.
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-foundation/09-01-SUMMARY.md`
</output>
