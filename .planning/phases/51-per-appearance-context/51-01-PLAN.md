---
phase: 51-per-appearance-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/breakdown_service.py
  - backend/app/tests/test_breakdown_service.py
  - frontend/src/components/Breakdown/ElementCard.tsx
autonomous: true
requirements: [APPR-01, APPR-02, APPR-03]

must_haves:
  truths:
    - "After extraction, each AI-sourced ElementSceneLink.context holds the AI's per-appearance context string (no longer empty) — APPR-02 (D-51-01)"
    - "An element appearing in two scenes is one element with two scene links, not two elements — APPR-03 verified, not rebuilt (D-51-03)"
    - "Each element exposes its scene links via the API/UI — APPR-01 verified"
    - "Breakdown UI surfaces per-appearance context: detail scene list shows it inline, card scene chips expose it on hover — APPR-02 (D-51-02)"
    - "Empty context renders gracefully (no 'undefined', no empty tooltip) for legacy/user links (D-51-02)"
    - "User-sourced scene links are preserved; only AI-sourced links are deleted/recreated on re-extract (D-51-01, D-51-03)"
    - "Existing breakdown tests stay green: single AI call, dedup, upsert, staleness clear, Phase 50 prompt unchanged (D-51-03)"
  artifacts:
    - path: "backend/app/services/breakdown_service.py"
      provides: "_map_scene_indices_to_ids returns (scene_id, context) pairs; _reconcile_scene_links writes appearance context"
      contains: "context=context"
    - path: "backend/app/tests/test_breakdown_service.py"
      provides: "APPR-02 proof (context persisted) + APPR-03 verify (consolidation) tests"
      contains: "link.context"
    - path: "frontend/src/components/Breakdown/ElementCard.tsx"
      provides: "Scene chips expose link.context as hover tooltip, graceful when empty"
      contains: "title="
  key_links:
    - from: "breakdown_service.extract()"
      to: "_map_scene_indices_to_ids"
      via: "passes extracted_el.scene_appearances, receives (scene_id, context) pairs"
      pattern: "_map_scene_indices_to_ids"
    - from: "_reconcile_scene_links"
      to: "database.ElementSceneLink.context"
      via: "writes context=context (not context='')"
      pattern: "context=context"
    - from: "ElementCard scene chip"
      to: "link.context"
      via: "title attribute tooltip"
      pattern: "title=\\{link.context"
---

<objective>
Thread the AI's per-appearance `context` through the scene-id mapping boundary so
`ElementSceneLink.context` is populated (it is currently overwritten with `""`),
and expose that context on the breakdown card scene chips. The detail scene list
already renders `link.context` — this plan completes the one backend gap and the
one remaining UI gap, then locks behavior with tests.

Purpose: Deliver APPR-02 (per-appearance context note, surfaced in UI) and verify
APPR-01 (element records its scenes) and APPR-03 (duplicate consolidation) — both
already satisfied by existing code — without regressing the breakdown subsystem.

Output: Populated `ElementSceneLink.context`, card chip tooltips, and tests proving
context persistence + consolidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v7.0-RESCOPE-NOTE.md
@.planning/phases/51-per-appearance-context/51-CONTEXT.md

@backend/app/services/breakdown_service.py
@backend/app/tests/test_breakdown_service.py
@frontend/src/components/Breakdown/ElementCard.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

backend/app/services/breakdown_service.py (line 32-40):
```python
class ExtractedSceneAppearance(BaseModel):
    scene_index: int   # 1-based, AI-reported
    context: str       # per-appearance note — THE value to thread through
```

Current shape to change (breakdown_service.py ~457-479):
`_map_scene_indices_to_ids(ctx, scene_appearances) -> List[str]` — drops context.
Caller (extract() ~550-551):
```python
scene_ids = self._map_scene_indices_to_ids(ctx, extracted_el.scene_appearances)
self._reconcile_scene_links(db, db_element, scene_ids)
```
Current `_reconcile_scene_links(db, element, new_scene_ids: List[str])` writes
`ElementSceneLink(..., context="", source="ai")` at line ~452.

database.ElementSceneLink columns (already exist, reuse as-is):
`element_id, scene_item_id, context (Text), source ("ai"|"user")`.

frontend/src/types/index.ts (~271): `SceneLink { id, scene_item_id, context: string, source, scene_title? }`
ElementCard scene chip (~254-266) renders a `<button>` with text `Scene {index + 1}` and NO title attr.
ElementSceneList.tsx (~33-37) ALREADY renders `link.context` inline when truthy — DO NOT touch it; it satisfies the detail-view half of D-51-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread per-appearance context through the scene-id mapping (APPR-02 backend)</name>
  <files>backend/app/services/breakdown_service.py</files>
  <read_first>
    breakdown_service.py: lines 319-346 (_deduplicate_elements — DO NOT change),
    420-479 (_reconcile_scene_links + _map_scene_indices_to_ids — the boundary),
    509-575 (extract() caller at ~550-551).
  </read_first>
  <behavior>
    - _map_scene_indices_to_ids returns a list of (scene_id, context) pairs (tuple — Claude's Discretion per D-51-01) instead of List[str], carrying each ExtractedSceneAppearance.context alongside its mapped scene_id.
    - Invalid/out-of-range scene_index is still skipped with the same logger.warning (unchanged).
    - _reconcile_scene_links accepts the new (scene_id, context) shape and writes context=<context> into each AI-sourced ElementSceneLink (replacing the context="" at ~452).
    - Still deletes ONLY source="ai" links; preserves source="user" links; skips an (element, scene) pair when a user-sourced link already exists.
    - extract() caller (~550-551) passes/consumes the new shape unchanged in behavior.
  </behavior>
  <action>
    Change `_map_scene_indices_to_ids` to build and return a list of (scene_id, context) tuples — for each appearance, append `(ctx.scene_summaries[zero_based]["id"], appearance.context)` when in range; keep the out-of-range skip + logger.warning exactly as-is. Update its return type annotation. Change `_reconcile_scene_links` parameter (rename `new_scene_ids` to e.g. `new_links: List[Tuple[str, str]]`, update type hint) and iterate `for scene_id, context in new_links:`; keep the source="ai" delete, the user-link skip check (unchanged), and replace `context=""` with `context=context` per D-51-01. Update the extract() caller variable name accordingly (the two-line call at ~550-551). Add `Tuple` to the typing import if not already present. PRESERVE per D-51-03: the single AI call, EXTRACTION_SYSTEM_PROMPT, _deduplicate_elements (APPR-03), _upsert_elements (user_modified/is_deleted), staleness clear, audit run, Phase 50 scene-scoped prompt — touch nothing else. No schema change, no migration (the context column already exists).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && grep -n 'context=context' app/services/breakdown_service.py && ! grep -n 'context=""' app/services/breakdown_service.py</automated>
  </verify>
  <acceptance_criteria>
    `_reconcile_scene_links` writes `context=context` and no longer contains a `context=""` write; `_map_scene_indices_to_ids` returns pairs; the out-of-range warning path is intact; the extract() caller compiles against the new shape.
  </acceptance_criteria>
  <done>Backend threads appearance.context into ElementSceneLink.context; the source `context=""` write is gone; all preserved behavior untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests — context persistence (APPR-02), consolidation (APPR-03), scene exposure (APPR-01)</name>
  <files>backend/app/tests/test_breakdown_service.py</files>
  <read_first>
    test_breakdown_service.py: lines 380-458 (test_deduplicate scaffold + test_scene_linking ~424
    which already drives scene_appearances with context strings "Draws sword"/"Presents sword"),
    and the _mock_extraction_response / _setup_project_with_screenplay helpers near the top of the file.
  </read_first>
  <behavior>
    - APPR-02 proof: after extract() with appearances carrying context strings, the persisted ElementSceneLink.context equals the AI's per-appearance context for the matching scene (was "" before this phase).
    - APPR-03 verify: an element whose scene_appearances span two scenes consolidates to ONE BreakdownElement with TWO ElementSceneLinks (via existing _deduplicate_elements path) — assert one element, two links.
    - APPR-01 verify: the persisted element exposes its scene links (count > 0).
  </behavior>
  <action>
    Extend test_scene_linking (or add a sibling test, e.g. test_scene_link_context_persisted) to map each created ElementSceneLink to its expected context: assert the link for scene_ids[0] has context "Draws sword" and the link for scene_ids[2] has context "Presents sword" (per the existing mock at ~428-438) — proving APPR-02. Add an APPR-03 verify test: feed an ExtractedElement (or two same-name same-category elements) whose appearances cover two distinct scene_index values, run extract(), and assert exactly one BreakdownElement with name match and exactly two ElementSceneLink rows. Reuse the AsyncMock chat_completion_structured pattern, _mock_extraction_response, and _setup_project_with_screenplay already in the file. Do NOT modify the yolo/session-isolation suites (per .planning/v6.0-PREEXISTING-TEST-CONCERN.md) and do not alter existing assertions in test_scene_linking — extend additively.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest app/tests/test_breakdown_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    New/extended tests prove ElementSceneLink.context is populated with the AI's per-appearance context and that a two-scene element consolidates to one element with two links; all of test_breakdown_service.py passes.
  </acceptance_criteria>
  <done>APPR-02 context persistence and APPR-03 consolidation are asserted; APPR-01 scene exposure confirmed; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Surface context on card scene chips (APPR-02 UI)</name>
  <files>frontend/src/components/Breakdown/ElementCard.tsx</files>
  <read_first>
    ElementCard.tsx: lines 251-268 (scene chip map — the `<button>` rendering `Scene {index + 1}`).
    NOTE: ElementSceneList.tsx (~33-37) ALREADY renders link.context inline — the detail-view half of
    D-51-02 is done. Do NOT touch ElementSceneList.tsx or ElementDetailPage.tsx.
  </read_first>
  <action>
    On the scene-chip `<button>` add `title={link.context || undefined}` so a non-empty per-appearance context shows as a native hover tooltip and an empty context produces no tooltip (never "undefined") — graceful empty per D-51-02. Use the existing dark-amber chip styling; introduce NO new tooltip/popover dependency (D-51-02, Deferred Ideas). No other behavior change to the chip (navigation onClick stays).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    `npm run build` (tsc && vite build) passes; scene chips carry `title={link.context || undefined}`; empty context yields no tooltip.
  </acceptance_criteria>
  <done>Card scene chips expose per-appearance context on hover; build is clean; no new dependency added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI → DB (`ElementSceneLink.context`) | AI-generated context string written to an existing Text column |
| DB → UI (chip `title`, detail line) | Stored context string rendered as React text / native title attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | AI context string written to ElementSceneLink.context | accept | Same trust level as existing AI-generated element name/description already persisted to the same table; no new external input, auth, or dependency. Internal tool (MEMORY: not market-facing). |
| T-51-02 | Information Disclosure | Context rendered in UI | accept | React escapes text by default; the `title` attribute is plain text (not HTML). No injection surface; context is the same provenance as already-rendered description. |
| T-51-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — explicitly forbidden by D-51-02/D-51-03 (no new dependency, no migration). |
</threat_model>

<verification>
- Backend service + API + staleness suites stay green:
  - `cd backend && source venv/bin/activate && pytest app/tests/test_breakdown_service.py -x -q`
  - `cd backend && source venv/bin/activate && pytest app/tests/test_breakdown_api.py app/tests/test_staleness.py -q`
- Frontend type/build gate (no FE test harness; lint is broken repo-wide — do not rely on it):
  - `cd frontend && npm run build`
- Source assertion that the context="" write is gone and the appearance context is written:
  - `grep -n 'context=context' backend/app/services/breakdown_service.py`
  - `! grep -n 'context=""' backend/app/services/breakdown_service.py`
</verification>

<success_criteria>
- ElementSceneLink.context is populated with the AI's per-appearance context after extract() (was "") — APPR-02 backend.
- Card scene chips expose context on hover; detail scene list shows it inline (pre-existing) — APPR-02 UI.
- A two-scene element consolidates to one element with two links — APPR-03 verified.
- Elements expose their scene links via API/UI — APPR-01 verified.
- All existing breakdown tests (service, api, staleness) remain green; single AI call, dedup, upsert, staleness, Phase 50 prompt unchanged — D-51-03.
- No schema change, no migration, no new dependency.
</success_criteria>

<output>
Create `.planning/phases/51-per-appearance-context/51-01-SUMMARY.md` when done.
</output>
