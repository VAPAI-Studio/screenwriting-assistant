---
phase: 69-auto-episode-summary-lazy-regeneration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/tests/test_episode_summary.py
  - backend/app/services/template_ai_service.py
  - backend/app/api/endpoints/projects.py
autonomous: true
requirements: [ESUM-01]
must_haves:
  truths:
    - "POST /api/projects/{id}/episode-summary generates and stores a non-empty episode_summary and clears episode_summary_stale"
    - "The endpoint rejects a project not owned by the caller (404)"
    - "summarize_episode produces bounded prose (json_mode=False, max_tokens~500), not the full script"
    - "Episode source text is reconstructed by episode_index, never positionally"
  artifacts:
    - path: "backend/app/tests/test_episode_summary.py"
      provides: "ESUM-01 unit tests + chat_completion mock helper (Wave 0 scaffold)"
      contains: "def _patch_chat_completion"
    - path: "backend/app/services/template_ai_service.py"
      provides: "async summarize_episode(db, project) + _read_episode_text_by_index"
      contains: "async def summarize_episode"
    - path: "backend/app/api/endpoints/projects.py"
      provides: "POST /{project_id}/episode-summary owner-scoped trigger"
      contains: "episode-summary"
  key_links:
    - from: "backend/app/api/endpoints/projects.py"
      to: "template_ai_service.summarize_episode"
      via: "await in the episode-summary endpoint"
      pattern: "summarize_episode"
    - from: "backend/app/services/template_ai_service.py"
      to: "ai_provider.chat_completion"
      via: "bounded prose call json_mode=False"
      pattern: "chat_completion\\("
---

<objective>
Build the shared episode summarizer and the eager initial-generation trigger (ESUM-01).

Add an async `summarize_episode(db, project) -> str` to `template_ai_service` that reads the
episode's screenplay text from `ScreenplayContent` **by episode_index** and calls
`ai_provider.chat_completion(json_mode=False, max_tokens~500)` to produce a BOUNDED prose
summary (NOT the full script — locked D2). Expose it via a NEW owner-scoped endpoint
`POST /api/projects/{id}/episode-summary` that stores `episode_summary`, clears
`episode_summary_stale`, and commits (caller-commits, Phase 67 convention).

Also create the Wave 0 test scaffold (`test_episode_summary.py` + a `chat_completion` mock
helper) that Plan 69-02 also uses.

Purpose: Supply the missing write path for ESUM-01 and the one summarizer that the lazy-regen
pre-pass (Plan 69-02) will reuse.
Output: New endpoint + service method + summarizer source-read helper + ESUM-01 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-auto-episode-summary-lazy-regeneration/69-RESEARCH.md
@.planning/phases/69-auto-episode-summary-lazy-regeneration/69-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration required. -->

ai_provider.chat_completion (backend/app/services/ai_provider.py:40) — async:
  chat_completion(messages: List[Dict[str,str]], temperature=0.7, max_tokens=2000,
                  json_mode=False, provider=None) -> str

Source-of-truth summarizer analog — template_ai_service._update_synopsis
(backend/app/services/template_ai_service.py:253-303): bounded prose, json_mode=False,
max_tokens=700, temperature=0.3, system="You are a precise story editor...", returns
(text or "").strip(); on failure logs + returns prior. `from ...services.ai_provider import chat_completion`
is already imported in this module.

Source-text read analog — breakdown_service._align_screenplay_to_scenes
(backend/app/services/breakdown_service.py:204-252): joins ScreenplayContent STRICTLY by
formatted_content.episode_index; rows ordered created_at.desc()/id.desc() so FIRST match per
index wins (newest); NO positional fallback.

ScreenplayContent (backend/app/models/database.py:298): project_id (UUID FK),
content (Text), formatted_content (JSON, holds {"episode_index": int, ...}).

Project columns (database.py:171-172): episode_summary (Text, nullable),
episode_summary_stale (Boolean default False).

Owner-scope pattern (projects.py:123-141): filter Project.id == project_id AND
Project.owner_id == current_user.id; raise NotFoundException(resource="Project",
identifier=str(project_id)) when missing. projects.router is mounted at prefix
"/api/projects" (main.py:128). `from ..dependencies import get_db, get_current_user`.

Test fixtures (backend/conftest.py): client, db_session, mock_auth_headers.
test_episode_summary_staleness.py helpers to mirror: _create_project_via_api(client,
mock_auth_headers, title), MOCK_USER_ID = "12345678-1234-5678-1234-567812345678".
test_bible_injection.py: _create_show / _create_project helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test scaffold + chat_completion mock helper</name>
  <files>backend/app/tests/test_episode_summary.py</files>
  <read_first>
    - backend/app/tests/test_episode_summary_staleness.py (fixture/helper patterns: _create_project_via_api, MOCK_USER_ID, _get_project)
    - backend/app/tests/test_bible_injection.py (lines 1-70: _create_show/_create_project, patch/AsyncMock imports)
    - backend/conftest.py (client, db_session, mock_auth_headers fixtures)
    - .planning/phases/69-auto-episode-summary-lazy-regeneration/69-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
  </read_first>
  <action>
    Create backend/app/tests/test_episode_summary.py with the shared scaffolding both plans
    use. Add module-level helpers reused across tests: _create_show / _create_project
    (mirror test_bible_injection.py), _insert_screenplay_content(db, project_id, episode_index,
    content) that adds a ScreenplayContent row with formatted_content={"episode_index": idx},
    and a context-manager mock helper _patch_chat_completion(return_value="...") that patches
    app.services.template_ai_service.chat_completion with an AsyncMock so summary tests are
    deterministic and offline (NEVER hit a live provider). Add one trivial sanity test asserting
    the helper patches correctly. Keep tests keyed so VALIDATION.md -k selectors work
    (initial, by_index, lazy_regen, preserves_fresh, regen_failure, existence_gate).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_episode_summary.py exists and collects/imports without error
    - _patch_chat_completion patches app.services.template_ai_service.chat_completion with an AsyncMock (no live API call)
    - _insert_screenplay_content writes a ScreenplayContent row carrying formatted_content.episode_index
  </acceptance_criteria>
  <done>The test file and mock helper exist; the sanity test passes; helpers are importable by Plan 69-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: summarize_episode service method + episode_index source read</name>
  <files>backend/app/services/template_ai_service.py, backend/app/tests/test_episode_summary.py</files>
  <read_first>
    - backend/app/services/template_ai_service.py:253-303 (_update_synopsis bounded-prose precedent to mirror — word cap, json_mode=False, temperature=0.3, system prompt, .strip())
    - backend/app/services/ai_provider.py:40-62 (chat_completion async signature)
    - backend/app/services/breakdown_service.py:204-252 (episode_index join, newest-first, no positional fallback)
    - backend/app/models/database.py:171-172,298-308 (Project.episode_summary/_stale, ScreenplayContent)
    - $HOME/.claude/... project memory note: ScreenplayContent has NO reliable order — join by episode_index ONLY (bit the project twice: v6.0 WR-01, v7.0 ph50)
  </read_first>
  <behavior>
    - _read_episode_text_by_index(db, project_id): inserts with SHUFFLED episode_index reconstruct in ascending episode_index order (test fails under a positional/created_at-order read)
    - rows lacking episode_index or with empty content are skipped
    - summarize_episode(db, project): calls chat_completion with json_mode=False and a bounded max_tokens (~500); returns stripped prose; empty source text returns ""
    - the prompt instructs a bounded prose summary (word cap ~250), NOT the full script, no JSON/headings
    - summarize_episode does NOT commit and does NOT write episode_summary (caller owns the write — Phase 67 convention)
  </behavior>
  <action>
    Add a module-level _read_episode_text_by_index(db, project_id) -> str to
    template_ai_service.py that queries ScreenplayContent filtered by project_id, ordered
    created_at.desc()/id.desc(), builds a dict keyed by formatted_content.episode_index
    (first-wins = newest), skips None index / empty content, and joins values in sorted
    episode_index order with "\n\n". Mirror breakdown_service._align_screenplay_to_scenes —
    no positional fallback. Add async summarize_episode(self, db, project) -> str on
    template_ai_service: read scene_text via the helper; if empty after strip return ""; else
    build a bounded continuity-summary prompt (WORD_CAP=250, prose only, no headings/JSON,
    explicitly "summary for continuity when writing LATER episodes", include project.title)
    and call chat_completion(json_mode=False, max_tokens=500, temperature=0.3) with the
    story-editor system prompt; return (text or "").strip(). Do NOT commit; do NOT mutate the
    project. Add the matching unit tests (-k by_index, -k initial summarizer-call assertions)
    using _patch_chat_completion and _insert_screenplay_content.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -k "by_index or initial" -q</automated>
  </verify>
  <acceptance_criteria>
    - summarize_episode calls chat_completion with json_mode=False and max_tokens ~500, returns stripped prose (asserted via the mock), not the full script
    - ScreenplayContent rows inserted with shuffled episode_index reconstruct in episode_index order (test must fail if the read goes positional)
    - empty/whitespace source text yields "" (no provider call required to return content)
    - summarize_episode performs no db.commit and does not set project.episode_summary
  </acceptance_criteria>
  <done>summarize_episode + _read_episode_text_by_index exist; by_index and initial summarizer tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST /api/projects/{id}/episode-summary eager trigger (ESUM-01)</name>
  <files>backend/app/api/endpoints/projects.py, backend/app/tests/test_episode_summary.py</files>
  <read_first>
    - backend/app/api/endpoints/projects.py:123-141 (owner-scope GET pattern, NotFoundException, imports)
    - backend/app/api/endpoints/wizards.py:386-391 (str()-coerced owner filter convention)
    - backend/app/services/template_ai_service.py (template_ai_service singleton + new summarize_episode from Task 2)
    - backend/app/tests/test_episode_summary_staleness.py:41-49 (_create_project_via_api)
    - .planning/phases/69-.../69-RESEARCH.md §Security Domain (V4 owner-scope; D-04: do not surface episode_summary text on the Project read schema)
  </read_first>
  <behavior>
    - POST /api/projects/{id}/episode-summary on an owned project returns 200, stores a non-empty episode_summary, sets episode_summary_stale=False, commits
    - the endpoint rejects a project not owned by the caller (404 via the owner-scoped filter)
    - when the summarizer returns "" (no source text), respond without overwriting an existing summary with empty (return a clear non-200 or a no-op success that leaves summary untouched — document which)
  </behavior>
  <action>
    Add an owner-scoped async endpoint POST /{project_id}/episode-summary to projects.py.
    Resolve the project with Project.id == str(project_id) AND owner_id == str(current_user.id)
    (str-coerce per the wizards.py convention for SQLite/PG safety); 404 if absent. Call
    await template_ai_service.summarize_episode(db, project); if the returned text is empty,
    do NOT clobber an existing summary — return a 422/clear response (document the choice in the
    SUMMARY). Otherwise set project.episode_summary = text, project.episode_summary_stale = False,
    db.commit(). Return a small response (e.g. {"status":"success","episode_summary_stale":False});
    do NOT add episode_summary to the Project read schema (preserve D-04). Use template_ai_service
    singleton. Add the -k initial endpoint tests (200 + stored summary + flag cleared via mocked
    provider; cross-owner 404).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -k initial -q</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/projects/{id}/episode-summary returns 200 for an owned project, stores a non-empty episode_summary, sets episode_summary_stale=False
    - a request for a project owned by another user returns 404 (no cross-user write)
    - the Project read schema still does NOT expose episode_summary (D-04 preserved)
    - the endpoint awaits template_ai_service.summarize_episode and the caller (endpoint) performs the commit
  </acceptance_criteria>
  <done>The endpoint exists, is owner-scoped, writes+clears+commits, and the initial-generation + authz tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /episode-summary | authenticated owner triggers summary generation for a project id |
| stored screenplay text → AI prompt | owner's own ScreenplayContent is summarized via the provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information Disclosure / Tampering | POST /episode-summary | mitigate | Owner-scope the query (Project.owner_id == current_user.id, str-coerced); 404 on non-owner — mirrors projects.py:123-141. Test asserts cross-owner 404. |
| T-69-02 | Information Disclosure | Project read schema | mitigate | Do NOT add episode_summary to schemas.Project (preserve Phase 67 D-04); endpoint returns only the owner's own status. |
| T-69-03 | Tampering (prompt injection via screenplay text) | summarize_episode prompt | accept | Low risk: owner summarizes own content for own later episodes; output is bounded prose, never executed, triggers no elevated action. Bounded max_tokens caps blast radius. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH.md: no Package Legitimacy Audit required). |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -q` green
- Full suite: `cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/ -q` green except the 5 documented pre-existing failures (test_mcp_foundation / test_session_isolation / test_yolo_integration)
</verification>

<success_criteria>
- ESUM-01 SC-1: completing an episode (the new endpoint) generates + stores episode_summary
- summarizer is bounded prose via chat_completion(json_mode=False, max_tokens~500)
- source text read strictly by episode_index (positional read fails the by_index test)
- endpoint owner-scoped; episode_summary text not leaked on the read schema
</success_criteria>

<output>
Create `.planning/phases/69-auto-episode-summary-lazy-regeneration/69-01-SUMMARY.md` when done
</output>
