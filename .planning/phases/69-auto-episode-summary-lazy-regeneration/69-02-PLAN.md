---
phase: 69-auto-episode-summary-lazy-regeneration
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - backend/app/utils/episode_summary.py
  - backend/app/api/endpoints/wizards.py
  - backend/app/tests/test_episode_summary.py
autonomous: true
requirements: [ESUM-03]
must_haves:
  truths:
    - "A stale-with-summary prior is regenerated before connected-mode generation reads it; its flag is cleared"
    - "Up-to-date priors are NOT regenerated or disturbed (SC-3)"
    - "Summary-less priors are NOT regenerated (existence-gate)"
    - "On regen failure the flag stays True, Phase 68's '(summary may be out of date)' marker is injected, and generation still proceeds"
    - "The pre-pass runs in run_wizard BEFORE build_bible_context, gated on connected mode"
  artifacts:
    - path: "backend/app/utils/episode_summary.py"
      provides: "async regenerate_stale_priors(db, show, project) lazy-regen pre-pass"
      contains: "async def regenerate_stale_priors"
    - path: "backend/app/api/endpoints/wizards.py"
      provides: "connected-mode pre-pass call before build_bible_context in run_wizard"
      contains: "regenerate_stale_priors"
  key_links:
    - from: "backend/app/api/endpoints/wizards.py"
      to: "episode_summary.regenerate_stale_priors"
      via: "await immediately before build_bible_context(db, project)"
      pattern: "await regenerate_stale_priors"
    - from: "backend/app/utils/episode_summary.py"
      to: "template_ai_service.summarize_episode"
      via: "per-prior regen inside try/except"
      pattern: "summarize_episode"
---

<objective>
Wire the lazy regeneration of stale prior summaries (ESUM-03) as an async pre-pass in
`wizards.py::run_wizard`, executed in connected mode IMMEDIATELY BEFORE the synchronous
`build_bible_context(db, project)` call (line ~134) so the reader sees fresh rows.

Add `async regenerate_stale_priors(db, show, project)` (new module
`backend/app/utils/episode_summary.py`) that mirrors Phase 68's prior-episode query shape,
regenerates ONLY priors that are `episode_summary_stale=True` AND already have a non-empty
summary (SC-3 + existence-gate), clears the flag on success, and on per-prior AI failure
leaves the flag True (so Phase 68 injects the stale text WITH its marker) — generation NEVER
fails. Commits after the loop.

Do NOT touch `build_bible_context` (keep it a pure sync reader). Do NOT remove Phase 68's
marker path. Do NOT wire review / ai_chat / MCP read sites (Phase 71+ scope).

Purpose: Close ESUM-03 — stale priors are refreshed just-in-time before they feed a later
connected episode.
Output: New regen helper + run_wizard pre-pass wiring + ESUM-03 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-auto-episode-summary-lazy-regeneration/69-RESEARCH.md
@.planning/phases/69-auto-episode-summary-lazy-regeneration/69-VALIDATION.md
@.planning/phases/68-mode-aware-generation-context-injection/68-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration required. -->

summarize_episode (from Plan 69-01, template_ai_service):
  async summarize_episode(self, db, project) -> str  # bounded prose; no commit; no write

Phase 68 prior-episode query to mirror — bible_context._build_prior_episodes_block
(backend/app/utils/bible_context.py:33-69):
  db.query(Project).filter(
      Project.show_id == str(show.id),
      Project.episode_number < project.episode_number,
      Project.episode_summary.isnot(None),
  ).order_by(Project.episode_number.asc()).all()
  then existence-gate: [p for p in priors if (p.episode_summary or "").strip()]
  STALE_SUMMARY_MARKER = " (summary may be out of date)"; injected when episode_summary_stale.
  build_bible_context only emits priors when show.continuity_mode == ContinuityMode.CONNECTED.value
  (VARCHAR string compare, NOT the enum object).

run_wizard hook site — wizards.py:116-164. Line ~134:
  bible_context = build_bible_context(db, project)
  The result is FROZEN into a string passed to the async background task (_run_wizard_background),
  so regen MUST run BEFORE this line (Pitfall 2). project is already owner-resolved above (line 124-129).
  show is NOT loaded in run_wizard yet — load it (db.query(Show).filter(Show.id == str(project.show_id)))
  or have regenerate_stale_priors load+gate internally.

ContinuityMode (app.models.schemas): ContinuityMode.CONNECTED.value == "connected".
Project columns: episode_summary (Text), episode_summary_stale (Boolean), episode_number (Integer, nullable), show_id.

Test scaffold (from Plan 69-01, test_episode_summary.py): _patch_chat_completion,
_insert_screenplay_content, _create_show, _create_project. Fixtures: client, db_session, mock_auth_headers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: regenerate_stale_priors async pre-pass helper (ESUM-03 core + SC-3)</name>
  <files>backend/app/utils/episode_summary.py, backend/app/tests/test_episode_summary.py</files>
  <read_first>
    - backend/app/utils/bible_context.py:33-69 (the EXACT prior-episode query shape + existence-gate to mirror; STALE_SUMMARY_MARKER)
    - backend/app/services/template_ai_service.py (summarize_episode from Plan 69-01; template_ai_service singleton)
    - backend/app/models/database.py:171-172 (episode_summary / episode_summary_stale)
    - backend/app/tests/test_episode_summary.py (Plan 69-01 scaffold: _patch_chat_completion, _create_show, _create_project)
    - .planning/phases/69-.../69-RESEARCH.md §Pattern 3 + Pitfalls 4 & 6
  </read_first>
  <behavior>
    - regenerates a stale prior that HAS a non-empty summary: rewrites episode_summary, sets episode_summary_stale=False
    - does NOT touch an up-to-date prior (episode_summary_stale=False) — its text is byte-identical after the call (SC-3)
    - does NOT regenerate a summary-less prior (episode_summary None/empty/whitespace) even if flagged stale (existence-gate)
    - only strictly-prior episodes of THIS show (episode_number < current, show_id == show.id) are considered; current/later untouched
    - when summarize_episode raises for one prior, that prior keeps episode_summary_stale=True and the loop continues (no exception escapes)
    - project.episode_number None -> no-op return; commits once after the loop
  </behavior>
  <action>
    Create backend/app/utils/episode_summary.py with async regenerate_stale_priors(db, show, project).
    Return early if project.episode_number is None. Query strictly-prior episodes mirroring
    _build_prior_episodes_block: Project.show_id == str(show.id), Project.episode_number <
    project.episode_number, Project.episode_summary.isnot(None), episode_summary_stale.is_(True),
    order_by episode_number.asc(). For each prior, skip if (episode_summary or "").strip() is
    falsy (existence-gate). Inside a per-prior try/except: fresh = await
    template_ai_service.summarize_episode(db, prior); if fresh truthy set prior.episode_summary =
    fresh and prior.episode_summary_stale = False. On Exception: logger.warning(...) and leave the
    flag True (do NOT re-raise). After the loop db.commit(). Use the template_ai_service singleton
    import. Add -k preserves_fresh, -k existence_gate, -k regen_failure (helper-level) tests using
    the Plan 69-01 scaffold + _patch_chat_completion (including a side_effect that raises).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -k "preserves_fresh or existence_gate or regen_failure" -q</automated>
  </verify>
  <acceptance_criteria>
    - regenerate_stale_priors regenerates a stale-with-summary prior, clears its flag, and does NOT touch an up-to-date prior or a summary-less prior
    - a stale prior with an empty/whitespace summary is not passed to summarize_episode (existence-gate)
    - when summarize_episode raises, the prior's episode_summary_stale stays True and no exception escapes regenerate_stale_priors
    - the helper commits once and only considers strictly-prior episodes of the same show
  </acceptance_criteria>
  <done>regenerate_stale_priors exists; preserves_fresh, existence_gate, and helper-level regen_failure tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the pre-pass into run_wizard + end-to-end regen/marker tests (ESUM-03)</name>
  <files>backend/app/api/endpoints/wizards.py, backend/app/tests/test_episode_summary.py</files>
  <read_first>
    - backend/app/api/endpoints/wizards.py:116-164 (run_wizard; line ~134 build_bible_context; the frozen-string handoff, Pitfall 2)
    - backend/app/utils/episode_summary.py (regenerate_stale_priors from Task 1)
    - backend/app/utils/bible_context.py:72-120 (build_bible_context connected gate + Prior Episodes block + marker)
    - backend/app/tests/test_episode_summary.py (scaffold helpers)
    - backend/app/tests/test_bible_injection.py (connected-mode build_bible_context assertions to mirror for the end-to-end check)
  </read_first>
  <behavior>
    - After the pre-pass runs in connected mode, build_bible_context for the later episode shows the FRESH prior text with NO '(summary may be out of date)' marker for regenerated priors (lazy_regen)
    - When the provider raises during the pre-pass, run_wizard still completes (returns the WizardRun, 200) and the stale prior is later injected by build_bible_context WITH the marker, flag remains True (regen_failure end-to-end)
    - The pre-pass is gated on connected mode: anthology/standalone/show_id-NULL projects skip it (no summarize_episode call)
    - The pre-pass executes BEFORE build_bible_context(db, project) in run_wizard (the frozen string sees fresh rows)
  </behavior>
  <action>
    In wizards.py::run_wizard, immediately BEFORE line ~134's bible_context =
    build_bible_context(db, project), add a connected-mode-gated pre-pass: load the show
    (db.query(Show).filter(Show.id == str(project.show_id)).first()) when project.show_id is set,
    and if show and show.continuity_mode == ContinuityMode.CONNECTED.value, await
    regenerate_stale_priors(db, show, project). Import regenerate_stale_priors from
    ...utils.episode_summary, Show + ContinuityMode as needed. Do NOT modify build_bible_context.
    Keep the existing build_bible_context call exactly where it is (now reading fresh rows). Add
    end-to-end tests: (lazy_regen) seed a connected show with a stale-with-summary prior + a later
    episode, drive the pre-pass + build_bible_context, assert the prior text is the mocked-fresh
    text and the marker is ABSENT and flag cleared; (regen_failure) _patch_chat_completion to raise,
    assert the pre-pass leaves the flag True, build_bible_context injects the stale text WITH the
    marker, and the regen path raised no exception (generation proceeds). Prefer testing run_wizard
    via the API client when feasible (mock the wizard generation/provider) or test the pre-pass+
    build_bible_context composition directly if full run_wizard plumbing is heavy — document which.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -k "lazy_regen or regen_failure" -q</automated>
  </verify>
  <acceptance_criteria>
    - the regenerate_stale_priors call sits in run_wizard BEFORE build_bible_context(db, project) and is gated on show.continuity_mode == ContinuityMode.CONNECTED.value
    - after the pre-pass, the regenerated prior appears in build_bible_context output with fresh text and WITHOUT the '(summary may be out of date)' marker, flag cleared
    - when chat_completion raises during regen, the stale prior is injected WITH the marker, episode_summary_stale stays True, and generation proceeds (no exception)
    - anthology/standalone/show_id-NULL paths do not invoke summarize_episode
    - build_bible_context signature unchanged (still sync; no await added)
  </acceptance_criteria>
  <done>The pre-pass is wired before build_bible_context, connected-gated; lazy_regen + regen_failure end-to-end tests pass; build_bible_context untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored prior screenplay text → AI prompt | a later episode's connected generation regenerates prior summaries from owner content |
| run_wizard → background generation | regen pre-pass mutates+commits prior rows before the frozen bible_context string is built |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | Information Disclosure / Tampering | regenerate_stale_priors query | mitigate | Scope strictly to Project.show_id == this project's show (same show, same owner) and episode_number < current — mirrors Phase 68 T-68-01. project is already owner-resolved in run_wizard before the pre-pass. |
| T-69-05 | Denial of Service (generation aborts on AI error) | per-prior regen loop | mitigate | Per-prior try/except; on failure leave the flag True and fall through to Phase 68's marker path. Test asserts generation proceeds + flag stays True. |
| T-69-06 | DoS (token blow-up on long seasons) | connected prompt size | accept | Bounded summary (~250 words, Plan 69-01) + Phase 68 PRIOR_EPISODE_CAP=8 already bound the prompt; concurrent double-regen accepted best-effort for the internal-tool MVP (RESEARCH Pitfall 5). |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=. ./venv/bin/python -m pytest app/tests/test_episode_summary.py -q` green (all 6 VALIDATION.md rows)
- Full suite green except the 5 documented pre-existing failures (test_mcp_foundation / test_session_isolation / test_yolo_integration)
- Manual grep gate: `grep -n "regenerate_stale_priors" backend/app/api/endpoints/wizards.py` shows the call ABOVE the `build_bible_context(db, project)` line; build_bible_context remains `def` (not `async def`)
</verification>

<success_criteria>
- ESUM-03 SC-2: a stale prior is regenerated before it feeds a later connected episode; flag cleared after success
- ESUM-03 SC-3: up-to-date priors are not regenerated/disturbed; summary-less priors skipped
- Regen failure degrades to Phase 68's marker path; generation never fails
- Pre-pass lives in run_wizard before build_bible_context, connected-gated; build_bible_context unchanged
- Review / ai_chat / MCP read sites NOT wired (Phase 71+ scope)
</success_criteria>

<output>
Create `.planning/phases/69-auto-episode-summary-lazy-regeneration/69-02-SUMMARY.md` when done
</output>
