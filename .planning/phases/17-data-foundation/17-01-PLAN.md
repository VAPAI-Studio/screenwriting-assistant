---
phase: 17-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/delta/002_shotlist_tables.sql
  - backend/migrations/init_db.sql
  - backend/app/models/database.py
  - backend/app/models/schemas.py
  - backend/app/tests/test_shotlist_models.py
  - docker-compose.yml
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-06]

must_haves:
  truths:
    - "Shot, ShotElement, and AssetMedia ORM models are importable and mapped to correct tables"
    - "Shot model has JSONB fields (script_range, fields) that survive ORM round-trip"
    - "scene_item_id FK uses ON DELETE SET NULL (not CASCADE) — shot survives scene deletion"
    - "Deleting a shot cascades to its shot_elements junction rows"
    - "AssetMedia has dual nullable FKs (element_id and shot_id) with ON DELETE SET NULL"
    - "Project model has shotlist_stale boolean defaulting to False"
    - "Pydantic schemas validate and serialize correctly with metadata_ alias"
    - "Delta migration is fully idempotent (IF NOT EXISTS on every statement)"
    - "Docker volume media_uploads is defined for persistent media storage"
  artifacts:
    - path: "backend/migrations/delta/002_shotlist_tables.sql"
      provides: "DDL for shots, shot_elements, asset_media tables + shotlist_stale ALTER"
      contains: "CREATE TABLE IF NOT EXISTS shots"
    - path: "backend/migrations/init_db.sql"
      provides: "Consolidated baseline schema with new tables appended"
      contains: "CREATE TABLE IF NOT EXISTS shots"
    - path: "backend/app/models/database.py"
      provides: "Shot, ShotElement, AssetMedia ORM models + Project updates"
      contains: "class Shot"
    - path: "backend/app/models/schemas.py"
      provides: "ShotCreate, ShotUpdate, ShotResponse, AssetMediaCreate, AssetMediaResponse Pydantic schemas"
      contains: "class ShotCreate"
    - path: "backend/app/tests/test_shotlist_models.py"
      provides: "Tests covering DATA-01 through DATA-06"
      min_lines: 150
    - path: "docker-compose.yml"
      provides: "media_uploads volume for persistent media file storage"
      contains: "media_uploads"
  key_links:
    - from: "backend/app/models/database.py (Shot)"
      to: "backend/app/models/database.py (Project)"
      via: "sa_relationship back_populates='shots'"
      pattern: "back_populates=\"shots\""
    - from: "backend/app/models/database.py (AssetMedia)"
      to: "backend/app/models/database.py (Project)"
      via: "sa_relationship back_populates='asset_media'"
      pattern: "back_populates=\"asset_media\""
    - from: "backend/app/models/database.py (Shot.scene_item_id)"
      to: "list_items table"
      via: "ForeignKey with ondelete=SET NULL"
      pattern: "ondelete=\"SET NULL\""
    - from: "backend/app/models/schemas.py (AssetMediaResponse)"
      to: "backend/app/models/database.py (AssetMedia.metadata_)"
      via: "validation_alias for metadata_ column alias"
      pattern: "validation_alias=\"metadata_\""
    - from: "backend/migrations/delta/002_shotlist_tables.sql"
      to: "backend/migrations/init_db.sql"
      via: "Same DDL in both files for fresh vs upgrade paths"
      pattern: "CREATE TABLE IF NOT EXISTS shots"
---

<objective>
Create the database foundation for v3.0 Shotlist and Production Breakdown: delta migration, SQLAlchemy models, Pydantic schemas, tests, and Docker volume for media storage.

Purpose: Phases 18-25 depend on these tables and models. This establishes the data contracts that all downstream shot CRUD, media upload, and staleness features build on.
Output: 6 files — migration SQL, updated init_db.sql, ORM models in database.py, Pydantic schemas in schemas.py, test file, updated docker-compose.yml.
</objective>

<execution_context>
@/Users/yvesfogel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yvesfogel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-data-foundation/17-CONTEXT.md
@.planning/phases/17-data-foundation/17-RESEARCH.md

@backend/app/models/database.py
@backend/app/models/schemas.py
@backend/migrations/delta/001_breakdown_tables.sql
@backend/migrations/init_db.sql
@backend/app/tests/test_breakdown_models.py
@backend/app/tests/conftest.py
@docker-compose.yml

<interfaces>
<!-- Existing patterns the executor must follow exactly -->

From backend/app/models/database.py (Project model, lines 87-106):
```python
class Project(Base):
    __tablename__ = "projects"
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    owner_id = Column(UUID(as_uuid=True), nullable=False, index=True)
    title = Column(String(255), nullable=False)
    # ... other columns ...
    breakdown_stale = Column(Boolean, default=False)

    sections = sa_relationship("Section", back_populates="project", cascade="all, delete-orphan")
    phase_data = sa_relationship("PhaseData", back_populates="project", cascade="all, delete-orphan")
    breakdown_elements = sa_relationship("BreakdownElement", back_populates="project",
                                          cascade="all, delete-orphan")
    breakdown_runs = sa_relationship("BreakdownRun", back_populates="project",
                                      cascade="all, delete-orphan")
```

From backend/app/models/database.py (BreakdownRun ends at line 532 — append new models after this):
```python
class BreakdownRun(Base):
    __tablename__ = "breakdown_runs"
    # ... last model in file
    project = sa_relationship("Project", back_populates="breakdown_runs")
```

From backend/app/models/database.py (imports, line 3):
```python
from sqlalchemy import Column, String, DateTime, ForeignKey, Enum, Text, JSON, func, Integer, Boolean, BigInteger, UniqueConstraint, Float
```

From backend/app/models/database.py (metadata_ alias pattern, used in AIMessage and Book):
```python
metadata_ = Column("metadata", JSON, default=dict)  # Avoids SQLAlchemy MetaData clash
```

From docker-compose.yml (volume pattern):
```yaml
volumes:
  - book_uploads:/app/uploads
# Top-level:
volumes:
  postgres_data:
  book_uploads:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delta migration, init_db.sql, ORM models, Docker volume</name>
  <files>
    backend/migrations/delta/002_shotlist_tables.sql,
    backend/migrations/init_db.sql,
    backend/app/models/database.py,
    docker-compose.yml
  </files>
  <action>
**1. Create `backend/migrations/delta/002_shotlist_tables.sql`:**

Follow the exact pattern of `001_breakdown_tables.sql`. Include header comment describing the migration.

Create 3 tables with `CREATE TABLE IF NOT EXISTS`:

**shots table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE`
- `scene_item_id UUID REFERENCES list_items(id) ON DELETE SET NULL` (nullable, SET NULL per locked decision)
- `shot_number INTEGER NOT NULL DEFAULT 1`
- `script_text TEXT DEFAULT ''`
- `script_range JSONB DEFAULT '{}'`
- `fields JSONB DEFAULT '{}'`
- `sort_order INTEGER DEFAULT 0`
- `source VARCHAR(20) DEFAULT 'user'`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`

Indexes: `idx_shots_project(project_id)`, `idx_shots_scene(scene_item_id)`, `idx_shots_project_sort(project_id, sort_order)`

**shot_elements table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `shot_id UUID NOT NULL REFERENCES shots(id) ON DELETE CASCADE`
- `element_id UUID NOT NULL REFERENCES breakdown_elements(id) ON DELETE CASCADE`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `CONSTRAINT uq_shot_element UNIQUE (shot_id, element_id)`

Indexes: `idx_shot_elements_shot(shot_id)`, `idx_shot_elements_element(element_id)`

**asset_media table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE`
- `element_id UUID REFERENCES breakdown_elements(id) ON DELETE SET NULL` (nullable)
- `shot_id UUID REFERENCES shots(id) ON DELETE SET NULL` (nullable)
- `file_type VARCHAR(20) NOT NULL`
- `file_path VARCHAR(1000) NOT NULL`
- `thumbnail_path VARCHAR(1000)`
- `original_filename VARCHAR(500) NOT NULL`
- `file_size_bytes BIGINT NOT NULL DEFAULT 0`
- `metadata JSONB DEFAULT '{}'`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`

Indexes: `idx_asset_media_project(project_id)`, `idx_asset_media_element(element_id)`, `idx_asset_media_shot(shot_id)`

**ALTER statement:**
`ALTER TABLE projects ADD COLUMN IF NOT EXISTS shotlist_stale BOOLEAN DEFAULT FALSE;`

**Triggers:** Add `updated_at` triggers for `shots` and `asset_media` tables (same `update_updated_at_column()` function used by all other tables). `shot_elements` does NOT need a trigger (no updated_at column).

```sql
CREATE OR REPLACE TRIGGER update_shots_updated_at
    BEFORE UPDATE ON shots FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE OR REPLACE TRIGGER update_asset_media_updated_at
    BEFORE UPDATE ON asset_media FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

**2. Update `backend/migrations/init_db.sql`:**

Append the same DDL (shots, shot_elements, asset_media tables) BEFORE the schema_migrations tracking section (before line ~403 "Schema migration tracking"). Insert after the existing breakdown indexes (after line 401 `idx_breakdown_runs_project`).

Specifically:
- Add `CREATE TABLE IF NOT EXISTS shots (...)` block with its indexes
- Add `CREATE TABLE IF NOT EXISTS shot_elements (...)` block with its indexes
- Add `CREATE TABLE IF NOT EXISTS asset_media (...)` block with its indexes
- Add `ALTER TABLE projects ADD COLUMN IF NOT EXISTS shotlist_stale BOOLEAN DEFAULT FALSE;`
- Add new indexes to the indexes section
- Add triggers for shots and asset_media to the triggers section at end of file

**3. Update `backend/app/models/database.py`:**

Append 3 new model classes AFTER the `BreakdownRun` class (after line 532). Add a section comment:

```python
# ============================================================
# Shotlist models (v3.0 -- Phase 17 Data Foundation)
# ============================================================
```

**Shot model** — follow the exact patterns from RESEARCH.md code examples:
- UUID PK with `default=uuid.uuid4`
- `project_id` FK to projects (NOT NULL, index=True)
- `scene_item_id` FK to list_items with `ondelete="SET NULL"` (nullable=True, index=True)
- `shot_number` Integer NOT NULL default=1
- `script_text` Text default=""
- `script_range` JSON default=dict
- `fields` JSON default=dict
- `sort_order` Integer default=0
- `source` String(20) default="user"
- `created_at` with server_default=func.now()
- `updated_at` with onupdate=func.now()
- Relationships: `project` (back_populates="shots"), `shot_elements` (back_populates="shot", cascade="all, delete-orphan"), `media` (back_populates="shot", cascade="all, delete-orphan")

**ShotElement model:**
- UUID PK
- `shot_id` FK to shots with `ondelete="CASCADE"` (NOT NULL, index=True)
- `element_id` FK to breakdown_elements with `ondelete="CASCADE"` (NOT NULL, index=True)
- `created_at` with server_default=func.now()
- Relationships: `shot` (back_populates="shot_elements"), `element` (to BreakdownElement, no back_populates)
- `__table_args__` with UniqueConstraint('shot_id', 'element_id', name='uq_shot_element')

**AssetMedia model:**
- UUID PK
- `project_id` FK to projects (NOT NULL, index=True)
- `element_id` FK to breakdown_elements with `ondelete="SET NULL"` (nullable=True, index=True)
- `shot_id` FK to shots with `ondelete="SET NULL"` (nullable=True, index=True)
- `file_type` String(20) NOT NULL
- `file_path` String(1000) NOT NULL
- `thumbnail_path` String(1000) nullable
- `original_filename` String(500) NOT NULL
- `file_size_bytes` BigInteger NOT NULL default=0
- `metadata_` = Column("metadata", JSON, default=dict) — use the metadata_ alias pattern
- `created_at` / `updated_at`
- Relationships: `project` (back_populates="asset_media"), `element` (to BreakdownElement, no back_populates), `shot` (back_populates="media")

**Update Project model** (around line 99-106):
- Add `shotlist_stale = Column(Boolean, default=False)` after `breakdown_stale`
- Add `shots = sa_relationship("Shot", back_populates="project", cascade="all, delete-orphan")` after breakdown_runs relationship
- Add `asset_media = sa_relationship("AssetMedia", back_populates="project", cascade="all, delete-orphan")` after shots relationship

**4. Update `docker-compose.yml`:**

Add media volume to backend service volumes section:
```yaml
- media_uploads:/app/media
```

Add to top-level volumes:
```yaml
media_uploads:
```
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -c "from app.models.database import Shot, ShotElement, AssetMedia, Base, Project; assert 'shots' in Base.metadata.tables; assert 'shot_elements' in Base.metadata.tables; assert 'asset_media' in Base.metadata.tables; assert hasattr(Project, 'shotlist_stale'); assert hasattr(Project, 'shots'); assert hasattr(Project, 'asset_media'); print('All models importable and Project updated')"</automated>
  </verify>
  <done>
    - 002_shotlist_tables.sql exists with idempotent DDL for 3 tables + ALTER + triggers
    - init_db.sql updated with same DDL for fresh installs
    - Shot, ShotElement, AssetMedia classes exist in database.py with correct FKs and relationships
    - Project model has shotlist_stale column and shots/asset_media relationships
    - docker-compose.yml has media_uploads volume
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic schemas and test suite</name>
  <files>
    backend/app/models/schemas.py,
    backend/app/tests/test_shotlist_models.py
  </files>
  <behavior>
    - test_shot_importable: Shot.__tablename__ == "shots"
    - test_shot_element_importable: ShotElement.__tablename__ == "shot_elements"
    - test_asset_media_importable: AssetMedia.__tablename__ == "asset_media"
    - test_tables_in_metadata: "shots", "shot_elements", "asset_media" all in Base.metadata.tables
    - test_project_shotlist_stale: New Project has shotlist_stale == False
    - test_shot_orm_roundtrip: Create Shot with fields JSONB and script_range JSONB, verify all fields survive commit+refresh
    - test_shot_scene_set_null: Delete a ListItem that a Shot references via scene_item_id, verify shot.scene_item_id becomes None (not deleted)
    - test_shot_cascade_delete: Delete a Shot, verify its ShotElement junction rows are also deleted
    - test_shot_element_unique: Two ShotElements with same (shot_id, element_id) raise IntegrityError
    - test_asset_media_orm_roundtrip: Create AssetMedia with metadata_ alias, verify fields survive round-trip
    - test_asset_media_dual_fk: AssetMedia can be created with element_id only, shot_id only, or both
    - test_project_cascade_to_shots: Delete Project, verify its Shots are deleted
    - test_shot_create_valid: ShotCreate accepts valid data with source="user"
    - test_shot_create_invalid_source: ShotCreate rejects source not matching "user" or "ai"
    - test_shot_update_partial: ShotUpdate accepts partial fields (only shot_number, only fields)
    - test_shot_response_from_orm: ShotResponse.model_validate(orm_shot) produces correct fields
    - test_asset_media_response_metadata_alias: AssetMediaResponse.model_validate(orm_media) correctly maps metadata_ to metadata
    - test_script_range_schema: ScriptRange validates {scene_index, start_offset, end_offset, content_hash}
  </behavior>
  <action>
**1. Append Pydantic schemas to `backend/app/models/schemas.py`:**

Add a section comment before the new schemas:

```python
# ============================================================
# Shotlist Schemas (v3.0 -- Phase 17 Data Foundation)
# ============================================================
```

Add these schemas (follow existing BreakdownElementCreate/Response patterns):

**ScriptRange** — nested model for shot script_range JSONB shape:
- `scene_index: int = 0`
- `start_offset: int = 0`
- `end_offset: int = 0`
- `content_hash: str = ""`

**ShotCreate:**
- `scene_item_id: Optional[UUID] = None`
- `shot_number: int = Field(default=1, ge=1)`
- `script_text: str = ""`
- `script_range: Optional[Dict] = Field(default_factory=dict)`
- `fields: Dict = Field(default_factory=dict)`
- `sort_order: Optional[int] = None`
- `source: str = Field(default="user", pattern="^(user|ai)$")`

**ShotUpdate:**
- All fields optional (partial update pattern, matching BreakdownElementUpdate)
- `scene_item_id: Optional[UUID] = None`
- `shot_number: Optional[int] = Field(None, ge=1)`
- `script_text: Optional[str] = None`
- `script_range: Optional[Dict] = None`
- `fields: Optional[Dict] = None`
- `sort_order: Optional[int] = None`

**ShotResponse:**
- All Shot fields with `model_config = ConfigDict(from_attributes=True)`
- `id: UUID`, `project_id: UUID`, `scene_item_id: Optional[UUID] = None`
- `shot_number: int`, `script_text: str = ""`, `script_range: Dict`, `fields: Dict`
- `sort_order: int = 0`, `source: str = "user"`
- `created_at: datetime`, `updated_at: Optional[datetime] = None`

**ShotElementCreate:**
- `element_id: UUID`

**ShotElementResponse:**
- `id: UUID`, `shot_id: UUID`, `element_id: UUID`, `created_at: datetime`
- `model_config = ConfigDict(from_attributes=True)`

**AssetMediaCreate:**
- `element_id: Optional[UUID] = None`
- `shot_id: Optional[UUID] = None`
- `file_type: str = Field(..., pattern="^(image|audio)$")`
- `original_filename: str = Field(..., min_length=1, max_length=500)`
- `file_size_bytes: int = Field(..., ge=0)`

**AssetMediaResponse:**
- All AssetMedia fields, with `metadata: Dict = Field(default_factory=dict, validation_alias="metadata_")` for the column alias
- `model_config = ConfigDict(from_attributes=True)`

Ensure necessary imports are present at top of schemas.py: `Dict` from typing, `Field` from pydantic (both likely already imported — verify and add if missing).

**2. Create `backend/app/tests/test_shotlist_models.py`:**

Follow the exact structure of `test_breakdown_models.py`. Use the same test patterns: importability tests, metadata tests, ORM round-trip with db_session fixture, cascade/constraint tests, and Pydantic schema validation tests.

Write ALL tests listed in the `<behavior>` section. Each test should be a standalone function starting with `test_`. Import models from `app.models.database` and schemas from `app.models.schemas`.

Key patterns from the existing test file:
- Use `db_session` fixture (from conftest.py) for ORM tests
- Create Project first, then child objects
- Use `db_session.flush()` + `db_session.refresh()` for checking defaults
- Use `db_session.commit()` + `db_session.refresh()` for full round-trips
- Use `pytest.raises(IntegrityError)` for constraint tests, followed by `db_session.rollback()`
- For SET NULL test: create PhaseData + ListItem + Shot referencing the ListItem, delete the ListItem, commit, refresh the Shot, assert scene_item_id is None
- For cascade test: create Shot + ShotElement, delete Shot, verify ShotElement is gone
- Schema tests import from `app.models.schemas` and use `model_validate()` for ORM-to-schema conversion

**3. Run the RED phase first** — create test file, verify tests fail (models exist from Task 1 but schemas might have issues). Then fix any schema issues until all tests pass GREEN.
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/test_shotlist_models.py -x -v</automated>
  </verify>
  <done>
    - All 18+ tests in test_shotlist_models.py pass
    - Pydantic schemas (ShotCreate, ShotUpdate, ShotResponse, ShotElementCreate, ShotElementResponse, AssetMediaCreate, AssetMediaResponse, ScriptRange) exist and validate correctly
    - ShotResponse.model_validate(orm_shot) works with from_attributes=True
    - AssetMediaResponse correctly maps metadata_ alias
    - Full existing test suite still passes: `python -m pytest app/tests/ -x`
  </done>
</task>

</tasks>

<verification>
1. `cd backend && python -m pytest app/tests/test_shotlist_models.py -x -v` — all new tests pass
2. `cd backend && python -m pytest app/tests/ -x` — existing test suite unbroken
3. `python -c "from app.models.database import Shot, ShotElement, AssetMedia"` — models importable
4. `grep -c "IF NOT EXISTS" backend/migrations/delta/002_shotlist_tables.sql` — confirms idempotency (should be >= 10)
5. `grep "media_uploads" docker-compose.yml` — volume exists
6. `grep "shotlist_stale" backend/app/models/database.py` — column on Project model
</verification>

<success_criteria>
- shots, shot_elements, asset_media tables defined in both delta migration and init_db.sql
- Shot ORM model has JSONB fields, SET NULL FK to list_items, cascade to shot_elements
- AssetMedia ORM model has dual nullable FKs with SET NULL, metadata_ alias
- Project model has shotlist_stale column and relationships to Shot and AssetMedia
- All Pydantic schemas pass validation with correct field types
- Docker volume media_uploads defined for persistent media storage
- All new tests pass, all existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/17-data-foundation/17-01-SUMMARY.md`
</output>
