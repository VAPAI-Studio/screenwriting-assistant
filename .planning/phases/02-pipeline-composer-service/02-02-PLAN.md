---
phase: 02-pipeline-composer-service
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/tests/test_pipeline_composer.py
  - backend/app/services/pipeline_composer.py
autonomous: true
requirements: [COMP-03]

must_haves:
  truths:
    - "Running compose_pipeline twice with identical agent descriptions produces identical output (cache hit, no second AI call)"
    - "A cosmetic agent edit (name, color, icon) does NOT trigger re-composition -- is_semantic_change returns False"
    - "A semantic edit (system_prompt_template, description, agent_type) DOES invalidate the cache key"
  artifacts:
    - path: "backend/app/services/pipeline_composer.py"
      provides: "Hash-based cache with SHA-256, is_semantic_change() helper, SEMANTIC_FIELDS constant"
      contains: "SEMANTIC_FIELDS"
    - path: "backend/app/tests/test_pipeline_composer.py"
      provides: "COMP-03 unit tests for cache determinism and semantic change detection"
      min_lines: 120
  key_links:
    - from: "backend/app/services/pipeline_composer.py"
      to: "backend/app/services/pipeline_composer.py"
      via: "_compute_cache_key() check at top of compose_pipeline(), skip AI call on hit"
      pattern: "_cache\\.get\\(|self\\._cache\\["
---

<objective>
Add hash-based cache and semantic change detection to the pipeline composer, ensuring deterministic re-composition behavior.

Purpose: COMP-03 requires that re-composition only triggers when semantic fields change (system_prompt_template, description, agent_type), not cosmetic fields (name, icon, color). The hash-based cache keyed on semantic fields provides both determinism (SC2) and cost efficiency (no redundant AI calls).

Output: 3 new COMP-03 tests + cache/semantic-change logic in pipeline_composer.py
</objective>

<execution_context>
@/Users/yvesfogel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yvesfogel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline-composer-service/02-CONTEXT.md
@.planning/phases/02-pipeline-composer-service/02-RESEARCH.md
@.planning/phases/02-pipeline-composer-service/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: pipeline_composer.py exports -->

From backend/app/services/pipeline_composer.py (created in Plan 01):
```python
class PipelineComposer:
    _cache: Dict[str, List[Dict]]  # hash -> parsed AI result

    async def compose_pipeline(self, owner_id: UUID, db: Session) -> List[AgentPipelineMap]: ...
    def _compute_cache_key(self, agents: List[Agent]) -> str: ...
    def is_semantic_change(self, update_fields: Dict) -> bool: ...
    # ... other internal methods

SEMANTIC_FIELDS = {"system_prompt_template", "description", "agent_type"}

pipeline_composer = PipelineComposer()
```

From backend/app/models/database.py:
```python
class Agent(Base):
    # Semantic fields (trigger recomposition):
    system_prompt_template = Column(Text, nullable=False)
    description = Column(Text)
    agent_type = Column(Enum(AgentType, ...), default=AgentType.BOOK_BASED)
    # Cosmetic fields (do NOT trigger recomposition):
    name = Column(String(255), nullable=False)
    color = Column(String(7), default="#6366f1")
    icon = Column(String(50), default="book")
```

From backend/app/tests/conftest.py:
```python
@pytest.fixture(scope="function")
def db_session(test_engine): ...  # SQLite in-memory, rollback after test
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write COMP-03 cache and semantic-change tests (RED)</name>
  <files>backend/app/tests/test_pipeline_composer.py</files>
  <behavior>
    - test_cache_hit_deterministic: Two calls to compose_pipeline with identical agents hit cache on second call -- chat_completion called exactly once, both calls return same mappings
    - test_cosmetic_change_no_recompose: is_semantic_change({name, color, icon}) returns False; _compute_cache_key produces same hash when only cosmetic fields differ
    - test_semantic_change_invalidates_cache: is_semantic_change({description}) returns True; _compute_cache_key produces different hash when system_prompt_template or description changes
  </behavior>
  <action>
Append 3 new tests to the existing `backend/app/tests/test_pipeline_composer.py` file (which already has 4 COMP-01 tests from Plan 01).

1. `test_cache_hit_deterministic(db_session)`:
   - Create 2 Agent instances in db_session
   - Mock `app.services.ai_provider.chat_completion` to return valid mappings JSON
   - Create a fresh `PipelineComposer()` instance (to start with empty cache)
   - Call `compose_pipeline(owner_id, db_session)` -- first call
   - Call `compose_pipeline(owner_id, db_session)` -- second call with same agents
   - Assert: `mock_chat_completion.call_count == 1` (cache hit on second call)
   - Assert: both calls return mappings with the same agent_ids, phases, and subsection_keys

2. `test_cosmetic_change_no_recompose()`:
   - Create a `PipelineComposer()` instance
   - Assert: `composer.is_semantic_change({"name": "New Name"})` returns `False`
   - Assert: `composer.is_semantic_change({"color": "#ff0000"})` returns `False`
   - Assert: `composer.is_semantic_change({"icon": "star"})` returns `False`
   - Assert: `composer.is_semantic_change({"name": "X", "color": "#Y", "icon": "Z"})` returns `False`
   - Also test cache key stability: create 2 Agent objects with identical semantic fields but different names. Assert `_compute_cache_key([agent_a]) == _compute_cache_key([agent_a_renamed])` -- wait, cache key includes agent.id so this won't work directly. Instead: the key point is that `is_semantic_change` correctly identifies cosmetic-only updates.

3. `test_semantic_change_invalidates_cache(db_session)`:
   - Create a `PipelineComposer()` instance
   - Assert: `composer.is_semantic_change({"description": "new desc"})` returns `True`
   - Assert: `composer.is_semantic_change({"system_prompt_template": "new prompt"})` returns `True`
   - Assert: `composer.is_semantic_change({"agent_type": "orchestrator"})` returns `True`
   - Assert: `composer.is_semantic_change({"name": "X", "description": "Y"})` returns `True` (mixed cosmetic+semantic = semantic)
   - Also verify cache key changes: create 2 Agent instances, compute cache key. Then change one agent's `system_prompt_template` in-place, recompute cache key. Assert the two keys differ.

Run tests to confirm RED state (these tests should FAIL because the cache/semantic logic may not be fully wired or the test expectations differ from current implementation):

Actually, if Plan 01 already implemented `_compute_cache_key` and `is_semantic_change` as specified, these tests might PASS immediately. That is acceptable -- it means Plan 01 did its job well. The TDD value here is in HAVING the tests, not necessarily in them failing first. If they pass immediately, skip to verification.

If tests pass immediately: commit as `test(02-02): add COMP-03 cache and semantic-change tests` and proceed.
If tests fail: that IS the RED state. Fix in Task 2.

Commit: `test(02-02): add COMP-03 cache and semantic-change tests`
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/test_pipeline_composer.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>3 COMP-03 tests exist in test_pipeline_composer.py. Tests either fail (RED, proceed to Task 2) or pass (GREEN, Plan 01 already implemented correctly -- skip to verification).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix cache and semantic-change logic if needed (GREEN)</name>
  <files>backend/app/services/pipeline_composer.py</files>
  <behavior>
    - _compute_cache_key sorts agents by ID, hashes semantic fields with SHA-256
    - compose_pipeline checks cache before AI call, stores result after AI call
    - is_semantic_change correctly distinguishes semantic from cosmetic fields
  </behavior>
  <action>
**If all 3 COMP-03 tests from Task 1 already pass:** This task is a no-op. The cache and semantic logic from Plan 01 is correct. Run full suite to verify, then skip to verification.

**If any COMP-03 tests fail:** Fix the specific issue in `backend/app/services/pipeline_composer.py`:

Possible fixes:
1. **Cache not checked before AI call:** Ensure `compose_pipeline()` checks `self._cache.get(cache_key)` before calling `_compose_batched()`. If cache hit, use cached result for DB write (skip AI call entirely).
2. **Cache not stored after AI call:** Ensure result is stored as `self._cache[cache_key] = ai_results` after successful composition.
3. **is_semantic_change missing or wrong:** Ensure `SEMANTIC_FIELDS = {"system_prompt_template", "description", "agent_type"}` is defined and `is_semantic_change` checks intersection correctly.
4. **_compute_cache_key wrong:** Ensure it sorts by `str(a.id)`, includes `system_prompt_template`, `description or ""`, and `agent_type.value`, joined with `"|"`, hashed with `hashlib.sha256`.

After fixes, run tests:
`cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/test_pipeline_composer.py -x -v`

Then run full suite:
`cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/ -x`

Commit (only if changes made): `feat(02-02): fix cache and semantic-change detection for COMP-03`
  </action>
  <verify>
    <automated>cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/test_pipeline_composer.py -x -v && python -m pytest app/tests/ -x</automated>
  </verify>
  <done>All 7 tests pass (4 COMP-01 + 3 COMP-03). Full test suite passes with zero regressions. Cache prevents redundant AI calls. is_semantic_change correctly gates on semantic fields only.</done>
</task>

</tasks>

<verification>
1. `cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/test_pipeline_composer.py -x -v` -- all 7 tests pass (4 COMP-01 + 3 COMP-03)
2. `cd /Users/yvesfogel/Desktop/screenwriting-assistant/backend && python -m pytest app/tests/ -x` -- full suite passes with zero regressions
3. Cache determinism: second call to compose_pipeline with same agents does NOT call chat_completion
4. Semantic gating: is_semantic_change returns False for {name, color, icon}, True for {description, system_prompt_template, agent_type}
</verification>

<success_criteria>
- 3 COMP-03 tests pass: cache hit determinism, cosmetic no-recompose, semantic invalidation
- compose_pipeline called twice with identical agents makes exactly 1 AI call (cache hit on second)
- is_semantic_change({name: ...}) returns False; is_semantic_change({description: ...}) returns True
- Full test suite (all existing + new tests) passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-composer-service/02-02-SUMMARY.md`
</output>
