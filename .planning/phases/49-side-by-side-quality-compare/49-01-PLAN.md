---
phase: 49-side-by-side-quality-compare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/template_ai_service.py
  - backend/app/api/endpoints/wizards.py
  - backend/app/models/schemas.py
  - backend/app/tests/test_scene_compare.py
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "A single scene can be regenerated by episode_index through the IMPROVED generation path (continuity + voice + craft from phases 45-48), per D-49-01"
    - "Regenerate returns the new {title,content,episode_index} as a PREVIEW and writes nothing to the store, per D-49-02"
    - "Keep-new persists the chosen scene into screenplays[episode_index] in screenplay_editor PhaseData.content AND the matching ScreenplayContent row, per D-49-02/D-49-03"
    - "Keep-new marks breakdown + shotlist stale; preview and keep-current do not, per D-49-02"
    - "The global running synopsis is left untouched on a single-scene keep, per D-49-05 (open-question default)"
    - "All prior contracts and the phases 45-48 generation behavior are preserved; the 24 prior script suites + 3 wizard tests stay green, per D-49-05"
    - "Both new endpoints are auth-scoped to the project owner (404 on non-owned), mirroring run_wizard"
  artifacts:
    - path: "backend/app/services/template_ai_service.py"
      provides: "_generate_one_scene shared helper + regenerate_single_scene public method"
      contains: "_generate_one_scene"
    - path: "backend/app/api/endpoints/wizards.py"
      provides: "POST /regenerate-scene (preview) + POST /keep-scene-version (persist)"
      contains: "regenerate-scene"
    - path: "backend/app/models/schemas.py"
      provides: "RegenerateSceneRequest/Response + KeepSceneVersionRequest"
      contains: "RegenerateSceneRequest"
    - path: "backend/app/tests/test_scene_compare.py"
      provides: "regenerate-improved-prompt + keep-persist + no-persist + owner-auth tests"
      min_lines: 80
  key_links:
    - from: "regenerate_single_scene"
      to: "_generate_one_scene"
      via: "single-episode call with supplied synopsis/prev_scene_text"
      pattern: "_generate_one_scene"
    - from: "_generate_scripts"
      to: "_generate_one_scene"
      via: "per-iteration delegation in the existing loop"
      pattern: "_generate_one_scene"
    - from: "keep-scene-version endpoint"
      to: "flag_modified(phase_data, \"content\")"
      via: "JSONB write of the single updated screenplays[i] slot"
      pattern: "flag_modified"
---

<objective>
Add a backend single-scene regenerate-and-keep capability that reuses the improved phases 45-48 generation path. This is the backend prerequisite slice of EVAL-01 (the user-facing capability ships in 49-02).

Purpose: Let a user regenerate ONE scene by episode_index with the improved continuity/voice/craft prompt, preview the result without clobbering the stored version, and explicitly persist the chosen version.
Output: A shared `_generate_one_scene` helper, a public `regenerate_single_scene` method, two owner-scoped endpoints (preview + keep), request/response schemas, and a backend test suite — with all 27 prior tests staying green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-side-by-side-quality-compare/49-CONTEXT.md
@.planning/phases/49-side-by-side-quality-compare/49-PATTERNS.md

@backend/app/services/template_ai_service.py
@backend/app/api/endpoints/wizards.py
@backend/app/api/endpoints/phase_data.py
@backend/app/models/schemas.py
@backend/app/tests/test_continuity_generation.py

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. Use directly — no exploration needed. -->

The per-scene prompt body to reuse (template_ai_service.py:331-471) builds, in order:
- continuity_block (334-345): present only when (synopsis or prev_scene_text); first/single scene → ""
- character_block (347-358): present only when character_section truthy; anchor "distinct, consistent voice"
- the UNCONDITIONAL "## Screenwriting Craft" block (391-397): anchors "## Screenwriting Craft", "on-the-nose", "subtext", "economical", "show, don't tell", "no internal or unfilmable description", "white space"
- "## YOUR TASK: Write scene {i+1} of {len}" (SCENE_MARKER "YOUR TASK: Write scene")
- native TITLE contract (408-411): first line "TITLE: <title>"

LLM call (415-423): chat_completion(messages=[system, {role:user, content:prompt}], temperature=0.7, max_tokens=4000, json_mode=False). System content is the exact "expert screenwriter ... native plain text only" string at line 417.

Native parse (429-461): strip a stray leading/trailing ``` fence; split a leading case-insensitive "title:" line; empty title → fall back to summary. Returns {"title","content","episode_index": i}. The [Generation failed: ...] branch (472-479) returns {episode_index,title:summary,content:"[Generation failed: ...]",error}.

run_wizard owner-auth header (wizards.py:116-129): Depends(get_current_user) + Depends(get_db); query Project filtered by id==request.project_id AND owner_id==current_user.id; 404 "Project not found" if missing.

Keep persistence analog (wizards.py:256-285): query PhaseData(project_id, phase, subsection_key="screenplay_editor"); content=dict(phase_data.content or {}); mutate; assign back; flag_modified(phase_data,"content"); create/update ScreenplayContent(project_id, content, formatted_content); _mark_breakdown_stale(db, project.id); _mark_shotlist_stale(db, project.id); db.commit().

Helpers available in wizards.py: _get_project_context(db, project, bible_context=None) (22-43), _get_character_data(db, project_id) (46-58), build_bible_context(db, project) (imported line 15), _mark_breakdown_stale/_mark_shotlist_stale (imported line 16).

ScreenplayContent model (database.py:286-297): id, project_id, list_item_id (nullable, unused), content (Text), formatted_content (JSON — holds the full {title,content,episode_index}), version (Int default 1), created_at, updated_at. No back-relationship. NO migration, NO new column (D-49-03).

Test mock pattern (test_continuity_generation.py): patch("app.services.template_ai_service.chat_completion", new_callable=AsyncMock, side_effect=mock). The _MockChat routes scene calls by SCENE_MARKER "YOUR TASK: Write scene"; _scene_writer returns "TITLE: {title}\n\n{content}". template_ai_service is a module-level singleton.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _generate_one_scene helper and add regenerate_single_scene</name>
  <files>backend/app/services/template_ai_service.py, backend/app/tests/test_scene_compare.py</files>
  <read_first>
    backend/app/services/template_ai_service.py:305-481 (the full _generate_scripts loop — the prompt body, LLM call, native parse, success-only advance, failure branch)
    backend/app/tests/test_continuity_generation.py:1-120 (the _MockChat + _scene_writer mocking pattern, SCENE_MARKER routing)
  </read_first>
  <behavior>
    - regenerate_single_scene(config, project_context, episode_index, synopsis, prev_scene_text) returns ONE {title, content, episode_index} dict (episode_index == the passed index).
    - For a NON-FIRST scene (prev_scene_text/synopsis supplied), the scene prompt contains ALL improved-path anchors: "Story so far" / "Previous scene" (continuity), "distinct, consistent voice" (voice, when characters present), "## Screenwriting Craft" + "on-the-nose" (craft, unconditional), and "TITLE:" (native).
    - For a FIRST scene (episode_index 0, empty synopsis + empty prev_scene_text), the prompt has NO continuity block ("Story so far" / "Previous scene" absent) — matching _generate_scripts lines 343-345.
    - The scene call runs with json_mode=False (recorded by the mock).
    - On chat_completion raising, returns {episode_index, title (=summary fallback), content "[Generation failed: ...]", error} — same shape as the existing failure branch.
    - _generate_scripts STILL returns {screenplays:[{title,content,episode_index}], synopsis} byte-identically: the existing 24 script suite tests pass unchanged.
  </behavior>
  <action>
    Refactor the per-scene loop interior of `_generate_scripts` (lines 331-471) into a private helper `async def _generate_one_scene(self, ep, i, total, project_context, character_section, scene_outline, runtime_target, guidance, synopsis, prev_scene_text) -> dict` that returns the `{title, content, episode_index}` dict (and the failure-branch dict on exception). Move the prompt construction (continuity_block, character_block, the unconditional "## Screenwriting Craft" literal, the YOUR TASK section, the native TITLE contract), the `chat_completion` call (temperature=0.7, max_tokens=4000, json_mode=False, exact system string), the fence-tolerant native parse, the TITLE split, and the empty-title→summary fallback into this helper VERBATIM — do not alter any anchor substrings, the SCENE_MARKER, the system prompt, or the return shape.

    Rewrite `_generate_scripts` to: build `character_section` and `scene_outline` once (as today), keep `synopsis`/`prev_scene_text` continuity state and the success-only advance (call `_update_synopsis` only after a non-error scene, exactly as lines 468-471), and inside the loop call `await self._generate_one_scene(ep, i, len(episodes), project_context, character_section, scene_outline, runtime_target, guidance, synopsis, prev_scene_text)`. Append the returned dict; advance continuity only when the dict has no "error" key. Preserve the `if not episodes: return {"screenplays": [], "error": ...}` guard and the final `return {"screenplays": screenplays, "synopsis": synopsis}` exactly. This delegation MUST be behavior-identical so the phases 45-48 anchor assertions still hold (D-49-05).

    Add a public `async def regenerate_single_scene(self, config, project_context, episode_index, synopsis, prev_scene_text) -> dict`: derive `episodes = config.get("episodes", [])`, validate `0 <= episode_index < len(episodes)` (raise ValueError otherwise), build `character_section` and `scene_outline` the same way `_generate_scripts` does (so the regenerate prompt is identical to the batch prompt for that index), then `return await self._generate_one_scene(ep=episodes[episode_index], i=episode_index, total=len(episodes), project_context=project_context, character_section=character_section, scene_outline=scene_outline, runtime_target=config.get("runtime_target",""), guidance=config.get("custom_guidance",""), synopsis=synopsis, prev_scene_text=prev_scene_text)`. Do NOT call `_update_synopsis` here (D-49-05: leave synopsis untouched on single-scene regenerate).

    Create `backend/app/tests/test_scene_compare.py` reusing the `_MockChat`/`_scene_writer` mocking pattern from test_continuity_generation.py (patch `app.services.template_ai_service.chat_completion`, new_callable=AsyncMock, side_effect=mock). Tests must pass in ISOLATION (per .planning/v6.0-PREEXISTING-TEST-CONCERN.md — no reliance on suite ordering). Cover, calling `template_ai_service.regenerate_single_scene` via `asyncio.run`:
    - regenerate returns ONE dict with episode_index == the requested index and a title+content parsed from the native "TITLE: ...\n\n..." output.
    - for a NON-FIRST scene (episode_index 1, synopsis="PRIOR SYNOPSIS", prev_scene_text="PRIOR SCENE", config with `_characters` set so character_section is non-empty), the captured scene prompt contains "Story so far", "Previous scene", "distinct, consistent voice", "## Screenwriting Craft", "on-the-nose", and "TITLE:".
    - for the FIRST scene (episode_index 0, synopsis="", prev_scene_text=""), the captured prompt does NOT contain "Story so far" or "Previous scene".
    - the recorded json_mode for the scene call is False.
    Use a config shaped like `{"episodes": [{"summary": "Scene 1 ..."}, {"summary": "Scene 2 ..."}], "_characters": [<one character dict>]}` so character_section renders. Build `_characters` to match `_build_character_section`'s expected input (inspect that method's read of the list).
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest app/tests/test_scene_compare.py -x -q && pytest app/tests/test_continuity_generation.py app/tests/test_character_voice_injection.py app/tests/test_craft_guidance.py app/tests/test_wizard_injection.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_scene_compare.py regenerate tests pass in isolation.
    - All 24 prior script suite tests (continuity 10, voice 8, craft 6) + 3 wizard tests stay green.
    - regenerate_single_scene reuses _generate_one_scene (no divergent prompt); _generate_scripts delegates to the same helper.
  </acceptance_criteria>
  <done>Single-scene regeneration via the improved path works and is tested; the batch path is behavior-identical and all prior suites pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regenerate-scene (preview) + keep-scene-version (persist) endpoints and schemas</name>
  <files>backend/app/models/schemas.py, backend/app/api/endpoints/wizards.py, backend/app/tests/test_scene_compare.py</files>
  <read_first>
    backend/app/api/endpoints/wizards.py:116-129 (owner-auth header), 240-285 (script_writer_wizard persist branch — flag_modified + ScreenplayContent + stale-marking)
    backend/app/api/endpoints/phase_data.py:21-53 (_mark_breakdown_stale/_mark_shotlist_stale — no-commit, conditional)
    backend/app/models/schemas.py:483-558 (WizardRunRequest + ScreenplayContentResponse shapes)
    backend/app/models/database.py:286-297 (ScreenplayContent)
  </read_first>
  <behavior>
    - POST /api/wizards/regenerate-scene with {project_id, phase, episode_index}: owner gets the new {title, content, episode_index} preview; the store is UNCHANGED (no ScreenplayContent added, no screenplays[] mutation, no stale flags).
    - A non-owner (different current_user) gets 404.
    - POST /api/wizards/keep-scene-version with {project_id, phase, episode_index, title, content}: owner — screenplays[episode_index] in screenplay_editor PhaseData.content is replaced with {title,content,episode_index}; content["synopsis"] is unchanged (D-49-05); the matching ScreenplayContent row (by formatted_content.episode_index, fallback to ordering) has content+formatted_content updated; breakdown_stale and shotlist_stale become True WHEN a breakdown/shots exist; returns {status:"success", episode_index}.
    - keep-current is the absence of a keep call (no endpoint needed) — verified implicitly by the preview test asserting no persistence.
    - A non-owner keep gets 404.
  </behavior>
  <action>
    In schemas.py (after ScreenplayContentResponse, ~line 558) add three BaseModels mirroring WizardRunRequest's `project_id: UUID` style:
    - `RegenerateSceneRequest`: `project_id: UUID`, `phase: str = "write"`, `episode_index: int`.
    - `RegenerateSceneResponse`: `title: str`, `content: str`, `episode_index: int`, `error: Optional[str] = None` (plain BaseModel — dict-built, NO from_attributes).
    - `KeepSceneVersionRequest`: `project_id: UUID`, `phase: str = "write"`, `episode_index: int`, `title: str`, `content: str`.

    In wizards.py add two endpoints, each copying the run_wizard owner-auth header VERBATIM (Depends(get_current_user), Depends(get_db), Project filter on id + owner_id==current_user.id, 404 "Project not found"):

    `@router.post("/regenerate-scene", response_model=schemas.RegenerateSceneResponse)` — after the owner check: build `bible_context = build_bible_context(db, project)`, `project_context = _get_project_context(db, project, bible_context=bible_context)`. Assemble the regenerate `config`: read the scene ListItems under PhaseData(phase="scenes", subsection_key="scene_list") ordered by sort_order into `episodes` (same query shape as _get_character_data but phase="scenes"/key="scene_list"; map each ListItem to `{"item_type": li.item_type, **(li.content or {})}`), set `config["_characters"] = _get_character_data(db, project.id)`, and carry `runtime_target`/`custom_guidance` from the stored script-wizard config if available (else "" — discretion: read WizardRun config if present, otherwise omit). Read the screenplay_editor PhaseData.content to source continuity inputs: `synopsis = content.get("synopsis","")` and `prev_scene_text = screenplays[episode_index-1]["content"]` when `episode_index > 0` and that slot exists, else "". Call `result = await template_ai_service.regenerate_single_scene(config, project_context, request.episode_index, synopsis, prev_scene_text)` and return it directly (FastAPI coerces the dict to RegenerateSceneResponse). Write NOTHING to the DB in this endpoint (D-49-02). Document inline that this endpoint is synchronous and may exceed the 30s API timeout — the FRONTEND calls it with CHAT_TIMEOUT (120s) per 49-02; do not add background-task plumbing.

    `@router.post("/keep-scene-version")` — after the owner check: query the screenplay_editor PhaseData (project_id, phase=request.phase, subsection_key="screenplay_editor"); 404 if absent. `content = dict(phase_data.content or {})`; `screenplays = list(content.get("screenplays", []))`; validate `0 <= request.episode_index < len(screenplays)` (404/400 if out of range); set `screenplays[request.episode_index] = {"title": request.title, "content": request.content, "episode_index": request.episode_index}`; `content["screenplays"] = screenplays` (leave `content["synopsis"]` untouched — D-49-05); `phase_data.content = content`; `flag_modified(phase_data, "content")`. Then update the matching ScreenplayContent: query all rows for project_id, pick the one whose `formatted_content.get("episode_index") == request.episode_index`; if none matches, fall back to ordering (the row at array position episode_index among rows sorted by created_at/id — NO migration); set `sc.content = request.content` and `sc.formatted_content = {"title": request.title, "content": request.content, "episode_index": request.episode_index}`. Call `_mark_breakdown_stale(db, project.id)` and `_mark_shotlist_stale(db, project.id)` (keep-new ONLY — D-49-02), then `db.commit()`. Return `{"status": "success", "episode_index": request.episode_index}`.

    Extend test_scene_compare.py with FastAPI TestClient tests authenticated as the mock owner (Bearer mock-token per CLAUDE.md; follow the auth setup used by an existing endpoint test such as test_wizard_injection.py or test_api.py for app/client/DB fixtures). Patch `app.services.template_ai_service.chat_completion` so regenerate returns deterministic native text. Cover:
    - regenerate-scene returns {title,content,episode_index} and DOES NOT persist: after the call, no new ScreenplayContent row exists, the stored screenplays[] is unchanged, and breakdown_stale/shotlist_stale are unchanged.
    - keep-scene-version persists into screenplays[episode_index] (assert the stored slot title+content) AND updates the matching ScreenplayContent row, AND (with a breakdown element + a shot seeded) flips breakdown_stale and shotlist_stale to True; content["synopsis"] is unchanged.
    - owner-auth: a request for a project owned by a different user returns 404 for BOTH endpoints.
    Seed the screenplay_editor PhaseData with a 2-scene screenplays list + a synopsis, scene_list ListItems, a character, and (for the stale assertions) one BreakdownElement and one Shot — reuse seeding helpers/fixtures from existing endpoint tests where present.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest app/tests/test_scene_compare.py -x -q && pytest app/tests/test_continuity_generation.py app/tests/test_character_voice_injection.py app/tests/test_craft_guidance.py app/tests/test_wizard_injection.py -q</automated>
  </verify>
  <acceptance_criteria>
    - regenerate-scene returns a preview and writes nothing (assertion-verified).
    - keep-scene-version persists screenplays[i] + matching ScreenplayContent + marks both stale; leaves synopsis untouched.
    - Both endpoints 404 for a non-owner.
    - All 27 prior tests remain green.
  </acceptance_criteria>
  <done>Both endpoints exist, are owner-scoped, and the preview/keep persistence semantics are proven by tests; prior suites stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/wizards/regenerate-scene | Authenticated request crosses here; project_id/episode_index are untrusted input |
| client → /api/wizards/keep-scene-version | Authenticated write request; title/content are AI-generated screenplay text persisted to the store |
| service → OpenAI (chat_completion) | Outbound LLM call (existing trust level, unchanged by this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Elevation of Privilege | regenerate-scene / keep-scene-version | mitigate | Owner-scoped query (id + owner_id==current_user.id) + 404 on miss, copied verbatim from run_wizard:124-129; tested by non-owner-404 assertions |
| T-49-02 | Tampering | keep-scene-version episode_index | mitigate | Bounds-check `0 <= episode_index < len(screenplays)`; write only the single targeted slot; leave synopsis + other slots untouched (D-49-05) |
| T-49-03 | Information Disclosure | regenerate-scene preview | mitigate | Preview returns only the requesting owner's regenerated scene; no DB write, no cross-project read (project pre-filtered by owner) |
| T-49-04 | Denial of Service | regenerate-scene latency (max_tokens=4000, ~up to 60s) | accept | Internal tool, single authenticated owner; synchronous call bounded by upstream OpenAI; frontend uses 120s CHAT_TIMEOUT. No rate-limit change needed (existing RateLimitMiddleware applies) |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency added in this phase (D-49 "no new dependency"); no install task → gate N/A. If an install is introduced, halt and run the legitimacy gate |
</threat_model>

<verification>
- `cd backend && source venv/bin/activate && pytest app/tests/test_scene_compare.py -x -q` — new suite passes in isolation.
- `cd backend && source venv/bin/activate && pytest app/tests/test_continuity_generation.py app/tests/test_character_voice_injection.py app/tests/test_craft_guidance.py app/tests/test_wizard_injection.py -q` — the 27 prior tests stay green.
- No migration, no new column, no new dependency added (D-49-03 / D-49 scope).
</verification>

<success_criteria>
- `_generate_one_scene` is the single shared per-scene path; both `_generate_scripts` (loop) and `regenerate_single_scene` (single call) use it.
- regenerate-scene returns a preview with the improved-path anchors present for a non-first scene and writes nothing.
- keep-scene-version persists exactly one scene slot + the matching ScreenplayContent, marks breakdown+shotlist stale, leaves the global synopsis untouched.
- Both endpoints are owner-scoped (404 on non-owned). All 27 prior tests green.
</success_criteria>

<output>
Create `.planning/phases/49-side-by-side-quality-compare/49-01-SUMMARY.md` when done.
</output>
