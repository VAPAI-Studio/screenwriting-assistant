---
phase: 71-mode-aware-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/agent_review_middleware.py
  - backend/app/api/endpoints/wizards.py
  - backend/app/tests/test_agent_review_middleware.py
autonomous: true
requirements: [SREV-01]
task_count: 2

must_haves:
  truths:
    - "In a connected show, script_writer_wizard review surfaces continuity considerations checked against the prior-episode summaries (character/plot coherence)."
    - "In anthology and standalone modes, review performs no cross-episode continuity checks — the merge prompt is byte-identical to today (no continuity block)."
    - "Zero mapped review agents in connected mode still returns raw output unchanged (REVW-04 pass-through preserved); continuity context enriches an existing review, never forces one."
  artifacts:
    - path: "backend/app/services/agent_review_middleware.py"
      provides: "review_step_output + _merge_reviews accept optional continuity_context; bounded coherence instruction injected into merge prompt only when present"
      contains: "continuity_context"
    - path: "backend/app/api/endpoints/wizards.py"
      provides: "connected-mode + script_writer_wizard call site that builds the prior-episode block and threads it as continuity_context"
      contains: "continuity_context"
    - path: "backend/app/tests/test_agent_review_middleware.py"
      provides: "pytest coverage: connected threads continuity into merge prompt; anthology/standalone pass None (no block); zero-agent connected pass-through"
      contains: "continuity"
  key_links:
    - from: "backend/app/api/endpoints/wizards.py"
      to: "agent_review_middleware.review_step_output"
      via: "continuity_context kwarg (connected + script_writer_wizard only)"
      pattern: "continuity_context\\s*="
    - from: "backend/app/services/agent_review_middleware.py review_step_output"
      to: "_merge_reviews"
      via: "continuity_context threaded into merge prompt construction"
      pattern: "continuity_context"
    - from: "backend/app/api/endpoints/wizards.py"
      to: "_build_prior_episodes_block"
      via: "reuse Phase 68 prior-episode block as coherence reference (episode_number order, cap 8, stale-regen already done by pre-pass)"
      pattern: "_build_prior_episodes_block"
---

<objective>
Make the agent review middleware mode-aware for `script_writer_wizard` in `connected` shows: thread the existing Phase 68 prior-episode summary block into the review/merge step as an optional `continuity_context`, and inject a BOUNDED coherence instruction into the merge prompt when it is present. Anthology/standalone and zero-mapped-agent paths stay byte-identical to today.

Purpose: Satisfy SREV-01 — connected-mode episode review additionally considers character/plot coherence against prior-episode summaries — without standing up a continuity-inconsistency engine (the deferred D-out scope).

Output: Additive `continuity_context` parameter threaded through `review_step_output` → `_merge_reviews` → merge prompt; one conditional connected-mode call site in `wizards.py`; pytest coverage for the three behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-mode-aware-review/71-CONTEXT.md
@.planning/phases/71-mode-aware-review/71-RESEARCH.md
@backend/app/services/agent_review_middleware.py
@backend/app/api/endpoints/wizards.py
@backend/app/utils/bible_context.py
@backend/app/tests/test_agent_review_middleware.py

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

Seam (backend/app/services/agent_review_middleware.py):
  async def review_step_output(self, phase, subsection_key, raw_output, owner_id,
                               session_factory, wizard_type=None) -> Dict
      # REVW-04: returns {output, agents_consulted:[], review_applied:False} when no agents / no successful reviews.
      # Calls: refined_output = await self._merge_reviews(raw_output, successful, merge_type)
  async def _merge_reviews(self, raw_output, reviews, wizard_type) -> Optional[Dict]
      # Builds system_prompt = MERGE_SYSTEM_PROMPT.format(agent_count, wizard_type, schema_description)
      # then chat_completion(messages=[{system}, {user}], temperature=0.3, json_mode=True)
  MERGE_SYSTEM_PROMPT  # module-level template, mode-agnostic today.

Reuse (backend/app/utils/bible_context.py):
  def _build_prior_episodes_block(db, show, project) -> Optional[str]
      # Strictly-prior episodes (episode_number < current), order by episode_number ASC,
      # drop empty/whitespace summaries, cap to most-recent PRIOR_EPISODE_CAP=8, stale tagged.
      # Returns None when no priors. THIS is the coherence reference — do not rebuild ordering/staleness.

Call site (backend/app/api/endpoints/wizards.py):
  run_wizard (~line 126): loads project; connected pre-pass at 140-143 already runs
      regenerate_stale_priors(db, show, project) (so priors are FRESH in this request).
  build_bible_context(db, project) at 146.
  Background task _run_wizard_background (line 63) calls review_step_output at line 91-98.
  NOTE: review runs in the BACKGROUND task with its own SessionLocal — build the continuity
  STRING in the request session and pass it through as a plain string arg (same pattern as
  bible_context: str = None), never an ORM object or live session.

Test conventions (backend/app/tests/test_agent_review_middleware.py):
  Fixtures: db_session, owner_id, make_agent.
  Mock pattern: with patch("app.services.agent_review_middleware.chat_completion",
      new_callable=AsyncMock, side_effect=[mock_review, ..., mock_merge]) as mock_chat:
  Assert prompt content via mock_chat.call_args / call_args_list -> kwargs["messages"].
  script_writer_wizard merge mock must return {"screenplays": [...]} to pass schema validation.
  Run: cd backend && source venv/bin/activate && pytest app/tests/test_agent_review_middleware.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread continuity_context through the middleware + bounded merge-prompt block</name>
  <files>backend/app/services/agent_review_middleware.py, backend/app/tests/test_agent_review_middleware.py</files>
  <behavior>
    - When continuity_context is a non-empty string, the merge prompt (the system message sent to chat_completion in _merge_reviews) contains the prior-episode text AND a bounded coherence instruction (flag character/plot coherence considerations against prior summaries; explicitly NOT exhaustive inconsistency detection). Per D4.
    - When continuity_context is None (default), the merge prompt is byte-identical to today — no continuity block, no extra instruction. Per D5.
    - Zero mapped agents → still returns {output: raw_output, review_applied: False}; chat_completion never called (REVW-04 preserved regardless of continuity_context). Per D3.
  </behavior>
  <action>
Add `continuity_context: Optional[str] = None` as the final parameter of `AgentReviewMiddleware.review_step_output` and of `_merge_reviews`. In `review_step_output`, pass `continuity_context` through to the `_merge_reviews(...)` call. Do NOT touch the zero-agent / no-successful-reviews early returns — REVW-04 pass-through must be unchanged (D3).

In `_merge_reviews`, after building the existing `system_prompt` (the `MERGE_SYSTEM_PROMPT.format(...)` result), conditionally append a continuity block ONLY when `continuity_context` is a truthy/non-whitespace string. Build the block as directive prose appended to the system prompt: a header naming it as prior-episode summaries for coherence reference, the `continuity_context` text itself, and a bounded instruction telling the reviewer to additionally flag character/plot COHERENCE CONSIDERATIONS against these prior episodes while EXPLICITLY forbidding exhaustive inconsistency detection / a correctness audit of prior episodes (honor the deferred D-out scope, D4). Keep it a single appended segment so that when `continuity_context` is None the prompt string is identical to the pre-Phase-71 output (D5). Do not alter temperature, json_mode, schema validation, or the user message.

Add tests to `test_agent_review_middleware.py` (mirror existing patterns — db_session, owner_id, make_agent, patch chat_completion with AsyncMock + side_effect, script_writer_wizard merge mock returns `{"screenplays": [...]}`):
  - test_connected_threads_continuity_into_merge_prompt: call review_step_output with a script_writer_wizard, ≥1 mapped agent, and continuity_context set to a recognizable prior-episode string (e.g. contains "Episode 1"). Inspect the merge call's system message via mock_chat.call_args_list (the last chat_completion call is the merge) and assert it contains the prior-episode text AND a coherence-instruction token, AND a token forbidding exhaustive/inconsistency auditing.
  - test_no_continuity_context_merge_prompt_clean: same setup but continuity_context omitted/None; assert the merge system message contains NO continuity/coherence tokens.
  - test_zero_agents_passthrough_with_continuity: zero mapped agents + continuity_context set, connected; assert result["output"] == raw_output, review_applied is False, chat_completion never called.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest app/tests/test_agent_review_middleware.py -x -q</automated>
  </verify>
  <done>review_step_output and _merge_reviews accept continuity_context; merge prompt gains a bounded coherence block only when it is present; the three new tests pass and all pre-existing middleware tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build + thread the connected-mode prior-episode block at the wizard call site</name>
  <files>backend/app/api/endpoints/wizards.py</files>
  <action>
In `run_wizard` (where the connected pre-pass `regenerate_stale_priors` + `build_bible_context` already run, ~lines 140-146): when the project belongs to a show in `connected` mode AND `request.wizard_type == "script_writer_wizard"`, build the prior-episode coherence block by reusing `_build_prior_episodes_block(db, show, project)` (import it from `...utils.bible_context`). The pre-pass at line 143 has already regenerated stale priors, so the block reads fresh summaries; ordering/cap/staleness are handled inside the reused helper (episode_number ASC, cap 8 — never positional). Capture the result as a plain string (or None) in the REQUEST session, before handing off to the background task.

Thread this string into the background task: add a `continuity_context: str = None` parameter to `_run_wizard_background`, pass the captured string when calling `background_tasks.add_task(_run_wizard_background, ...)`, and forward it into the `agent_review_middleware.review_step_output(...)` call (line 91-98) as `continuity_context=continuity_context`. Mirror the existing `bible_context: str = None` plain-string hand-off pattern exactly (no ORM objects / live sessions cross the background boundary — async/session safety).

Gate strictly: anthology, standalone, `show_id` NULL, and any non-`script_writer_wizard` wizard MUST leave `continuity_context` as None so review_step_output is called identically to today (D5). Reuse the `show` already fetched in the connected pre-pass block rather than re-querying; do not duplicate the `ContinuityMode.CONNECTED.value` mode check logic beyond what is needed for the gate. Caller-commits convention: this endpoint owns its own commit flow — do not add commits inside the helper.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && python -c "import ast,sys; src=open('app/api/endpoints/wizards.py').read(); body=[l for l in src.splitlines() if not l.lstrip().startswith('#')]; b='\n'.join(body); assert '_build_prior_episodes_block' in b, 'block builder not wired'; assert 'continuity_context' in b, 'continuity_context not threaded'; ast.parse(src); print('OK')"</automated>
  </verify>
  <done>run_wizard builds the prior-episode block only for connected-mode script_writer_wizard runs, threads it through _run_wizard_background into review_step_output as continuity_context; all other modes/wizards pass None; module parses and imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request session → background task | Continuity string must cross as a plain string; live ORM sessions/objects must not leak across the async boundary (DetachedInstanceError / cross-session corruption). |
| user content → AI merge prompt | Prior-episode summaries are AI-generated app data injected into the merge prompt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Information disclosure | continuity_context crossing background boundary | mitigate | Pass as plain string built in the request session (mirror `bible_context: str = None`); no ORM object/live session crosses the boundary. |
| T-71-02 | Denial of service | merge prompt token blow-up from prior-episode block | mitigate | Reuse `_build_prior_episodes_block` PRIOR_EPISODE_CAP=8 + bounded auto-summaries; no unbounded concatenation added. |
| T-71-03 | Tampering | scope creep into inconsistency-detection engine | accept | D4 bounded instruction explicitly forbids exhaustive auditing; out-of-scope detection stays deferred. |
| T-71-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies installed this phase — additive param + prompt block only; nothing to verify. |
</threat_model>

<verification>
- `cd backend && source venv/bin/activate && pytest app/tests/test_agent_review_middleware.py -q` — all middleware tests (existing + 3 new) pass.
- `cd backend && source venv/bin/activate && python -c "import app.api.endpoints.wizards"` — wizards module imports cleanly.
- Grep gate (header-comment safe): connected-mode script_writer_wizard gate + `_build_prior_episodes_block` reuse present in wizards.py; `continuity_context` threaded through both files.
- Pre-existing unrelated failures (`test_yolo_integration.py`, `test_session_isolation.py`, `test_mcp_foundation.py`) are NOT in scope and may remain red (RESEARCH Test surface note).
</verification>

<success_criteria>
1. Connected `script_writer_wizard` review merge prompt contains the prior-episode summaries plus a bounded character/plot coherence instruction (SREV-01, success criterion #1).
2. Anthology/standalone (and non-script_writer wizards, and zero mapped agents) produce a merge path / prompt byte-identical to today — `continuity_context` is None (success criterion #2, D5).
3. Zero-agent pass-through (REVW-04) preserved in connected mode — continuity context enriches an existing review, never forces one (D3).
</success_criteria>

<output>
Create `.planning/phases/71-mode-aware-review/71-01-SUMMARY.md` when done.
</output>
