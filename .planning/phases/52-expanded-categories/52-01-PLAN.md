---
phase: 52-expanded-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/database.py
  - backend/app/services/breakdown_service.py
  - backend/app/models/schemas.py
  - frontend/src/types/index.ts
  - frontend/src/lib/constants.ts
  - backend/app/tests/test_breakdown_service.py
  - backend/app/tests/test_breakdown_api.py
autonomous: true
requirements: [CATG-01, CATG-02, CATG-03]

must_haves:
  truths:
    - "Schema accepts a new category: BreakdownElementCreate(category='set_dressing') validates with no 422 (CATG-01, D-52-02)"
    - "A category NOT in the 10-value set still rejects with a validation error (CATG-01 — gate is real, not removed)"
    - "An AI extraction returning a new-category element (e.g. set_dressing) persists with that category and is returned by the service (CATG-01, D-52-04)"
    - "The existing 5 categories still extract, validate, and persist unchanged — no regression (CATG-02, D-52-02)"
    - "No new migration file exists and category remains a String(50) column — existing rows untouched (CATG-02, D-52-02)"
    - "The frontend builds (tsc) with 10 categories; CategoryTabs renders 10 tabs by iterating the constant (CATG-03, D-52-03)"
    - "The on-screen-only CRITICAL RULES in EXTRACTION_SYSTEM_PROMPT are preserved verbatim (D-52-04)"
  artifacts:
    - path: "backend/app/models/database.py"
      provides: "BreakdownCategory enum with 10 members"
      contains: "SET_DRESSING ="
    - path: "backend/app/models/schemas.py"
      provides: "BreakdownElementCreate.category regex accepting 10 values"
      contains: "set_dressing"
    - path: "backend/app/services/breakdown_service.py"
      provides: "EXTRACTION_SYSTEM_PROMPT CATEGORIES list + precedence note + ExtractedElement schema description, all listing 10 categories"
      contains: "set_dressing"
    - path: "frontend/src/types/index.ts"
      provides: "BreakdownCategory union with 10 literals"
      contains: "set_dressing"
    - path: "frontend/src/lib/constants.ts"
      provides: "BREAKDOWN_CATEGORIES + CATEGORY_COLORS + ELEMENT_EXTENDED_FIELDS all covering 10 categories"
      contains: "set_dressing"
  key_links:
    - from: "schemas.py BreakdownElementCreate.category regex"
      to: "database.py BreakdownCategory enum + breakdown_service.py prompt"
      via: "lockstep — all backend sites list the identical 10 values"
      pattern: "set_dressing|animal|sfx|makeup_hair|extras"
    - from: "frontend/src/types/index.ts BreakdownCategory union"
      to: "constants.ts CATEGORY_COLORS + ELEMENT_EXTENDED_FIELDS (Record<BreakdownCategory,...>)"
      via: "tsc exhaustiveness — adding union members REQUIRES adding map keys or build fails"
      pattern: "set_dressing|animal|sfx|makeup_hair|extras"
    - from: "constants.ts BREAKDOWN_CATEGORIES"
      to: "frontend/src/components/Breakdown/CategoryTabs.tsx"
      via: "CategoryTabs.map(BREAKDOWN_CATEGORIES) auto-renders tabs + filter (no code change)"
      pattern: "BREAKDOWN_CATEGORIES.map"
---

<objective>
Broaden the breakdown element taxonomy from 5 to 10 categories, additively, across every definition site so extraction, validation, persistence, and the UI all recognize the new set. Adds: set_dressing, animal, sfx, makeup_hair, extras (D-52-01).

Purpose: v7.0 Breakdown Fidelity — capture more production-relevant on-screen elements (CATG-01/02/03). Each new category is a physically-on-screen production-department element, consistent with the existing "physically present" rule.

Output: 5 backend/frontend definition sites updated in lockstep, prompt guidance extended, tests proving new-category accept + extract + persist with no regression, and a clean frontend build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-expanded-categories/52-CONTEXT.md

<interfaces>
<!-- The 6 definition sites (verified against source). The new 5 categories MUST be -->
<!-- added in LOCKSTEP. snake_case values; Title Case labels per D-52-01. -->

New categories (D-52-01):
| value         | label           |
|---------------|-----------------|
| set_dressing  | Set Dressing    |
| animal        | Animal          |
| sfx           | SFX             |
| makeup_hair   | Makeup & Hair   |
| extras        | Extras          |

BACKEND site 1 — database.py:138-143 (enum, snake_case values):
```
class BreakdownCategory(str, enum.Enum):
    CHARACTER = "character"
    LOCATION = "location"
    PROP = "prop"
    WARDROBE = "wardrobe"
    VEHICLE = "vehicle"
```
Note: database.py:548 `category = Column(String(50))` — free string, NO migration.

BACKEND site 2 — breakdown_service.py:101-106 (EXTRACTION_SYSTEM_PROMPT CATEGORIES list).
The CRITICAL RULES block (lines 88-94) and DEDUPLICATION block (96-99) MUST stay verbatim.

BACKEND site 3 — schemas.py:698 (THE GATE):
```
category: str = Field(..., pattern="^(character|location|prop|wardrobe|vehicle)$")
```

BACKEND site 4 — breakdown_service.py:44-46 (ExtractedElement structured-output schema description):
```
category: str = Field(
    description="One of: character, location, prop, wardrobe, vehicle"
)
```

FRONTEND site 5 — types/index.ts:269 (union):
```
export type BreakdownCategory = 'character' | 'location' | 'prop' | 'wardrobe' | 'vehicle';
```

FRONTEND site 6 — constants.ts: THREE places keyed by the union (all must cover 10 or tsc fails):
- BREAKDOWN_CATEGORIES (278-284): Array<{value,label}> — drives the UI tabs.
- CATEGORY_COLORS (286-292): Record<BreakdownCategory,string> — EXHAUSTIVE, tsc errors if a key is missing.
- ELEMENT_EXTENDED_FIELDS (294-320): Record<BreakdownCategory, Array<...>> — EXHAUSTIVE, tsc errors if a key is missing.

CategoryTabs.tsx (20,38) iterates BREAKDOWN_CATEGORIES — auto-renders, NO code change.
types/index.ts:313 `counts_by_category: Record<BreakdownCategory, number>` is a response TYPE (not an object literal) consumed via `?? 0` — no edit needed, will not error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend taxonomy — all 4 backend sites in lockstep</name>
  <read_first>
    - backend/app/models/database.py:138-143 (enum) and :548 (String column — confirm no migration)
    - backend/app/services/breakdown_service.py:42-55 (ExtractedElement) and :84-106 (full prompt incl. CRITICAL RULES + DEDUPLICATION + CATEGORIES)
    - backend/app/models/schemas.py:697-701 (BreakdownElementCreate)
  </read_first>
  <files>backend/app/models/database.py, backend/app/services/breakdown_service.py, backend/app/models/schemas.py</files>
  <action>
    Add the 5 new categories to ALL FOUR backend sites with identical snake_case values (set_dressing, animal, sfx, makeup_hair, extras), per D-52-01/D-52-02. These MUST move together — the schema regex (schemas.py) is the gate that 422s a new-category POST/extract if not extended.

    1. database.py BreakdownCategory enum (after VEHICLE): add SET_DRESSING = "set_dressing", ANIMAL = "animal", SFX = "sfx", MAKEUP_HAIR = "makeup_hair", EXTRAS = "extras". Do NOT touch the String(50) column at :548 — no migration (CATG-02).
    2. schemas.py:698 — extend the regex to ^(character|location|prop|wardrobe|vehicle|set_dressing|animal|sfx|makeup_hair|extras)$. This is THE gate.
    3. breakdown_service.py EXTRACTION_SYSTEM_PROMPT CATEGORIES list (:101-106) — append 5 on-screen-only description lines, each consistent with the CRITICAL RULES (physically visible, not implied/off-screen), per D-52-04: set_dressing = furniture/décor dressing a location, not handled as a prop (couch, paintings, rugs); animal = animals appearing on screen (dogs, horses, birds); sfx = practical effects that physically occur on screen (fire, smoke, rain, explosions, breaking glass), NOT implied/off-screen; makeup_hair = notable makeup/hair/prosthetics as a production element (wounds, aging, distinctive hairstyles, appliances); extras = background performers / crowd visible on screen. Then add a brief precedence note for ambiguous cases (D-52-04): a ridden horse → animal (living creature wins over vehicle); set_dressing vs prop → if handled/featured by a character it is a prop, else set_dressing. PRESERVE the CRITICAL RULES (:88-94) and DEDUPLICATION (:96-99) blocks verbatim — do not reword them.
    4. breakdown_service.py ExtractedElement.category Field description (:44-46) — update the "One of: ..." string to list all 10 categories so the structured-output schema teaches the AI the full set.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && python -c "from app.models.database import BreakdownCategory; from app.models.schemas import BreakdownElementCreate; from app.services.breakdown_service import EXTRACTION_SYSTEM_PROMPT, ExtractedElement; assert {c.value for c in BreakdownCategory} == {'character','location','prop','wardrobe','vehicle','set_dressing','animal','sfx','makeup_hair','extras'}; BreakdownElementCreate(category='set_dressing', name='Couch'); [print(c) for c in ['set_dressing','animal','sfx','makeup_hair','extras'] if c not in EXTRACTION_SYSTEM_PROMPT and exit(1)]; assert 'PHYSICALLY PRESENT ON SCREEN' in EXTRACTION_SYSTEM_PROMPT and 'merely mentioned in dialogue or backstory' in EXTRACTION_SYSTEM_PROMPT; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - BreakdownCategory enum has exactly 10 members with the snake_case values above.
    - BreakdownElementCreate(category='set_dressing') validates; a value outside the 10-set (e.g. 'nonsense') raises ValidationError.
    - All 5 new values + the verbatim CRITICAL RULES text are present in EXTRACTION_SYSTEM_PROMPT.
    - ExtractedElement.category description lists all 10 categories.
    - No new migration file added; database.py:548 remains String(50).
  </acceptance_criteria>
  <done>All 4 backend sites list the identical 10 categories; the regex gate accepts new values and still rejects unknown ones; CRITICAL RULES preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend taxonomy — union + all 3 category-keyed sites (tsc gate)</name>
  <read_first>
    - frontend/src/types/index.ts:269 (union) and :313 (counts_by_category — confirm it is a type, no edit)
    - frontend/src/lib/constants.ts:278-320 (BREAKDOWN_CATEGORIES, CATEGORY_COLORS, ELEMENT_EXTENDED_FIELDS)
    - frontend/src/components/Breakdown/CategoryTabs.tsx (confirms auto-render — no edit)
  </read_first>
  <files>frontend/src/types/index.ts, frontend/src/lib/constants.ts</files>
  <action>
    Add the 5 new categories to the union AND to ALL THREE category-keyed structures in constants.ts. CATEGORY_COLORS and ELEMENT_EXTENDED_FIELDS are Record&lt;BreakdownCategory,...&gt; — once the union grows, tsc errors with "missing properties" unless every map gets the 5 new keys. This is the build gate (CATG-03, D-52-03).

    1. types/index.ts:269 — extend the union to include 'set_dressing' | 'animal' | 'sfx' | 'makeup_hair' | 'extras'. Do NOT edit :313 (counts_by_category is a response type consumed via `?? 0`).
    2. constants.ts BREAKDOWN_CATEGORIES (278-284) — append {value:'set_dressing',label:'Set Dressing'}, {value:'animal',label:'Animal'}, {value:'sfx',label:'SFX'}, {value:'makeup_hair',label:'Makeup & Hair'}, {value:'extras',label:'Extras'} (Title Case labels per D-52-01).
    3. constants.ts CATEGORY_COLORS (286-292) — add a distinct rgb(...) entry for each of the 5 new keys (choose tasteful Tailwind-400-range colors not already used; e.g. set_dressing teal, animal orange/amber-variant, sfx rose/pink, makeup_hair fuchsia, extras slate — any distinct hues are fine).
    4. constants.ts ELEMENT_EXTENDED_FIELDS (294-320) — add an entry for each of the 5 new keys. Reuse the generic prop-style 3-field shape ({key:'specs',label:'Specifications',type:'textarea'}, {key:'owner',label:'Owner / Responsible',type:'text'}, {key:'status',label:'Status',type:'text'}) unless a more natural label fits (e.g. extras → {key:'count',label:'Headcount',type:'text'}); keep it simple, do not over-engineer.

    Do NOT modify CategoryTabs.tsx — it iterates BREAKDOWN_CATEGORIES and will auto-render the 10 tabs (D-52-03). Tab overflow polish is OUT OF SCOPE unless trivially needed.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc + vite) completes with no type errors.
    - The BreakdownCategory union, BREAKDOWN_CATEGORIES, CATEGORY_COLORS, and ELEMENT_EXTENDED_FIELDS each cover all 10 categories.
    - Labels are Title Case: 'Set Dressing', 'Animal', 'SFX', 'Makeup & Hair', 'Extras'.
    - CategoryTabs.tsx is unchanged.
  </acceptance_criteria>
  <done>Frontend builds clean with 10 categories defined in lockstep across the union and all three category-keyed maps; CategoryTabs renders 10 tabs without code change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — new-category accept + extract + persist, no regression</name>
  <read_first>
    - backend/app/tests/test_breakdown_service.py:262-312 (test_extraction_produces_elements pattern: _setup_project_with_screenplay, _mock_extraction_response, ExtractedElement/ExtractedSceneAppearance) and :754-761 (on-screen rules guard)
    - backend/app/tests/test_breakdown_api.py:440-460 (counts_by_category API assertions) and the BreakdownElementCreate / POST element test pattern
  </read_first>
  <files>backend/app/tests/test_breakdown_service.py, backend/app/tests/test_breakdown_api.py</files>
  <behavior>
    - Test A (schema accept): BreakdownElementCreate(category='set_dressing', name='Antique Couch') validates with no error; BreakdownElementCreate(category='not_a_category', name='X') raises pydantic.ValidationError. (CATG-01)
    - Test B (extract + persist): an extract() run with a mocked AI returning a set_dressing element (and at least one existing-category element, e.g. character) persists a BreakdownElement with category='set_dressing' and it is queryable from the DB. (CATG-01, D-52-04)
    - Test C (no regression): existing 5-category extraction (character/prop/location) still completes and persists — reuse/keep the existing test_extraction_produces_elements green. (CATG-02)
  </behavior>
  <action>
    Add tests mirroring the existing patterns (use the module's _setup_project_with_screenplay, _mock_extraction_response, ExtractedElement, ExtractedSceneAppearance — import as the file already does). Place Test A as a pytest unit test asserting accept + reject. Place Test B as an async @patch("app.services.breakdown_service.chat_completion_structured", new_callable=AsyncMock) test that returns one set_dressing ExtractedElement with a scene_appearance and asserts the persisted row's category == 'set_dressing'. Do NOT weaken or delete existing tests (Test C is satisfied by the existing suite staying green). Keep all three target suites green.
  </action>
  <verify>
    <automated>cd backend && source venv/bin/activate && pytest app/tests/test_breakdown_service.py app/tests/test_breakdown_api.py app/tests/test_staleness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - New schema test asserts set_dressing accepted AND an unknown value rejected.
    - New extract test asserts a set_dressing element persists with category='set_dressing'.
    - All of test_breakdown_service.py, test_breakdown_api.py, test_staleness.py pass (no regression).
  </acceptance_criteria>
  <done>New tests prove a new category validates, extracts, and persists; the full breakdown + staleness suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (POST/PATCH breakdown element) | user/AI-supplied `category` string crosses into persistence |
| AI extraction → DB | model-returned `category` strings persisted as free String(50) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | schemas.py BreakdownElementCreate.category regex | mitigate | The regex remains a closed allow-list of exactly 10 values (Task 1) — extending it does not open it to arbitrary strings; Task 3 asserts an unknown value still rejects. This is the gate that prevents junk categories. |
| T-52-02 | Denial of Service (self-inflicted) | regex/enum/FE union NOT updated in lockstep | mitigate | If the regex is not extended alongside the enum/prompt, a new-category POST/extract 422s (broken feature). Task 1 forces all 4 backend sites together; Task 2 + tsc force the FE; the lockstep grep gate in <verification> catches drift. |
| T-52-03 | Information Disclosure | new categories in EXTRACTION_SYSTEM_PROMPT | accept | Categories are on-screen-only descriptors of the user's own script; no PII or secrets introduced. Internal tool. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this phase (no migration, no dependency). No legitimacy checkpoint required. |
</threat_model>

<verification>
Run after all tasks complete:

1. Backend suites green:
   `cd backend && source venv/bin/activate && pytest app/tests/test_breakdown_service.py app/tests/test_breakdown_api.py app/tests/test_staleness.py -q`
2. Frontend build clean:
   `cd frontend && npm run build`
3. Lockstep assertion — every new value present in the enum, the schema regex, the prompt, and the FE union/constant; and NO new migration file:
   `cd /Users/yvesfogel/Desktop/screenwriting-assistant && for v in set_dressing animal sfx makeup_hair extras; do grep -q "$v" backend/app/models/database.py && grep -q "$v" backend/app/models/schemas.py && grep -q "$v" backend/app/services/breakdown_service.py && grep -q "$v" frontend/src/types/index.ts && grep -q "$v" frontend/src/lib/constants.ts || { echo "MISSING SITE: $v"; exit 1; }; done && echo "LOCKSTEP OK"`
   `cd /Users/yvesfogel/Desktop/screenwriting-assistant && git diff --name-only --diff-filter=A HEAD | grep -E "migration|alembic" && { echo "UNEXPECTED NEW MIGRATION"; exit 1; } || echo "NO NEW MIGRATION (expected)"` (W1 fix: backend/migrations/ already exists, so a name match is meaningless — check only for NEWLY ADDED migration files via git diff)
</verification>

<success_criteria>
- BreakdownCategory enum, EXTRACTION_SYSTEM_PROMPT CATEGORIES + ExtractedElement description, and BreakdownElementCreate regex all list the identical 10 categories (CATG-01).
- A new-category element validates (no 422), extracts, and persists; an unknown category still rejects.
- Existing 5 categories and prior breakdown/staleness tests remain green; no migration file added; category column unchanged String(50) (CATG-02).
- Frontend builds (tsc) clean; CategoryTabs renders 10 tabs by iterating BREAKDOWN_CATEGORIES with no code change (CATG-03).
- CRITICAL RULES in the extraction prompt preserved verbatim (D-52-04).
</success_criteria>

<output>
Create `.planning/phases/52-expanded-categories/52-01-SUMMARY.md` when done.
</output>
