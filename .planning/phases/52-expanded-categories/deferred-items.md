# Deferred Items — Phase 52

- backend/requirements.txt has an uncommitted local change pinning `bcrypt<4.1`
  (passlib 1.7.4 incompatibility). Out of scope for plan 52-01 (not in files_modified;
  not introduced by any task). Left unstaged. Owner to decide whether to commit this
  environment pin separately.
